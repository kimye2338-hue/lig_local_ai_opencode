# 🐛 오류검토 보고서 — OpenCodeLIG

> **검토자:** Fable (mid) · 8개 영역 병렬 · **읽기 전용(수정 없음)**  
> **일자:** 2026-07-08 · **대상:** `agent_ops` 파이썬 66개 파일(~16k줄)  
> **높음 4건은 사람이 실제 코드로 교차확인 완료.**

---

## 📊 한눈에

- **총 53건** 발견 · 치명 0
- 🟧 **높음 4** · 🟨 보통 18 · ⬜ 낮음 31
- 크래시/보안/기능파손 위주로 선별됨 (스타일 지적 제외)

**영역별 건수**

- 코어/오케스트레이션: 7건
- LLM/런타임/게이트웨이: 8건
- 어댑터-COM/Windows: 6건 · 🟧1
- 어댑터-배치/웹/OCR: 6건 · 🟧1
- 지식/기억/위키: 6건
- 산출물/오피스/리포트: 4건
- 안전/상태/감사: 10건 · 🟧1
- 인프라/입출력/UI: 6건 · 🟧1

---

## 🟧 높음 — 우선 조치 권장

아래 4건은 **실제 파일을 열어 사람이 재확인**했습니다.

### 1. `command_guard.py:98`  ✅ 확정

**SAFE_PREFIXES의 'git diff'/'git log'가 --output=<파일> 옵션과 결합하면 셸 리다이렉트 없이 임의 파일을 자동 ALLOW로 덮어쓸(빈 diff면 truncate) 수 있다.**

- 분류: 보안 · 영역: 안전/상태/감사

analyze()는 safe prefix 후 체이닝/리다이렉트 문자([;&|<>^`\r\n], 191행)만 검사한다. 그러나 'git diff --output=agent_ops/core.py' 같은 명령은 이 문자를 전혀 포함하지 않으면서 git 자체 기능으로 대상 파일을 생성/덮어쓴다. 결과적으로 decision=ALLOW가 되어 guarded_run이 즉시 실행하고, 변경 없는 diff면 대상 파일이 0바이트로 truncate된다. 191행 주석이 막으려던 'type nul > file.py' 시나리오와 동일한 효과를 리다이렉트 문자 없이 달성하는 가드 우회다.

---

### 2. `agent_ops/adapters/solidworks_com.py:102`  ✅ 확정

**OpenDoc6의 문서 타입 인자를 0(swDocNONE)으로 하드코딩해 어떤 문서도 열리지 않고, RunMacro2의 ModuleName/ProcedureName도 빈 문자열이라 매크로 실행이 성립하지 않는다.**

- 분류: 로직오류 · 영역: 어댑터-COM/Windows

OpenDoc6(path, 0, 1, "", errors, warnings)에서 두 번째 인자는 swDocumentTypes_e(swDocPART=1/swDocASSEMBLY=2/swDocDRAWING=3)여야 하며 swDocNONE(0)으로는 열기가 실패해 model이 None이 되어 항상 '문서 열기 실패'로 끝난다. 열렸다 해도 105행 RunMacro2(macro, "", "", 0, 0)은 모듈/프로시저명이 비어 있어 실행할 프로시저를 지정하지 못한다(SolidWorks API는 프로시저명 필수). 파일럿 검증(available=False) 시 반드시 실패할 잠재 결함. 확장자에서 타입을 매핑(.sldprt/.sldasm/.slddrw)하고 .swp 매크로의 모듈·프로시저명(통상 main)을 지정해야 한다.

---

### 3. `workspace/agent_ops/adapters/browser_cdp.py:241`  ✅ 확정

**select_tab이 항상 실패 — Chrome의 /json/activate 응답은 JSON이 아닌 평문 'Target activated'인데 _json_get이 무조건 json.loads를 수행한다.**

- 분류: 로직오류 · 영역: 어댑터-배치/웹/OCR

Chrome DevTools HTTP 엔드포인트 /json/activate/<id>는 200과 함께 평문 'Target activated'를 반환한다(문서화된 동작). _json_get(246-249행)이 json.loads를 호출해 JSONDecodeError가 발생하고, execute의 광역 except가 ok=False로 변환하므로 탭은 실제로 활성화됐는데도 select_tab 액션은 매번 실패로 보고된다. 242행 _remember_tab(picked)도 실행되지 않아 '같은 탭 유지' 메커니즘(_ACTIVE_TAB_ID)이 갱신되지 않는다. 테스트(test_browser_adapter.py)는 ACTIONS 선언만 검사해 이 결함을 잡지 못한다.

---

### 4. `agent_ops/ui/hamster_overlay.py:522`  ✅ 확정

**_onscreen()이 re.search를 사용하지만 파일 어디에도 `import re`가 없어 NameError로 오버레이가 기동 실패한다.**

- 분류: 크래시 · 영역: 인프라/입출력/UI

첫 실행은 저장된 geometry가 없어 _onscreen이 호출되지 않아 정상 동작하지만, exit_app()→_save_position()이 종료 시 항상 hamster_pet_settings.json에 geometry를 저장하므로 두 번째 실행부터는 __init__ 535행에서 _onscreen(geom)이 호출되어 522행 `re.search(...)`에서 NameError가 발생, run_app()이 예외를 로그 후 re-raise하며 펫이 매번 뜨지 않게 된다. AST 검사로 `re`가 import/ImportFrom 어디에도 없음을 확인했다. pythonw(숨김창)로 실행되므로 사용자는 원인 없이 '펫이 안 뜬다'만 겪는다.

---

## 🟨 보통

#### `agent_ops/lig_runtime.py:192` — 크래시

게이트웨이가 message.content를 리스트(멀티파트)로 반환하면 call_llm이 content.strip()에서 AttributeError로 죽는다.

<sub>192행 `content = msg.get('content') or ''`는 content가 리스트여도 그대로 통과시키고, 199행/202행에서 `content.strip()`을 호출한다. toolcall_parser.py 158-168행은 바로 이 케이스('content가 리스트... call_llm 전체가 죽는 것을 막는다')를 명시적으로 방어했지만 런타임 쪽은 방어가 없다. tool call이 파싱되지 않은(status=none) 멀티파트 응답 1건이면 TransportError가 아닌 AttributeError가 발생해 재시도/폴백 정책을 타지 못하고 call_llm 전체가 크래시한다.</sub>

#### `agent_ops/lig_runtime.py:55` — 예외처리

LIG_GATEWAY_BASE_URL 미설정 시 base_url이 빈 문자열이 되어 urlopen이 ValueError('unknown url type')를 던지는데, default_transport가 이를 잡지 않아 call_llm이 원시 트레이스백으로 죽는다.

<sub>lig_providers.build_providers 195행은 gateway 미설정 시 base_url=''를 반환하고, _chat_completions_url('')은 '/v1/chat/completions'라는 상대경로를 만든다. default_transport의 except 절은 HTTPError/TimeoutError/URLError/OSError만 잡고 ValueError는 잡지 않으므로 TransportError로 변환되지 않는다. chat_with_fallback은 사전 가드가 있지만 call_llm을 직접 쓰는 경로(capability 라우팅 등)에서는 설정 누락 한 건이 깔끔한 stop 진단 대신 크래시가 된다.</sub>

#### `agent_ops/lig_runtime.py:302` — 로직오류

chat_with_fallback이 LIG_PROVIDER_PROFILE=local_openai 설정을 무시한다 — 게이트웨이 URL/KEY가 모두 있어야만 LIG 경로를 타고, 아니면 전혀 다른 설정(AGENTOPS_LLM_*)을 요구하는 레거시 클라이언트로 떨어진다.

<sub>build_providers/validate_config는 local_openai 프로필에서 LIG_GATEWAY_BASE_URL/LIG_API_KEY 없이도 정상(ready) 구성으로 처리하지만, 302행 분기는 두 값이 모두 있어야만 call_llm을 호출한다. 따라서 정당한 local_openai 구성(Ollama 등)에서 이 래퍼를 쓰면 llm_client.chat으로 넘어가 'LLM not configured' RuntimeError가 난다. 또한 파일이 아닌 셸 오버라이드(_SHELL_OVERRIDE_KEYS)로 프로필을 지정한 경우도 여기서는 반영되지 않는다.</sub>

#### `office_writer.py:70` — 로직오류

셀 문자열 "nan"/"inf"/"infinity"가 float 변환되어 openpyxl이 빈 셀로 기록 — 원본 데이터가 조용히 소실된다.

<sub>_is_number()(44행)가 float() 성공 여부만 보고 math.isfinite 검사를 하지 않아 "nan"/"inf" 문자열이 숫자로 판정되고, 70행에서 float('nan')/float('inf')로 변환되어 셀에 기록된다. openpyxl 3.1.5에서 실제 재현: write_xlsx(['a'], [['nan'],['inf']]) 후 다시 열면 A2/A3가 None(빈 셀)로 저장돼 텍스트가 사라진다. 동일 클래스 버그를 html_report.py의 _is_number(120행)는 isfinite로 이미 수정했으나 office_writer에는 누락됐다.</sub>

#### `office_writer.py:76` — 로직오류

headers가 빈 리스트일 때 숫자열 우측정렬 루프가 off-by-one — 첫 데이터 행이 미정렬되고 존재하지 않는 마지막+1 행에 스타일 셀이 생성된다.

<sub>headers=[]이면 헤더 행이 append되지 않아 데이터가 1행부터 시작하는데, 정렬 루프는 range(2, len(rows)+2)로 고정되어 있다. openpyxl 3.1.5로 재현: write_xlsx(path, [], [['1','2'],['3','4']]) 결과 A1/B1(첫 데이터 행)은 정렬이 안 되고, ws.cell(row=3,...)이 스타일만 있는 유령 셀을 만들어 시트 치수가 A1:B3으로 한 행 늘어난다(빈 3행 존재).</sub>

#### `doc_templates.py:56` — 로직오류

_flag_rows의 "이상" 마커가 substring 매칭이라 "이상 없음", "3개 이상" 같은 정상 문구의 행을 불량으로 플래그한다.

<sub>markers=('불합격','불량','이상',...)를 행 전체 텍스트에 대해 `in`으로 검사하므로 판정 셀이 "이상 없음"인 정상 행도 True가 된다. 이 flagged 카운트가 build_sections(64행, 71~75행)의 시험성적서 판정 문구에 직결되어, 전 항목 정상인 데이터에서도 "일부 항목 기준 미달 — 재시험/조치 필요"라는 잘못된 판정이 문서에 출력된다. HTML 경로(121행)에서도 같은 행이 빨간 flag 행으로 강조된다.</sub>

#### `safe_file_writer.py:34` — Windows호환

atomic_write가 모든 파일의 CRLF를 LF로 강제 변환해, 이 도구로 .bat/.cmd를 쓰면 프로젝트 불변 규칙(.bat는 CRLF, LF 금지)을 조용히 위반한다.

<sub>34행 data.replace("\r\n", "\n") + newline="\n"으로 항상 LF 저장된다. validate()(72-77행)는 .bat/.cmd에 대해 ASCII 여부만 검사하고 줄끝은 검사하지 않으므로 결과가 ok=True로 통과한다. LF-only 배치는 chcp/한글 환경에서 goto/label 오동작 위험이 있고, CLAUDE.md의 'LF 금지' 불변 규칙과 정면 충돌한다. '안전 작성기'가 오히려 배치 파일을 손상시키는 경로다.</sub>

#### `command_guard.py:232` — 예외처리

guarded_run에서 subprocess.run(timeout=...)의 TimeoutExpired를 잡지 않아, 타임아웃 시 가드 코드(10/20) 대신 미처리 트레이스백으로 크래시한다.

<sub>run 서브커맨드로 120초를 넘기는 명령을 실행하면 subprocess.TimeoutExpired가 main()까지 전파되어 파이썬 트레이스백과 종료코드 1로 죽는다. 가드의 의미 있는 반환코드 체계(0/10/20)가 깨져 호출측(플러그인/스크립트)이 BLOCK/ASK/타임아웃을 구분할 수 없다. 같은 파일의 core.run_cmd는 TimeoutExpired를 잡아 구조화 반환하는 것과 대조된다.</sub>

#### `schedule_store.py:107` — 로직오류

_parse_time이 'N시 M분' 형식의 분을 무시해, '내일 오후 3시 30분'이 15:30이 아닌 15:00으로 조용히 저장된다.

<sub>시간 파싱은 'H:MM'(105행) 또는 'N시'(107행)만 지원한다. '3시 30분' 입력 시 107행 정규식이 '3시'만 매치하고 lastindex==1이라 minute=0이 되며, 남은 '30분'은 날짜 파싱에서도 무시된다. 한국어에서 매우 흔한 표현이라 일정이 30분 어긋난 채 에러/질문 없이 등록되는 실사용 결함이다.</sub>

#### `agent_ops/adapters/office_convert.py:149` — 자원경쟁

_spec_to_pptx에서 json.loads가 CoInitialize(150행)보다 먼저 실행되어, spec JSON이 잘못되면 finally(177행)가 짝 없는 CoUninitialize를 호출한다.

<sub>재현: spec 파일이 깨진 JSON이면 149행에서 예외 → 150행 CoInitialize 미실행 → finally의 pythoncom.CoUninitialize()가 그대로 실행된다(불균형 호출은 예외를 던지지 않고 카운트만 감소시키므로 try/except로 방어되지 않음). 같은 스레드에 살아있는 COM 아파트가 있으면(예: excel_com이 open_copy로 세션을 유지 중) 아파트 참조수가 조기 감소해 이후 Excel COM 호출이 CO_E_NOTINITIALIZED류 오류로 실패할 수 있다. json.loads를 CoInitialize 이전에 별도 처리하거나 CoInitialize 성공 여부 플래그로 CoUninitialize를 가드해야 한다.</sub>

#### `workspace/agent_ops/adapters/fluent_batch.py:59` — Windows호환

Windows Fluent 배치 실행에 -wait 플래그가 없어 런처(fluent.exe)가 즉시 반환 — 솔버가 아직 실행 중인데 ok=True로 보고될 수 있다.

<sub>Windows에서 fluent.exe는 실제 솔버 프로세스를 spawn하는 런처이며, ANSYS 문서상 배치 스크립트에서는 -wait 플래그를 줘야 솔버 종료까지 명령이 블록된다. 현재 cmd에는 -wait가 없어 subprocess.run이 런처 종료 시점(거의 즉시)을 측정한다. 결과적으로 시뮬레이션이 진행 중인데 returncode 0/ok=True를 반환하고, stdout_tail은 비어 있으며, timeout_s(기본 1800초)도 실질적으로 무의미해진다. 버전/설치 형태에 따라 동작이 다를 수 있어 severity는 보통으로 둔다.</sub>

#### `workspace/agent_ops/adapters/ocr_screen.py:119` — 크래시

read_screen의 region 옵션에 숫자가 아닌 값이 오면 PowerShell 폴백 경로에서 int()가 ValueError를 던져 어댑터가 크래시한다(execute에 최상위 예외 가드 없음).

<sub>execute()에는 try/except가 없고 _capture 내부에서 mss(94-102행)·Pillow(105-113행) 경로만 except로 감싸져 있다. mss/Pillow가 미반입되었거나 실패해 PowerShell 폴백에 도달하면 119행 int(region[2])/int(region[3])가 문자열·None 등 잘못된 좌표에서 처리되지 않은 ValueError/TypeError를 발생시킨다. 모듈 독스트링의 '조용한 실패 금지, ok=False 보고' 설계와 달리 예외가 호출자까지 전파된다. LLM이 옵션을 생성하는 구조라 잘못된 region 형식이 들어올 개연성이 있다.</sub>

#### `workspace/agent_ops/adapters/autocad_batch.py:74` — 예외처리

shutil.copy2가 try 블록 밖에 있어 사본 생성 실패(권한/디스크/잠금) 시 예외가 호출자로 전파되고 audit 기록도 남지 않는다.

<sub>74행 shutil.copy2(dwg, copy_dwg)는 77행 try보다 앞에 있다. DWG가 있는 폴더가 읽기 전용이거나(네트워크 드라이브·권한), 디스크가 부족하거나, 원본이 다른 프로세스에 잠겨 있으면 PermissionError/OSError가 처리되지 않은 채 전파된다. 같은 파일의 다른 실패 경로는 모두 ok=False dict + _audit 기록으로 처리되는 반면, 이 경로만 크래시하며 감사 로그(risk=dangerous 작업)도 남지 않아 일관성이 깨진다.</sub>

#### `agent_ops/input_ingest.py:89` — 리소스누수

_xlsx_facts()에서 openpyxl read_only 워크북이 빈 시트 조기 return(89행) 및 반복 중 예외 경로에서 close되지 않아 파일 핸들이 누수된다.

<sub>wb.close()가 105~108행(정상 경로 끝)에만 있고 try/finally가 아니다. 첫 시트가 빈 XLSX면 85~89행에서 close 없이 return하고, iter_rows 도중 예외가 나도 호출부(ingest_inputs 231행)가 삼키고 계속 진행하므로 핸들이 열린 채 남는다. read_only 모드는 파일 핸들을 유지하므로 Windows에서 해당 XLSX가 잠긴 상태로 남아 사용자가 Excel로 열거나 이동/삭제할 때 공유 위반이 날 수 있다.</sub>

#### `knowledge_book.py:275` — 예외처리

위키 페이지 프론트매터의 records/active 값이 숫자가 아니면 int() ValueError로 책 생성 전체가 죽고, 자동 훅 경로에서는 조용히 은폐된다.

<sub>_wiki_pages_html 274~276행의 int(meta.get("records", "0") or 0)는 빈 문자열만 방어하고 비숫자 문자열은 방어하지 않는다. wiki_vault의 안내 노트가 '자동 생성 페이지도 직접 수정하면 반영된다'고 사용자 편집을 권장하므로, 사용자가 프론트매터를 손대 records: 값이 깨지면 발생 가능하다. 이 루프는 258~263행 try 블록 밖이라 예외가 build_book까지 전파되어 수동 book 명령은 크래시하고, rebuild_quietly(444행 except: pass) 경로에서는 예외가 삼켜져 지식책이 그 뒤로 갱신되지 않는 침묵 실패가 된다.</sub>

#### `queue_manager.py:114` — 자원경쟁

get_next_batch에서 병렬 불가(serial-only) 작업이 배치의 첫 항목이면 이후 안전 작업들이 같은 배치에 함께 담겨 직렬 보장이 깨진다.

<sub>조건이 `if batch and not _is_parallel_safe(task): continue` 로만 되어 있어, unsafe 작업(safe_write/repair/memory_apply, risk=review_required)이 우선순위상 첫 번째로 batch에 들어가면 그 뒤의 parallel-safe 작업들은 조건을 통과해 같은 배치에 추가된다. 결과적으로 run_loop_parallel(orchestrator.py:159)에서 '파일 쓰기/수리 작업은 직렬 유지'라는 주석(97행)의 의도와 달리 unsafe 작업이 다른 작업과 동시에 ThreadPool에서 실행된다. 반대 방향(배치에 unsafe가 이미 있으면 추가 중단)의 검사가 빠져 있다.</sub>

#### `core.py:201` — 자원경쟁

file_lock의 stale 락 해제(TOCTOU) 경쟁: 두 프로세스가 동시에 stale 판정하면 한쪽이 상대가 갓 만든 새 락을 지워 두 프로세스가 동시에 락을 보유할 수 있다.

<sub>타임아웃 시 _lock_is_stale 판정 후 lock_path.unlink()→재획득 루프인데, 프로세스 A가 stale 락을 unlink하고 O_EXCL로 새 락을 만든 직후, 같은 stale 판정을 이미 마친 프로세스 B가 A의 '새 락'을 unlink하고 자기 락을 만들 수 있다(판정 시점과 unlink 시점 사이 검증 없음). 이 경우 task_queue 등 공유 JSONL이 두 프로세스에서 동시 기록되어 작업 유실/중복이 가능하다. 재현 조건: 900초 이상 방치된 stale 락 + 2개 이상 대기 프로세스의 동시 타임아웃.</sub>

#### `agentops.py:259` — 인코딩

cmd_doctor만 stdout을 UTF-8로 재구성하고 status/verify/agent/work 등 나머지 명령은 ensure_ascii=False JSON·LLM 응답을 그대로 print하여 cp949 콘솔에서 UnicodeEncodeError로 크래시할 수 있다.

<sub>cmd_doctor(528행)는 sys.stdout.reconfigure(encoding='utf-8', errors='replace')를 하지만 cmd_status(259행), cmd_verify(536행), cmd_agent(730행, LLM final_content 출력), cmd_work 등은 하지 않는다. 런처(.bat, PYTHONUTF8) 없이 `python agent_ops\agentops.py status` 등으로 직접 실행하면 stdout이 cp949가 되고, read_text(errors='replace')가 만들어내는 U+FFFD(cp949에 없음)나 모델 출력의 비-cp949 문자에서 print가 UnicodeEncodeError로 즉사한다. doctor에만 방어가 있는 비일관성이 실제 장애 경로를 남겨둔 상태다.</sub>

---

## ⬜ 낮음

간결히 나열(상세는 파일:라인 참고).

- **`agent_ops/llm_client.py:19`** (크래시) — AGENTOPS_LLM_TIMEOUT 환경변수나 llm_config.json의 timeout이 숫자가 아니면 int()가 ValueError로 죽어 load_llm_config/is_configured/chat 전체가 크래시한다.
- **`agent_ops/llm_client.py:63`** (크래시) — 응답 본문이 dict가 아닌 JSON(배열/문자열/숫자)이면 obj.get(key)에서 AttributeError가 발생한다 — 'last resort: don't crash' 의도와 달리 크래시한다.
- **`agent_ops/probe_env.py:117`** (로직오류) — 그룹 정책 오버라이드 검사(comment: 'HKLM policies')가 실제로는 HKEY_CURRENT_USER를 읽어 머신 GPO 강제값을 감지하지 못한다.
- **`agent_ops/probe_env.py:57`** (로직오류) — _progid_exists가 ProgID 키 존재가 아니라 기본값(default value) 존재로 설치 여부를 판정해, 기본값이 비어 있는 ProgID는 설치돼 있어도 installed=False로 오탐한다.
- **`agent_ops/probe_env.py:210`** (인코딩) — tasklist를 text=True로 실행해 로케일/UTF-8 모드에 따라 cp949 출력 디코드가 깨질 수 있고, 실패 시 잔류 프로세스 수가 조용히 '확인 실패'로 강등된다.
- **`artifact_generators.py:1016`** (자원경쟁) — 같은 초에 시작한 두 프로세스가 exists() 체크와 mkdir 사이 TOCTOU로 동일 run 폴더를 공유해 고정 파일명 산출물이 서로 덮어써질 수 있다.
- **`audit.py:27`** (로직오류) — SECRET_MARKERS의 부분문자열 매칭('sk-', 'token' 등)이 과도하게 매치되어 평범한 detail이 통째로 [REDACTED] 처리된다.
- **`state_manager.py:237`** (자원경쟁) — append_blocker/append_done이 BLOCKERS.md·DONE_LOG.md를 file_lock 없이 read-modify-write 하여 동시 실행 시 항목이 유실될 수 있다.
- **`status_writer.py:37`** (자원경쟁) — _write_json이 고정 이름 .tmp 파일을 쓰기 때문에 동시 publish 시 Windows에서 replace 실패(PermissionError)나 혼합 내용 교체가 가능하고, 실패는 조용히 삼켜진다.
- **`verifier.py:20`** (로직오류) — scan_agents의 'patch:' 단순 부분문자열 검사가 'dispatch:' 등 무해한 텍스트에 오탐하여 verify() 전체를 ok=False로 만든다.
- **`approval.py:95`** (예외처리) — request_approval의 기본 input()이 비대화형(stdin 닫힘) 환경에서 EOFError를 던져 정상 거부 대신 미처리 크래시가 난다.
- **`state_manager.py:95`** (예외삼킴) — consume_interruption에서 queue_manager.update_task 실패를 except Exception: pass로 은폐해 ACTIVE_TASK.json과 큐 상태가 조용히 어긋날 수 있다.
- **`agent_ops/adapters/excel_com.py:113`** (자원경쟁) — _close_session이 세션 존재 여부와 무관하게 CoUninitialize를 호출해, 세션 없는 close·이중 close·atexit 중복 시 짝 없는 CoUninitialize가 발생한다.
- **`agent_ops/adapters/solidworks_com.py:88`** (리소스누수) — .bas 조기 반환(89-92행) 판정 전에 문서 사본을 먼저 생성해, 실행이 거부되는 호출마다 사본_ 파일이 디스크에 누적된다.
- **`agent_ops/adapters/office_convert.py:106`** (예외처리) — md_to_docx가 영어 내장 스타일명("Heading 1", "List Bullet", "Title")을 문자열로 지정하고 실패를 except:pass로 삼켜, 한국어 Word에서는 제목/목록 서식이 전부 조용히 소실된다.
- **`agent_ops/adapters/__init__.py:223`** (로직오류) — document→HWP 자동 실행 경로가 출력 경로에 unique-path 처리 없이 md와 같은 이름의 .hwp를 지정해, 기존 .hwp 파일을 조용히 덮어쓸 수 있다.
- **`workspace/agent_ops/adapters/autocad_batch.py:72`** (예외처리) — 옵션 숫자 변환(int(opts.get(...)))이 try 밖에서 수행되어 timeout_s/cores에 숫자가 아닌 문자열이 오면 처리되지 않은 ValueError로 크래시한다.
- **`workspace/agent_ops/adapters/browser_cdp.py:532`** (로직오류) — spa_map 초기 스크린샷 실패 시 예외 메시지 문자열을 Path로 감싸 initial_screenshot '경로'로 보고한다.
- **`agent_ops/failures.py:16`** (예외처리) — classify_failure()가 11행에서는 `(text or '')`로 None을 방어하면서 16·18·26행에서는 원본 text에 직접 `in` 연산을 해 text=None이면 TypeError가 난다.
- **`agent_ops/menu.py:78`** (로직오류) — _reminder()가 'i' 이외의 모든 입력(빈 Enter, 오타 포함)을 끄기(u)로 해석해 uninstall-reminder.bat를 실행한다.
- **`agent_ops/menu.py:115`** (예외처리) — 메뉴 루프의 115행 input() 및 do_task/일정추가의 input()들은 EOFError를 처리하지 않아 stdin이 닫히면(파이프/CI 주입 소진) 트레이스백으로 죽는다.
- **`agent_ops/local_tools.py:135`** (예외처리) — tool_search_files()가 모델이 준 pattern을 검증 없이 Path.glob에 넘겨, 절대경로 패턴 등 비정상 패턴에서 NotImplementedError/ValueError가 ToolError로 변환되지 않고 그대로 새어 나간다.
- **`knowledge_base.py:121`** (로직오류) — routing_debug 반환 타입 Dict[str, Any]에서 Any가 임포트되지 않음 (20행 typing 임포트에 Any 없음).
- **`knowledge_book.py:341`** (로직오류) — created_at이 없거나 파싱 불가한 기록이 '이번 주 새로 배움' 통계에 무조건 포함된다.
- **`wiki_manager.py:581`** (로직오류) — recall_pages/knowledge_book이 vault 시드 노트(0-위키-안내.md, 0-대시보드.md)를 지식 페이지로 오인한다.
- **`wiki_manager.py:237`** (예외처리) — 사용자가 aliases.json 값을 리스트 대신 문자열로 적으면 문자 단위로 쪼개져 1글자 검색어가 생긴다.
- **`api_reference.py:100`** (로직오류) — _excerpt의 '관련 섹션 0건 시 앞쪽 섹션 채우기' 폴백이 always 섹션 일부가 예산 초과로 탈락한 경우 동작하지 않는다.
- **`core.py:291`** (로직오류) — validate_written_file이 .bat/.cmd를 순수 ASCII로 강제하지만, 프로젝트 자체 launch/*.bat 대부분이 한국어(비ASCII)를 포함해 safe-write로는 이런 배치파일을 절대 갱신할 수 없다.
- **`tool_dispatch.py:323`** (로직오류) — _PARAM_DESCRIPTIONS 딕셔너리 리터럴에 'action'(310행/323행)과 'count'(303행/330행) 키가 중복 선언되어 뒤 값이 앞 값을 조용히 덮어쓴다.
- **`toolcall_parser.py:37`** (로직오류) — _repair_json_text의 잘린 JSON 복구가 닫는 괄호를 항상 ']' 다음 '}' 순서로 붙여, 배열 안 객체가 잘린 형태(툴콜 배열 `[{"name":...`)를 복구하지 못한다.
- **`queue_manager.py:136`** (자원경쟁) — mark_task_running(run_once/run_loop 경로)은 claim_task와 달리 status==pending 원자 검사가 없어, orchestrator 루프와 continue-once 등 두 프로세스가 같은 작업을 중복 실행할 수 있다.

---

## 📌 참고

- 본 목록은 Fable(mid)의 정적 검토 결과로, **높음 4건 외 보통·낮음은 미검증**입니다. 수정 전 각 건을 해당 파일에서 재확인하세요.
- 반복 패턴 3가지가 눈에 띕니다:
  1. **인코딩 비일관** — `text=True` subprocess / cp949 콘솔 출력 (agentops·probe_env·probe_env tasklist)
  2. **입력 방어 누락** — `int()`/`float()`/`.strip()`을 try 밖에서 호출해 쓰레기 입력에 크래시 (llm_client·ocr_screen·autocad_batch·knowledge_book)
  3. **직렬/원자성 보장 구멍** — 파일락 TOCTOU, claim 없는 상태전이 (core·queue_manager·status_writer·state_manager)
- **`hamster_overlay.py:522`는 최근 편집 회귀**입니다(저장된 위치가 있으면 2회차 실행부터 크래시). 별도로 빠르게 잡는 것을 권합니다.
