# /auto

자연어 요청을 하나만 받아 내부에서 가장 적절한 AgentOps 경로를 고릅니다.

```bash
python agent_ops/agentops.py auto --task "$ARGUMENTS"
```

필요할 때만 고급 직접 명령(`/work`, `/agent`, `/schedule`, `/recall`, `/wiki`)을 사용합니다.
기본 사용자는 이 명령 하나로 시작합니다.

