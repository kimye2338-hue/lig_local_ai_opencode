# 리뷰: P11-02 (r1) — **APPROVED**

| 항목 | 값 |
|------|-----|
| 대상 보고서 | plan/reports/P11-02-r1.md |
| 리뷰어 | Claude Fable 5 |
| 날짜 | 2026-07-04 |
| **Verdict** | **APPROVED** |

## 필수 수정

없음.

## 확인된 것 (리뷰어 실측/정독)

- **리포트 경로 분리(P11-01 리뷰 요구)가 정확히 반영**: mock 자가검증은
  `capability_floor_mock.md`, 실측은 `capability_floor_<모델명>.md`에 쓴다
  (`report_path_for_model`). doctor는 실측 리포트 > mock > legacy 순으로 우선 선택
  (`run_doctor`의 glob+mtime 정렬). 이제 회귀 실행이 실측 리포트를 덮지 않는다.
  test에 `mock report path is separated from real reports` check로 회귀화.
- **프롬프트 보강이 예산 내**: `AGENT_SYSTEM_PROMPT`에 "For file tasks, use tools before
  answering. Copy filenames exactly." 1문장 추가 + `_PARAM_DESCRIPTIONS` 축약으로 총량을
  2193 bytes로 유지(측정값 재현: prompt+schema compact JSON 2193 ≤ 2300). test에 상한
  검증 추가.
- **금지 조항 준수**: 실측 실패가 tool-call JSON 깨짐이 아니라 wrong_output(잘못된 파일명/
  내용)이라 추측 파서 규칙을 추가하지 않았다 — "실측 로그에 없는 형식 추측 금지"에 부합.
  deviation으로 정직하게 신고.
- **성공률 정직 기록**: 최종 20/30(66.7%), 중간 19~21/30 변동, native 0/30(runtime-last.json
  최종 턴 기준 관측). 목표 90% 미달을 수치 그대로 보고 — task의 "미달이면 조작 금지" 준수.

## 권고 (선택)

- `_PARAM_DESCRIPTIONS` 축약으로 `path` 설명이 "workspace-relative file or directory
  path" → "relative path"로 바뀌어 workspace-상대 뉘앙스가 옅어졌다. 예산 압박이 풀리면
  (스키마 여유 생기면) 한 단어라도 복원 고려.

## 미해결로 남는 것 (task 실패 아님 — 후속 결정 필요)

- **capability floor 90% 목표는 미달 상태로 열려 있다.** P11-02는 하네스 보강 + 실측 +
  정직 보고라는 DoD를 충족했으나, qwen2.5:7b의 실제 성공률(66.7%)은 목표에 못 미친다.
  이는 코드 결함이 아니라 약모델 역량의 실측 결과다. 90% 확보는 (a) 모델 상향(회사
  EXAONE/Qwen gateway는 P19 범위) 또는 (b) 프롬프트/파서 추가 라운드가 필요 —
  이 판단은 P00-03 회사 실측과 함께 Fable가 별도 task로 다룬다(P11-02 재작업 아님).

## 검증 재현

리뷰 환경: Linux, Python 3.11.15 (Ollama 없음 — 66.7% 수치 자체는 워커 환경 실측).

- `tests/test_capability_floor.py`(서버 부재): mock 자가검증 통과 후
  `SKIP  local llm not running — skipped, not failed`, exit 0.
- `tests/test_tool_dispatch.py` → `ALL 27 CHECKS PASSED` (r1 26 + 프롬프트 예산 1).
  prompt+schema 2193 bytes 재현.
- doctor 리포트 우선순위 로직 정독으로 실측>mock>legacy 확인.
- 전체 회귀: 19개 중 16개 exit 0. Windows 전용 3개는 이 diff 무접촉 확인으로 대체.

## STATUS 반영

- P11-02 → **APPROVED**. 직접 후속 READY 없음(리프). 90% 갭은 위 "미해결"로 이월.
