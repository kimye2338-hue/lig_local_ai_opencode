# 리뷰: P14-05 (r1)

| 항목 | 값 |
|------|-----|
| 대상 보고서 | plan/reports/P14-05-r1.md |
| 리뷰어 | Claude Fable 5 |
| 날짜 | 2026-07-04 |
| **Verdict** | **CHANGES-REQUESTED** |

## 필수 수정

1. `agent_ops/capabilities.py:weekly_report.keywords` — 문제: 영어 `weekly`가 **bare
   substring**이라 `biweekly`가 든 문장을 주간보고로 오라우팅한다. 리뷰어 실측(`plan_task`):
   `biweekly 회의 잡아줘` → caps에 `weekly_report` 포함 + `document` 아티팩트 계획.
   이는 P16-01 `.m`·P14-04 `minutes`와 **같은 계열**이다(빈도는 그 둘보다 낮지만
   capability 추가는 hard gate이므로 같은 기준을 적용). 한국어 키워드(주간보고/주간 보고/
   위클리)가 이미 의도를 커버한다. / 요구: bare `weekly`를 `weekly report`로 경계화하고,
   `biweekly`가 weekly_report로 라우팅되지 않는 negative check를 bench에 추가하라.

## 되는 방법 (리뷰어가 검증한 수정 — 그대로 채택 가능)

`capabilities.py`:

```python
"keywords": ["주간보고", "주간 보고", "위클리", "weekly report"],
```

리뷰어 실측(전부 통과): `주간보고 초안 만들어줘`✓·`위클리 리포트 작성`✓·
`weekly report 만들어줘`✓·`주간 보고 정리`✓ (양성 유지) / `biweekly 회의 잡아줘`✗·
`this week 일정`✗ (오라우팅 차단).

bench negative check:

```python
wk_neg = plan_task("biweekly 회의 잡아줘")
check("biweekly does not route to weekly_report",
      "weekly_report" not in {c["id"] for c in wk_neg["capabilities"]}, str(wk_neg))
```

## 권고 (선택 반영 — 채택 여부를 다음 보고서에 기록할 것)

- 보고서(P14-05-r1.md)가 `plan/templates/report-template.md` 섹션 구조를 그대로 쓰지
  않았다(1.5/DoD 체크박스 형식 등 축약). 내용은 충분하나, 다음 보고서부터 템플릿 섹션
  구조를 맞추면 리뷰가 빨라진다.

## 확인된 것 (잘한 것 — 유지)

- **3개 원천 반영이 실동작**: 리뷰어 E2E(격리 fixture) — audit(지난 7일 필터 정확: 07-02
  포함/06-10 제외, kind별 건수 + 대표 task), schedule(다음 주 open `치수 보고서` 표시,
  done 없으면 "없음"), artifacts(7일 내 파일 + 출처 경로). 세 원천 경로 모두 본문 명시.
- **초안 정직성**: `Weekly Report Draft` + `TODO: 정성 성과/이슈는 직접 보완` — 완성
  보고서로 포장하지 않음. 빈 섹션 "없음"(조용한 생략 없음). task 금지("추정 서술 금지")
  준수 — 기록된 것만 집계.
- audit `task` 필드가 **실제 audit.record 스키마와 일치**(audit.py:36) — fixture-실제
  괴리 없음을 리뷰어가 실기록으로 확인. `_within_last_days`로 기간 판정 일관.
- `document` 아티팩트 kind 재사용(전용 kind 미신설) — task 지시대로.

## 검증 재현 (리뷰어가 직접 실행/확인한 것)

리뷰 환경: Linux, Python 3.11.15 (+`py -3.11`→python3 셔님).

- `tests/test_secretary.py` → `ALL 37 CHECKS PASSED` / `tests/test_capability_bench.py`
  → 리뷰 환경 190 checks(보고서 192 − openpyxl 조건 분기 2).
- weekly E2E: audit/schedule/artifacts fixture로 주간보고 생성 → 3개 원천·기간 필터·
  초안 TODO 확인.
- 라우팅 실측: 양성 3건 통과, `biweekly` 오라우팅 재현(필수 수정 근거).
- 전체 회귀: 20개 중 17개 exit 0. Windows 전용 3개(agent_cli/encoding_paths/probes)는
  이 diff 무접촉 확인으로 대체.

## 피드백 (코칭 — 다음 작업의 습관으로)

- **좋았던 것**: 초안 정직성(TODO/없음)과 기록-only 집계가 task 정신을 정확히 지켰다.
  audit 스키마를 실제와 맞춘 것도 좋다.
- **반복되는 함정**: bare 영어 키워드(`.m`→`.md`, `minutes`→`5 minutes`, `weekly`→
  `biweekly`)가 세 번째다. **capability에 영어 단어를 넣을 땐 항상 "이게 더 긴 단어의
  일부로 들어가나?"를 self-check**하고, 워커 스스로 negative 라우팅 테스트를 1줄
  먼저 넣는 습관을 들이면 리뷰 왕복이 사라진다.

## 작업계획 수정내용 (task/보드 반영)

- `plan/tasks/P14-05-weekly-report.md`에 "리뷰 반영 (r1→r2)" 절 추가(키워드 경계화 +
  negative bench check).
- 보드: **P14-05 → CHANGES-REQUESTED**. 후속 의존 작업 없음(P14-05는 리프).
- Codex 다음 권장 순서: **P14-05 r2 → P18-02 → P15-03 → P16-02**(모두 현재 READY/재수정).

## STATUS 반영

- P14-05 → CHANGES-REQUESTED (r2 대기 — 키워드 1건 + negative check).
