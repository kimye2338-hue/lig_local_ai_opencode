# 리뷰: P16-03 (r2) — **APPROVED**

| 항목 | 값 |
|------|-----|
| 대상 보고서 | plan/reports/P16-03-r2.md |
| 리뷰어 | Claude Fable 5 |
| 날짜 | 2026-07-04 |
| **Verdict** | **APPROVED** |

## 필수 수정

없음 — r1 필수 수정 2건(키워드 경계화 + NEGATIVE_CORPUS 메타 체크) 반영·실측 확인.

## 확인된 것 (리뷰어 실측 재현)

- **키워드 오라우팅 해결**: bare `해석`/`journal` 제거, 도메인어(+cfd/fea)로 교체.
  실측 — 이전 누출 3케이스(`이 데이터 해석해줘`·`회의 내용 해석`·`journal 정리`) 전부
  차단, 양성 라우팅 유지(`플루언트 해석 journal`·`ansys fluent 시뮬레이션`·`메카니컬
  스크립트`·`cfd 해석 돌려줘` → fluent_journal/ansys_script).
- **NEGATIVE_CORPUS 메타 체크 채택**: 리뷰가 설계한 일상어 10문장 corpus를 그대로
  bench에 회귀화. 리뷰어 독립 실측 — **특수 capability 누출 0/10**. 이제 키워드 계열
  오라우팅(`.m`/`minutes`/`weekly`/`해석`, 4회 재발)이 **리뷰 없이 bench에서 차단**된다.
- r1에서 확인한 실체(fluent_journal/ansys_script 골격·공학책임 경고 quality 강제·fluent
  available=False·부재 안내·테스트 POSIX 이식성)는 무손상.

## 검증 재현

리뷰 환경: Linux, Python 3.11.15 (+`py -3.11`→python3 셔님, openpyxl 설치됨).

- `test_capability_bench.py` → `ALL 213 CHECKS PASSED`(NEGATIVE_CORPUS 10 + simulation
  routing 포함). `test_batch_adapters.py` POSIX green.
- NEGATIVE_CORPUS 독립 재현 0/10 누출, 양성 4건 유지(위).
- 전체 회귀: 22개 중 19개 exit 0. Windows 전용 3개(agent_cli/encoding_paths/probes)만
  RED(diff 무접촉).

## 피드백 (코칭)

- 리뷰가 준 corpus 메타 체크를 그대로 회귀화한 것이 이 작업의 최대 가치다 — 이제 키워드
  오라우팅은 클래스 단위로 봉쇄됐다. 앞으로 capability를 추가하는 워커는 새 일상어
  오라우팅 발견 시 corpus에 문장을 **추가**하면 되고(체크 완화/삭제 금지), 리뷰 왕복이
  사라진다. self-review 스킬 체크리스트 1번과 연동됨.

## 작업계획 수정내용 (task/보드 반영)

- P16-03 task "리뷰 반영" 2건 충족 — task 추가 변경 없음.
- 보드: **P16-03 → APPROVED**. 후속 의존 없음(P16-04 이미 READY). 신규 READY 없음.
- Codex 다음 권장 순서: **P16-04 → P18-02 → P19-01**(모두 READY).

## STATUS 반영

- P16-03 → **APPROVED**. 신규 READY 전환 없음.
