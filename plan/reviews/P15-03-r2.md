# 리뷰: P15-03 (r2) — **APPROVED**

| 항목 | 값 |
|------|-----|
| 대상 보고서 | plan/reports/P15-03-r2.md |
| 리뷰어 | Claude Fable 5 |
| 날짜 | 2026-07-04 |
| **Verdict** | **APPROVED** |

## 필수 수정

없음 — r1 필수 수정 1건 반영·실측 확인.

## 확인된 것 (리뷰어 실측 재현)

- **필수 1(subprocess import root) 해결**: 리뷰가 검증 제시한 대로 `CODE_ROOT =
  Path(__file__).resolve().parents[2]`를 추가하고 `_run_child`에서 `cwd=str(CODE_ROOT)`
  + `env["PYTHONPATH"]=str(CODE_ROOT)+os.pathsep+...`로 고정. 그대로 채택됨.
- **r1의 RED 조건이 GREEN으로**: `AGENTOPS_ROOT=<임시 데이터폴더>`에서
  `execute("read_calendar")`·`read_inbox`·`sync_calendar` **셋 다** 이제
  `{'ok': False, 'error': "pywin32 미설치 …"}` 정상 안내 반환(이전 "no JSON" 소멸).
  코드≠데이터 루트 분리 실사용 경로가 복구됨.
- `test_office_adapters.py`가 리뷰 환경(패키지 전역 import 불가)에서도 다시 통과:
  `SKIP Excel COM live checks …; ALL 26 STATIC CHECKS PASSED (office adapters)`.
- r1에서 확인한 나머지(GetActiveObject 전용/서브프로세스 격리/sync 중복방지/send
  fail-closed dangerous·비노출/available=False)는 이 diff에서 무손상.

## 검증 재현

리뷰 환경: Linux, Python 3.11.15 (+`py -3.11`→python3 셔님), pywin32 없음.

- 데이터-루트 분리 재현: 3개 read 액션 전부 정상 안내(위).
- 전체 회귀: 20개 중 17개 exit 0 — office_adapters 복구 확인, 남은 RED는 Windows 전용
  3개(agent_cli/encoding_paths/probes)뿐이며 이 diff 무접촉.
- `test_capability_bench.py` 191 / `test_schedule_store.py` 69 (보고서 193은 openpyxl +2).

## 피드백 (코칭)

- 리뷰 검증 코드를 그대로 채택한 것은 모범이다. 이번 교훈(**격리 subprocess는 부모
  sys.path 미상속 → 코드 루트를 cwd+PYTHONPATH로 명시**)은 P16-02(accoreconsole
  서브프로세스)·향후 모든 subprocess 격리 어댑터에 그대로 적용하라. 같은 함정을 두 번
  겪지 않도록.

## 작업계획 수정내용 (task/보드 반영)

- P15-03 task "리뷰 반영 (r1→r2)" 요구 충족 — task 추가 변경 없음.
- 보드: **P15-03 → APPROVED**. P15-03를 선행으로 갖는 대기 작업 없음(신규 READY 없음).
- Codex 다음 권장 순서: **P18-02 → P16-02 → P16-03/04 → P15-04(EXCEL)**.

## STATUS 반영

- P15-03 → **APPROVED**. 신규 READY 전환 없음.
