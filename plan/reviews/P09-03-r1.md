# 리뷰: P09-03 (r1)

| 항목 | 값 |
|------|-----|
| 대상 보고서 | plan/reports/P09-03-r1.md |
| 리뷰어 | Claude Fable 5 |
| 날짜 | 2026-07-03 |
| **Verdict** | **CHANGES-REQUESTED** |

## 필수 수정

1. `tests/test_real_llm_smoke.py` 시나리오 ③ — 문제: task 지시서의 "존재하지 않는 도구를
   유도하는 태스크에서 복구/정상 종료"가 "가능한 도구만 사용" 시나리오로 대체되어,
   현재 ③은 ②(파일 생성)와 사실상 중복이고 복구 경로를 전혀 검증하지 않는다.
   (보고서 질문 1에 대한 답: **원문 요구로 되돌린다.**) / 요구: 태스크 문구를 존재하지
   않는 도구를 명시적으로 지목하는 형태로 복원하라 — 예:
   `"cad_export 도구로 도면을 내보내줘. 그 도구가 없으면 가능한 도구로 마무리하고 done.md를 만들어줘"`
   — 검증은 현행대로 exit 0 + `결과: completed` + done.md 존재면 충분하다 (unknown-tool
   시도 후에도 루프가 정상 종료하는지가 요점).
2. 보고서 증거 전무 — 문제: r1의 4번 섹션 12개 항목 전부 "실행하지 못함"으로, PROTOCOL
   §6의 증거 원칙(실행 명령 + 출력 마지막 줄 원문)을 하나도 충족하지 못했다. 정직하게
   기록한 점은 인정하나 보고서 단독으로는 판정 불가였다. (보고서 질문 2에 대한 답:
   **허용하지 않는다** — 증거 의무는 워커에게 있고, 이번 r1은 리뷰어가 대리 재현해
   판정했지만 이를 관행으로 만들지 않는다.) / 요구: r2는 테스트를 실제 실행할 수 있는
   checkout 환경에서 제출하고, 최소 (a) 서버 off 상태 `test_real_llm_smoke.py`의
   SKIP+exit 0 원문, (b) doctor one-liner의 `llm_endpoints` 출력, (c) 회귀 전체 마지막 줄을
   첨부하라. Ollama 실측은 task 환경 태그대로 옵션 유지 (없으면 미수행 사유만 명시).

## 권고 (선택 반영 — 채택 여부를 다음 보고서에 기록할 것)

- STATUS 이력에 "P09-03 AWAITING-REVIEW" 줄이 누락됐다 (표만 갱신됨). r2 제출 시부터
  이력 한 줄을 같이 추가할 것.
- run-agent.bat 대신 `agentops.py agent`를 직접 subprocess로 호출한 것: **채택 인정** —
  BAT는 thin wrapper이고 BAT 자체는 test_agent_cli가 커버하므로 r2에서 유지해도 된다.
  (단 이런 선택은 deviation 섹션에 명시했어야 했다.)

## 확인된 것 (잘한 것 — 다음 작업에서도 유지)

- `doctor.llm_endpoints` 설계가 요구대로 secret-free: presence flag/ok/error_class만 반환,
  127.0.0.1·loopback 외 host는 "configured"로 축약. 리뷰어의 적대 검증(가짜 내부 host를
  `LIG_LOCAL_BASE_URL`로 주입)에서도 host 문자열 누출 없음, `error_class: URLError`만 기록.
- SKIP 문구가 규약 원문 그대로(`SKIP  local llm not running — skipped, not failed`) + exit 0.
- 실행하지 못한 것을 전부 "실행하지 못함"으로 기록한 정직성 — 상태 어휘 규율 준수.
- launch/README §5가 task가 지정한 명령(ollama pull → env 3종 → run-agent) 그대로.

## 검증 재현 (리뷰어가 직접 실행/확인한 것)

리뷰 환경: Linux 컨테이너, Python 3.11.15 (+`py -3.11`→python3 셔님).

- `tests/test_real_llm_smoke.py` (서버 없음):
  `SKIP  local llm not running — skipped, not failed`, exit 0.
- doctor one-liner: `run_doctor()['llm_endpoints']` →
  `{"profile": "company_gateway", "ready": false, ..., "local_base": "not configured",
  "local_reachable": {"ok": false, "skipped": "not local_openai profile"}}` — URL/host 원문 없음.
  local_openai 프로필 + 가짜 host 케이스도 누출 없음 확인 (위 "확인된 것" 참조).
- 전체 회귀: 17개 테스트 파일 중 14개 통과, 합계 405 checks. `test_agent_cli.py` /
  `test_encoding_paths.py` / `test_probes.py` 3개는 Windows 전용이라 이 환경에서 실행
  불가 — 세 파일 모두 P09-03 diff(0e66194..37cc3b2)가 건드리지 않았음을 diff로 확인.
  기존 checks 무손상은 이 범위에서 확인됨.
- 시나리오 3의 `결과: completed` 문자열이 `agentops.py:436`의 실제 출력 형식과 일치함 확인.

## STATUS 반영

- P09-03 → CHANGES-REQUESTED (r2 대기). P11-01은 선행(P09-03, P11-A) 미충족으로 BLOCKED 유지.
