# 리뷰: P11-A (r1) — **APPROVED**

- 대상: plan/reports/P11-A-r1.md / 리뷰어: Fable / 2026-07-03
- 확인 (task 델타 5종 전부 구현):
  ① toolcall_parser: structured id 보존 + "N/A"→"" 정규화 — function 키 접기 전에
  최상위 id를 읽으므로 OpenAI/flat 양쪽 shape에서 동작. 정규화 shape에 id 키 추가는
  기대값 갱신으로 처리(허용 범위).
  ② run_agent_loop: `call_<turn>_<index>` 자체 발급 — turns가 반복마다 증가해 전
  대화에서 유일, assistant.tool_calls[].id ↔ role:"tool".tool_call_id 1:1 대응.
  gateway가 "N/A"를 줘도 발신 메시지에 "N/A" 미등장 (payload 캡처 테스트로 검증).
  ③ lig_runtime: tool_call_mode(native|text_fallback|none)를 result와
  runtime-last.json에 기록 — parse_status "failed"는 "none"으로 접히나 parse_status가
  결과에 공존하므로 진단 손실 없음.
  ④ mock_transport: 실측 gateway 형태(id="N/A", finish_reason="tool_calls") 재현 —
  회사 실측(company_check scn_gateway_agent)과 정합.
  ⑤ 테스트 3파일 확장, 텍스트 복구(fallback) 경로 무손상.
- 검증 재현 (리뷰 환경): test_toolcall_parser.py 15 / test_tool_dispatch.py 26 /
  test_lig_runtime.py 23 checks, 전체 17개 테스트 파일 exit 0. 캡처 테스트가 실제
  송신 payload의 id 대응을 검사하므로 형식적 테스트 아님.
- deviation 2건(기대값 id 추가, 기존 격리 변수 재사용) 모두 task 허용 범위.
- STATUS: P11-A → APPROVED. P11-01(weak-model floor 하네스) 선행 충족 → READY.
  Codex 다음 순서는 P11-01을 P14-02보다 우선 권고 (P11 계열이 크리티컬 패스).
