# 리뷰: P14-04 (r2) — **APPROVED**

| 항목 | 값 |
|------|-----|
| 대상 보고서 | plan/reports/P14-04-r2.md |
| 리뷰어 | Claude Fable 5 |
| 날짜 | 2026-07-04 |
| **Verdict** | **APPROVED** |

## 필수 수정

없음 — r1 필수 수정 2건 모두 반영·실측 확인.

## 확인된 것 (리뷰어 실측 재현)

- **필수 1(minutes 오라우팅) 해결**: `minutes`→`meeting minutes`/`미팅노트` 경계화.
  실측 — `5 minutes 후에 알려줘`·`wait 10 minutes` 둘 다 meeting_minutes 미포함,
  양성 라우팅(`회의록 정리해줘`·`미팅노트 만들어줘`·`회의 내용 읽고 정리해줘`)은 유지.
  bench negative check 회귀화됨.
- **필수 2(owner 날짜 오추출) 해결**: 리뷰가 검증해 제시한 `_extract_owner`(후행 우선→
  날짜형 거부→선행 fallback→확인 필요)를 그대로 채택. 회의록 E2E 실측 —
  액션 표가 `| 1 | … | 김대리 | 7월 10일 |`로 담당=`김대리`, `| 7월 |` 조각 없음.
  bench에 `"| 7월 |" not in minutes` 정확성 check 회귀화됨.
- r1에서 확인한 생성기 골격/결정 추출/일정 제안 자동등록 없음/품질 규칙은 무손상.

## 되는 방법 (권고 반영용 — 리뷰어 검증 완료)

권고(아래)를 채택한다면 이 한 줄이면 된다. `_meeting_actions`의 todo 정리 마지막에:

```python
todo = re.sub(r"^(?:까지|까지는|에|에는)\s+", "", todo)
```

리뷰어 실측: `까지 도면 수정하기로 했다`→`도면 수정하기로 했다`,
`케이블 점검하기로 했다`(변화 없음), `도면 수정 7월 10일까지`(영향 없음). 안전하게 흡수됨.

## 권고 (선택 반영 — 채택 여부를 다음 보고서에 기록할 것)

- todo 칸에 `까지` 접두 잔여가 남는다: `김대리 담당: 7월 10일까지 도면 수정…`의 due
  정규식이 `7월 10일`만 캡처해 `10일까지`의 `까지`가 남아 todo가 `까지 도면 수정하기로
  했다`가 된다. 오값은 아니고 의미 보존되나 어색함 — 위 "되는 방법" 한 줄로 정리 가능.
  **P15-03 이후 별도 라운드에서 흡수해도 되고, 지금 고쳐도 된다(승인은 이와 무관).**

## 피드백 (코칭 — 다음 작업의 습관으로)

- **좋았던 것**: 리뷰가 제시한 검증 코드를 그대로 채택하고, 요구한 negative/정확성
  bench check를 둘 다 회귀화했다 — 리뷰 반영의 모범이다. 이 방식을 유지하라.
- **한 걸음 더**: 산출물 문자열을 정규식으로 도려낼 때는 "제거 후 남는 조사/접미
  (까지/에/은/는)"까지 한 번 훑는 습관을 들이면 이번 `까지` 잔여 같은 표시 nit가 처음부터
  안 생긴다. 추출→치환 계열 코드는 마지막에 `strip`+접두 조사 제거를 한 줄 넣어라.

## 검증 재현 (리뷰어가 직접 실행/확인한 것)

리뷰 환경: Linux, Python 3.11.15 (+`py -3.11`→python3 셔님).

- `tests/test_capability_bench.py` → `ALL 184 CHECKS PASSED` (보고서 186 − openpyxl 조건
  분기 2). 라우팅 negative + owner 정확성 check 포함 통과.
- 라우팅 실측: minutes 시간표현 2건 차단 + 양성 3건 유지.
- owner E2E: 회의록.md 생성 → 담당 `김대리`, `| 7월 |` 없음, 품질 ok=True.
- 전체 회귀: 19개 중 17개 exit 0. Windows 전용 3개(agent_cli/encoding_paths/probes)는
  이 diff 무접촉 확인으로 대체.

## 작업계획 수정내용 (task/보드 반영)

- P14-04 task의 "리뷰 반영 (r1→r2)" 절 요구사항 2건 모두 충족 — task 추가 변경 없음.
- 보드: **P14-04 → APPROVED**. P14-04를 선행으로 갖는 대기 작업 없음(P14-05는 이미
  READY, 선행 P13-01·P14-02 충족). 신규 READY 전환 없음.
- Codex 다음 권장 순서: **P18-02 → P14-05 → P15-03 → P16-02** (모두 현재 READY).

## STATUS 반영

- P14-04 → **APPROVED**. 신규 READY 전환 없음(후속 의존 작업 없음).
