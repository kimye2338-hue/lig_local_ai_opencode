# 리뷰: P14-02 (r2) — **APPROVED**

| 항목 | 값 |
|------|-----|
| 대상 보고서 | plan/reports/P14-02-r2.md |
| 리뷰어 | Claude Fable 5 |
| 날짜 | 2026-07-04 |
| **Verdict** | **APPROVED** |

## 필수 수정

없음 — r1 필수 수정 2건 모두 반영 확인.

## 확인된 것 (리뷰어 실측 재현)

- **필수 1(제목 훼손) 해결**: 실제 CLI로 재현 —
  `금형 부품 검토 7월 8일까지` → 저장 title `금형 부품 검토`,
  `수정사항 반영 내일까지` → `수정사항 반영`,
  `월간 보고 7월 10일까지 제출` → `월간 보고 제출`. r1의 소실("형/정사항/간") 없음.
  한 글자 요일 패턴을 전체형(`금요일…`)과 접두 결합형(`이번주|다음주|오는 [월화수목금토일]`)
  으로 분리한 것이 정확한 처방.
- **필수 2(기한 오탐) 해결**: `언제까지인지 모르는 일` → 되묻기 문구 + exit 2 (미저장).
  `parse_due`의 단어 내부 substring 매칭 제거를 실측 확인
  (`중요한 일` → ok=False). `WEEKDAYS`(전체형)과 `SHORT_WEEKDAYS`(접두 필수) 분리로
  기존 승인 케이스는 보존됨: `이번주 금`·`다음주 화`·`금요일` 전부 정상 파싱 재현.
- **권고 1 채택**: `까지` 치환을 `(?:까지)(?=$|\s)` 경계로 좁혀 `언제까지인지` 내부
  훼손 방지. 보고서에 채택 기록됨.
- 테스트 보강이 리뷰어 실패 입력을 그대로 회귀화: parse_due negative check,
  CLI exit 2 check, 3개 제목 보존 check(stdout + 저장 title 양쪽).

## 검증 재현

리뷰 환경: Linux, Python 3.11.15 (+`py -3.11`→python3 셔님).

- `tests/test_schedule_store.py` → `ALL 68 CHECKS PASSED` (r1 60 + 신규 8).
- `tests/test_capability_bench.py` → `ALL 169 CHECKS PASSED` (P16-01까지 반영된 현재
  HEAD 기준; 보고서 시점 158과의 차이는 후속 커밋 누적).
- deviation 1건(CLI today fixture `금요일 14시`→`오늘 14시`) — 날짜가 2026-07-04로
  넘어가며 시간 의존 테스트가 깨지는 것을 막는 정당한 위생 수정, 정직하게 신고됨.
- 전체 회귀: 19개 중 16개 exit 0. Windows 전용 3개(agent_cli/encoding_paths/probes)는
  이 diff 무접촉 확인으로 대체.

## STATUS 반영

- P14-02 → **APPROVED**. 후속 READY 전환: P14-03, P14-04, P14-05, P15-03.
