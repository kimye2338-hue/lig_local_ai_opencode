# 리뷰: P14-02 (r1)

| 항목 | 값 |
|------|-----|
| 대상 보고서 | plan/reports/P14-02-r1.md |
| 리뷰어 | Claude Fable 5 |
| 날짜 | 2026-07-03 |
| **Verdict** | **CHANGES-REQUESTED** |

## 필수 수정

1. `agent_ops/agentops.py:_schedule_clean_title` — 문제: 한 글자 요일 패턴
   (`월|화|수|목|금|토|일`)을 접두 문맥 없이 제거해 **일반 명사가 훼손**된다. 리뷰어
   실측(격리 store, 실제 CLI):
   - `schedule add "금형 부품 검토 7월 8일까지"` → 제목 `형 부품 검` (금·토 소실)
   - `schedule add "수정사항 반영 내일까지"` → 제목 `정사항 반영 내`
   - `schedule add "월간 보고 7월 10일까지 제출"` → 제목 `간 보고 제출`
   / 요구: 한 글자 요일은 (오늘|내일|모레|글피|이번주|다음주|다음 주|오는) 접두와 결합된
   경우에만 제거하고, 그 외에는 `…요일` 전체형만 제거하라. `test_schedule_store.py`에
   제목 보존 checks 추가: 위 3개 입력의 저장 title이 각각 `금형 부품 검토`,
   `수정사항 반영`, `월간 보고 제출`이어야 한다.
2. `agent_ops/schedule_store.py:parse_due`(WEEKDAYS 한 글자 항목의 substring 매칭) —
   문제: 기한이 없는 문장이 **오탐 등록**된다. 리뷰어 실측:
   `schedule add "언제까지인지 모르는 일"` → exit 0, `2026-07-05(일)`로 저장
   ("일"이 일요일로 매칭). CLI 계약의 "기한 미인식 → exit 2, 저장 안 함"이 이런 입력에서
   무력화된다. / 요구: 한 글자 요일은 명시 접두(이번주/다음주/오는 등)와 결합된 경우에만
   인정하고 단독·단어 내 등장은 불인정하라. parse_due negative check
   (`언제까지인지 모르는 일` → ok=False)와 CLI check(동일 입력 add → exit 2) 추가.
   기존 승인 checks는 전부 요일 전체형("금요일", "이번주 금요일", "다음주 화요일")만
   사용하므로 충돌 없음 — 확인 완료. (참고: 이 코드는 P14-01에서 승인된 부분이지만
   P14-02의 CLI가 사용자 노출면을 만들었으므로 r2 범위에 포함시킨다 — P14-01 리뷰가
   놓친 부분이며 워커 귀책이 아니다.)

## 권고 (선택 반영 — 채택 여부를 다음 보고서에 기록할 것)

- `_schedule_clean_title`의 `까지` 치환이 단어 내부도 잘라낸다
  ("언제까지인지" → "언제 인지"). 조사형(`까지$` 또는 공백 경계)만 제거 권장.
- list/today의 `%-4s` 정렬은 한글 전각 폭 때문에 열이 어긋난다 — 계약의 열 순서는
  지켰으므로 필수는 아니나, 표시 폭 보정(2칸 계산)을 고려.

## 확인된 것 (잘한 것 — 다음 작업에서도 유지)

- CLI 계약 골격은 리뷰어 실행으로 전부 확인: add 성공 exit 0 / 모호 입력
  (`다음에 정리`) 되묻기 문구 원문 + exit 2 + 미저장 / remove `삭제할까요? [y/N]`에서
  n → exit 3 / `--yes` 생략 / 빈 목록 `등록된 일정이 없습니다.` / done·remove 미존재 id exit 2.
- remove가 P13-01 `classify_risk("schedule.remove")` 경유 — approval 정책과 CLI 게이트가
  한 곳(approval.py §schedule.*)에서 일관되게 dangerous로 분류됨.
- `schedule_management` keywords가 task 지정 10개 그대로 — 과광범위 단어 없음. plan
  라우팅은 high-confidence 단독 매칭 시에만 schedule 명령으로 좁혀 오라우팅을 피함.
- subprocess CLI 테스트가 격리 `LIG_SCHEDULE_DIR`에서 add→today→done→remove 전 구간 커버.

## 검증 재현 (리뷰어가 직접 실행/확인한 것)

리뷰 환경: Linux 컨테이너, Python 3.11.15 (+`py -3.11`→python3 셔님).

- `tests/test_schedule_store.py` → `ALL 60 CHECKS PASSED (schedule store)` /
  `tests/test_capability_bench.py` → `ALL 156 CHECKS PASSED` (보고서 158 − openpyxl 2).
- CLI 실동작: 위 "확인된 것"의 계약 경로 전부 + 필수 수정 1·2의 실패 사례 재현
  (명령·출력 원문은 필수 수정 항목에 기재).
- 전체 회귀: 19개 테스트 파일 중 16개 exit 0. Windows 전용 3개는 이 diff 무접촉 확인으로 대체.

## STATUS 반영

- P14-02 → CHANGES-REQUESTED (r2 대기). P14-03/P14-04/P14-05는 BLOCKED 유지.
