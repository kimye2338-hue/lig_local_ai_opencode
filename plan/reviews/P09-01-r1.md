# 리뷰: P09-01 (r1)

| 항목 | 값 |
|------|-----|
| 대상 보고서 | plan/reports/P09-01-r1.md |
| 리뷰어 | Claude Fable 5 |
| 날짜 | 2026-07-03 |
| **Verdict** | **CHANGES-REQUESTED** |

## 필수 수정

1. `agent_ops/lig_providers.py:get_profile` — 문제: `LIG_PROVIDER_PROFILE`을 lig-api.env
   파일에서만 읽는다. P09-03 실측 절차와 `launch/probe-gateway.bat` 안내는
   `set LIG_PROVIDER_PROFILE=local_openai` (셸 환경변수) 방식이라, lig_runtime을 타는
   경로에서 프로필 전환이 불가능하다. / 요구: `os.environ`이 env 파일보다 **우선**하도록
   `LIG_PROVIDER_PROFILE`, `LIG_LOCAL_BASE_URL`, `LIG_LOCAL_MODEL` 3개 키에 os.environ
   오버라이드를 추가하고, 테스트에 "os.environ 프로필이 파일 값을 이긴다" check 1개를
   추가하라 (테스트 상단에서 해당 env 키 pop으로 결정성 유지 — 기존 테스트 참조).

## 권고

- `profile_warning` 문구가 영어("unknown profile; using company_gateway") — 사용자 문구는
  한국어 원칙이므로 다음 수정 시 한국어로.

## 확인된 것

- 4-tuple 라우트/모델 오버라이드, local ready 판정(placeholder 검사 포함), env.example
  placeholder-only — 설계 의도대로.
- deviation 3건 전부 정직 기록. 특히 test_agent_cli 하네스 수정(공백 경로)은 정당한
  발견이며 14 checks 통과 재확인함.

## 검증 재현

- 전체 12개 테스트 파일 직접 실행: 총 329 checks 전부 통과 (lig_providers 19 포함).

## STATUS 반영

- P09-01 → CHANGES-REQUESTED (r2 제출 시 P09-02 READY 전환 예정)
