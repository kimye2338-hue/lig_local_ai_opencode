# 리뷰: P12-03 (r1)

| 항목 | 값 |
|------|-----|
| 대상 보고서 | plan/reports/P12-03-r1.md |
| 리뷰어 | Claude Fable 5 |
| 날짜 | 2026-07-03 |
| **Verdict** | **APPROVED** |

## 필수 수정

없음.

## 권고 (선택 반영 — 채택 여부를 다음 보고서에 기록할 것)

- 스크린샷 PNG는 커밋되지 않고 경로만 기록됨 — 현행 유지 OK. 다음 adapter_validation
  문서부터 "스크린샷은 로컬 아티팩트로 커밋하지 않음" 한 줄을 넣으면 경로가 repo에
  없는 이유를 묻지 않게 된다.
- `test_capability_bench.py`의 browser check는 available=True 상태 자체를 고정(pin)한다
  — 의도된 것이나, 향후 browser를 되돌릴 일이 생기면 bench도 함께 갱신해야 함을 인지.

## 확인된 것 (잘한 것 — 다음 작업에서도 유지)

- hard gate 준수: available=True 전환 후 auto-advance하지 않고 리뷰 대기.
- `validated: "local Chrome CDP, 2026-07-03"` 근거 필드 + 회사 로그인 pending 문구 보존
  — 과장 없는 상태 어휘 그대로.
- bench check를 "browser는 검증 근거 필수 / 나머지는 여전히 False 강제" 두 개로 분리한
  설계 — 다른 어댑터의 무단 available 전환을 계속 차단한다.
- 증거 파일(`browser_2026-07-03.md`, `_text.txt`)에 Chrome 버전·action별 원문 결과·테스트
  마지막 줄까지 기록 — 재현 가능한 형태.

## 검증 재현 (리뷰어가 직접 실행/확인한 것)

리뷰 환경: Linux 컨테이너, Python 3.11.15 (+`py -3.11`→python3 셔님).

- `tests/test_browser_adapter.py` (Chrome 9222 없음): `SKIP Chrome CDP live checks -
  skipped, not failed; ALL 11 STATIC CHECKS PASSED (browser adapter)` — 신규
  available=True/validated/pending check 포함 통과.
- **CDP live 독립 재현**: 리뷰 컨테이너의 Chromium 141 headless를
  `--remote-debugging-port=9222 --user-data-dir=<별도 프로필>`로 띄우고
  `browser_cdp.execute` 4개 action을 직접 실행 —
  `open_url`(로컬 127.0.0.1 http 서버, `status: loaded`), `get_title`(제목 일치),
  `extract_text`(본문 원문 일치), `screenshot`(PNG 생성 확인) 전부 ok=True.
  example.com 대상 live check만 이 컨테이너의 외부망 차단으로 실패 — adapter 문제 아님
  (오류 페이지 본문 `ERR_CONNECTION_RESET`으로 원인 확인). data: URL이 `open_url
  requires http(s) url`로 거부되는 가드도 확인.
- 전체 회귀: 17개 테스트 파일 중 14개 통과, 합계 405 checks + real_llm_smoke SKIP.
  `test_capability_bench.py` 154 checks — 보고서의 156과의 차이 2는 openpyxl 유무 조건
  분기(이 환경 미설치)로 코드에서 확인, 불일치 아님.
- `test_agent_cli.py` / `test_encoding_paths.py` / `test_probes.py` 3개는 Windows 전용
  (BAT 실행 / cmd.exe / `\` 경로 구분자 기대)이라 이 환경에서 실행 불가 — 세 파일 모두
  P12-03 diff(34cc6ad..0429742)가 건드리지 않았음을 diff로 확인.
- STATUS 수정 범위: 본인 행 + 이력 줄만 — 소유권 침범 없음.

## STATUS 반영

- P12-03 → APPROVED. 선행으로 풀리는 BLOCKED 작업 없음 (P12-03을 선행으로 갖는 행 없음).
