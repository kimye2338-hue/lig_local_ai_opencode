# 리뷰: P16-04 (r1) — **APPROVED**

| 항목 | 값 |
|------|-----|
| 대상 보고서 | plan/reports/P16-04-r1.md |
| 리뷰어 | Claude Fable 5 |
| 날짜 | 2026-07-04 |
| **Verdict** | **APPROVED** |

## 필수 수정

없음.

## 확인된 것 (리뷰어 실측 재현)

- **hwp/solidworks available=False** 유지, app validation pending 정직 표기.
- **부재 안전**: pywin32 없는 리뷰 환경에서 `md_to_hwp`(pywin32 안내)·`run_macro`
  (`SolidWorks 문서 파일 없음`) 모두 crash 없이 안내, unknown action 거부.
- **HWP 신규 파일 정책**: action 표면이 `md_to_hwp`뿐(원본 열기 API 부재), md 원본을
  건드리지 않고 지정 out_path(.hwp)로 SaveAs — 원본 불가침 충족.
- **SolidWorks 사본/무저장**: `사본_<이름>` + `shutil.copy2`로 복사 후 실행, 결과
  `saved: False`(자동 저장 없음), `.bas`는 `manual_import` 강등 안내. task 금지(.bas
  자동 변환 성공 가정) 준수.
- **subprocess/CODE_ROOT 우려 없음**: 두 어댑터 모두 in-process COM(excel_com 패턴) —
  P15-03류 데이터-루트 함정 해당 없음.

## 권고 (선택 반영 — 채택 여부를 다음 보고서에 기록할 것)

- `hwp_com.md_to_hwp`의 out_path는 호출자 지정이라 기존 .hwp가 있으면 덮어쓴다(원본 md는
  안전하므로 정책 위반 아님). office_convert처럼 `_unique` 가드를 넣으면 실수로 이전
  산출물을 덮는 일을 막을 수 있다. 필수 아님.

## 검증 재현

리뷰 환경: Linux, Python 3.11.15 (+`py -3.11`→python3 셔님, pywin32 없음).

- `test_office_adapters.py` → `SKIP Excel COM live checks …; ALL 46 STATIC CHECKS PASSED`
  (HWP/SW 부재·action surface·사본/신규 정책·registry unavailable 포함).
- 어댑터 실측(위): available=False, 부재 안내, action 표면, unknown 거부.
- **HWP/SW live 실행은 리뷰 env에 앱/pywin32 없어 독립 재현 불가** — company validation
  pending으로 정직 스코프, 회사 P19 실측 대상.
- 전체 22개 중 19개 exit 0. Windows 전용 3개만 RED(diff 무접촉).

## 피드백 (코칭)

- excel_com/office_convert의 사본·optional-import·정직 강등 패턴을 그대로 재사용해 결함
  없이 마무리했다 — 어댑터 축의 성숙한 마무리다.

## 작업계획 수정내용 (task/보드 반영)

- task DoD 4항목 충족 — task 변경 없음.
- 보드: **P16-04 → APPROVED**. 선행 해제: **P17-02 → READY**(선행 P16-04 충족, INTERNET).
- Codex 다음 권장 순서: **P18-02 r2 → P17-02(INTERNET)**.

## STATUS 반영

- P16-04 → **APPROVED**. **P17-02 BLOCKED→READY**(INTERNET 환경 필요).
