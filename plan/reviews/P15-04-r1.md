# 리뷰: P15-04 (r1) — **APPROVED**

| 항목 | 값 |
|------|-----|
| 대상 보고서 | plan/reports/P15-04-r1.md |
| 리뷰어 | Claude Fable 5 |
| 날짜 | 2026-07-04 |
| **Verdict** | **APPROVED** |

## 필수 수정

없음.

## 확인된 것 (리뷰어 실측 재현)

- **hard gate 준수**: office adapter `available=False` 유지(집 실측을 app validation으로
  포장하지 않음). `home_smoke` 필드가 `"passed 2026-07-04 (Excel 최신) — Office 2016
  검증은 app validation pending"`으로 정직 표기.
- **신규 파일 정책(원본 미덮어쓰기)**: `_output_path`/`_unique_path` 실측 —
  `memo.docx`가 이미 있으면 `memo_2.docx` 생성(덮어쓰기 회피). `md_to_docx`/`spec_to_pptx`
  는 SaveAs를 새 out 경로로만, `finally: Quit()`로 프로세스 정리.
- **Excel 사본 정책 보존**: `_office_execute`가 conversion action만 office_convert로,
  나머지(read_range 등)는 excel_com으로 라우팅 → P15-02 copy-only 정책 그대로.
- **부재 안전**: pywin32 없는 리뷰 환경에서 `md_to_docx`·excel action 모두 crash 없이
  `pywin32 미설치` 안내. `test_office_live_smoke.py`는 pywin32 부재 시
  `SKIP Office live smoke - pywin32 unavailable, skipped not failed` + exit 0.

## 검증 재현 (리뷰어가 직접 실행/확인한 것)

리뷰 환경: Linux, Python 3.11.15 (+`py -3.11`→python3 셔님), Excel/pywin32 없음.

- `test_office_adapters.py` → `SKIP Excel COM live checks …; ALL 33 STATIC CHECKS PASSED`.
- `test_office_live_smoke.py` → SKIP+exit 0. `test_capability_bench.py` 191(보고서 193 −
  openpyxl 2). 전체 22개 중 실패는 Windows 전용 3개 + P16-02 test뿐(P15-04 무관).
- available=False / home_smoke / 신규파일 회피 / excel 라우팅 직접 실측(위).
- **못한 것(정직)**: 집 Excel 사본 왕복·원본 해시 불변·프로세스 잔류 없음의 *live* 실측은
  리뷰 환경에 Excel/pywin32가 없어 **독립 재현 불가**. 보고서의 집 PC 실측을 그대로
  인정하되(available=False·home_smoke로 정직 스코프됨), 이 부분은 Office 2016 회사
  검증(P19)에서 최종 확인된다.

## 피드백 (코칭)

- "집 성공=스모크일 뿐" 원칙을 available=False + home_smoke로 정확히 지켰다. COM 어댑터의
  finally-Quit·신규파일-only 패턴도 excel_com과 일관 — 유지.

## 작업계획 수정내용 (task/보드 반영)

- task 요구(사본 왕복·md→docx/spec→pptx·available=False·home_smoke) 충족 — task 변경 없음.
- 보드: **P15-04 → APPROVED**. 후속 의존 작업 없음(리프). 신규 READY 없음.
- 집→회사 이관: office 변환/Excel 왕복의 Office 2016 재검은 NEXT_ONSITE/P19 범위(이미 추적됨).

## STATUS 반영

- P15-04 → **APPROVED**. 신규 READY 전환 없음.
