# 리뷰: P16-01 (r1)

| 항목 | 값 |
|------|-----|
| 대상 보고서 | plan/reports/P16-01-r1.md |
| 리뷰어 | Claude Fable 5 |
| 날짜 | 2026-07-04 |
| **Verdict** | **CHANGES-REQUESTED** |

## 필수 수정

1. `agent_ops/capabilities.py:matlab_automation.keywords` — 문제: 키워드 `.m`이
   **substring 매칭**(capabilities.py:20 "matched … as substrings", `kw in text`)이라
   `.md`를 포함하는 모든 문서 작업이 matlab_automation으로 **오라우팅**되고
   `matlab_script` 아티팩트가 스퓨리어스하게 생성된다. 리뷰어 실측(`plan_task`):
   - `이 보고서를 정리해서 요약.md로 저장해줘` → caps `[document_generation, file_ops,
     matlab_automation]`, kinds `[document, **matlab_script**]`
   - `README.md 문서 작성해줘` → 동일하게 matlab_script 혼입
   순수 문서 요청이 MATLAB `작업.m` scaffold까지 만들어낸다. 이는 P14-02 task가 경고한
   "과광범위 키워드 → 오라우팅"과 같은 계열의 결함이며, capability/artifact kind 추가는
   hard gate라 리뷰에서 걸러야 한다. / 요구: `.m`을 키워드에서 제거하라("매트랩"/"matlab"이
   이미 커버). `.m` 확장자 신호를 꼭 남기려면 `.m 스크립트`/`.m 파일`처럼 경계를 준
   구절로 바꿔라. `test_capability_bench.py`에 **negative 라우팅 check** 추가:
   `요약.md로 저장해줘`(또는 `README.md 문서 작성해줘`)의 capabilities/artifact_kinds에
   `matlab_automation`/`matlab_script`가 **없어야** 한다.

## 권고 (선택 반영 — 채택 여부를 다음 보고서에 기록할 것)

- `그래프 그려`는 자연스러우나 `플롯`/`후처리`와 함께 비-MATLAB 맥락(엑셀 차트 등)에서도
  걸릴 여지가 있다. 지금은 문제 재현 안 되니 유지하되, 향후 오라우팅 신고 시 좁힐 것.

## 확인된 것 (잘한 것 — 유지)

- `gen_matlab_script`가 task 골격을 **그대로** 따름: 섹션 순서(설정→로드→필터→통계→
  플롯→저장), `try/catch err`, `exit(1)`, `INPUT_FILE` 상수, `matlab -batch` 실행 주석,
  `app validation pending — MATLAB 2024a` 상태 표기 전부 존재. 기존 generator 시그니처
  (`_ensure_context`/`_input_names`/`atomic_write_text`) 재사용.
- **input-grounded 실동작 확인**: CSV 입력 파일명(`시험결과.csv`)과 notable 항목
  (`치수B 초과`)이 `.m` 주석에 반영됨 — 리뷰어가 생성+품질검증 E2E로 재현
  (`validate_files('matlab_script', …)` → ok=True).
- quality 규칙 6종(batch 실행/try-catch/exit/INPUT_FILE/pending/base-함수)이 툴박스 함수
  (smoothdata/findpeaks/butter/filtfilt) 금지까지 포함 — task 금지 조항 준수.
- `_KIND_SUFFIXES`에 `.m`, `ARTIFACT_KIND_INFO`에 matlab_script 등록 — 일관.

## 검증 재현

리뷰 환경: Linux, Python 3.11.15 (+`py -3.11`→python3 셔님), MATLAB 없음.

- `tests/test_capability_bench.py` → `ALL 169 CHECKS PASSED` (양성 라우팅/생성/품질/입력
  반영 check 통과 — 단, 위 negative 케이스는 미커버).
- 오라우팅 실측: `plan_task`로 `.md` 4개 입력 중 3개에서 matlab_script 혼입 재현
  (필수 수정에 명령·출력 기재).
- 생성기 E2E: `작업.m` 생성 → suffix `.m` → 품질 ok=True → 입력/ notable 반영 확인.
- 전체 회귀: 19개 중 16개 exit 0. Windows 전용 3개는 이 diff 무접촉 확인으로 대체.

## STATUS 반영

- P16-01 → CHANGES-REQUESTED (r2 대기 — 키워드 1건 + negative check). P16-02/P16-03은
  BLOCKED 유지.
