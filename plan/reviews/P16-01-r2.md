# 리뷰: P16-01 (r2) — **APPROVED**

| 항목 | 값 |
|------|-----|
| 대상 보고서 | plan/reports/P16-01-r2.md |
| 리뷰어 | Claude Fable 5 |
| 날짜 | 2026-07-04 |
| **Verdict** | **APPROVED** |

## 필수 수정

없음 — r1 필수 수정 1건 반영 확인.

## 확인된 것 (리뷰어 실측 재현)

- **필수 1(`.m` 오라우팅) 해결**: 키워드에서 단독 `.m`을 제거하고 경계 있는
  `.m 스크립트`/`.m 파일`로 대체. 리뷰어가 `plan_task`로 다중 케이스 재현 —
  `요약.md로 저장해줘`·`README.md 문서 작성해줘`·`note.md 파일로 만들어줘`·
  `example.m 로 저장` **전부** matlab_automation/matlab_script 미포함(kinds=[document]).
  r1의 스퓨리어스 matlab_script 혼입 재발 없음.
- **양성 라우팅 유지**: `시험 데이터 후처리 매트랩 스크립트`·`.m 스크립트로 후처리`·
  `matlab 플롯 그려줘` 전부 matlab_script로 정상 라우팅 — 좁힌 키워드가 정당한 신호는
  계속 잡는다.
- 테스트 회귀화: bench에 negative check 2개(`markdown filename does not route to
  MATLAB`, `… does not plan MATLAB artifact`) 추가.
- 권고(그래프 그려/플롯/후처리 유지)는 미채택으로 정직하게 기록, 향후 오라우팅 신고 시
  축소 조건 명시 — 타당.
- r1에서 확인한 생성기/품질 검증기/input-grounded 실동작은 이 diff에서 무손상
  (capabilities.py 키워드 + bench 2줄만 변경).

## 검증 재현

리뷰 환경: Linux, Python 3.11.15 (+`py -3.11`→python3 셔님), MATLAB 없음.

- `tests/test_capability_bench.py` → `ALL 171 CHECKS PASSED` (보고서 173과의 차이 2는
  openpyxl 조건 분기 — 이 환경 미설치).
- 라우팅 실측: `.md`/`.m` 7개 케이스 전부 기대대로(4 negative + 3 positive) 재현.
- 전체 회귀: 19개 중 16개 exit 0. Windows 전용 3개(agent_cli/encoding_paths/probes)는
  이 diff 무접촉 확인으로 대체.

## STATUS 반영

- P16-01 → **APPROVED**. 후속 READY 전환: P16-02, P16-03.
