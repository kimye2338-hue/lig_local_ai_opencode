# 리뷰: P11-01 (r1)

| 항목 | 값 |
|------|-----|
| 대상 보고서 | plan/reports/P11-01-r1.md |
| 리뷰어 | Claude Fable 5 |
| 날짜 | 2026-07-03 |
| **Verdict** | **APPROVED** |

## 필수 수정

없음.

## 권고 (선택 반영 — 채택 여부를 다음 보고서에 기록할 것)

- mock 자가검증이 정본 경로 `agent_ops/results/reports/capability_floor.md`를 덮어쓴다
  — 회귀를 돌릴 때마다 마지막 **실측** 리포트가 mock 리포트로 교체되고 doctor의
  timestamp도 mock 시각을 가리키게 된다. task가 정본 생성 검증을 요구한 설계라 워커
  잘못은 아니며, P11-02에서 실측 리포트 보존 방식(모델명/모드 suffix 등)을 정하겠다
  (Fable가 P11-02 task에 반영 예정 — 워커는 지금 조치 불필요).
- native 비율 0/30에 대한 pending 노트(runtime-last.json이 최종 LLM 턴 기준)는 타당한
  관찰 — P11-02 해석 시 근거로 유지할 것.

## 확인된 것 (잘한 것 — 다음 작업에서도 유지)

- 시나리오 10종의 태스크 문자열·기대 파일·필수 단어가 task 표와 **글자 단위로 일치**
  (발명/완화 없음). fixture 내용도 지정 원문 그대로.
- P09-03 패턴(`local_llm_is_running`/`make_env`/`run_agent`/`copy_diag`) 재사용 — 재구현
  금지 규칙 준수. 실패 분류도 기존 diag 필드(`tool_call_mode`, `outcome`) 매핑만 사용,
  새 파서 없음.
- 금지 조항 준수: 제품 코드는 doctor 필드 1개만 추가(테스트 외 toolcall_parser/
  tool_dispatch/lig_runtime 무접촉 — 커밋 파일 범위로 확인).
- 실패 사례는 detail 180자 절단으로 대화 원문 저장 금지 준수.
- 요구 범위(하네스+mock)를 넘어 Ollama 실측 21/30까지 수행하고 70%를 90% 목표 미달로
  정직하게 기록 — P11-02의 출발점 데이터로 유효.

## 검증 재현 (리뷰어가 직접 실행/확인한 것)

리뷰 환경: Linux 컨테이너, Python 3.11.15 (+`py -3.11`→python3 셔님). Ollama 없음 —
실측 21/30 수치 자체는 재현 불가(워커 환경 실측), 하네스 로직은 아래로 검증.

- `tests/test_capability_floor.py` (서버 부재): mock 자가검증 7 checks 전부 PASS 후
  `SKIP  local llm not running — skipped, not failed`, exit 0 — DoD 1 원문 재현.
- doctor `artifact_pipeline.capability_floor_report`가 리포트 존재 시 path/timestamp
  dict를 반환함을 테스트 경유로 확인 (`doctor reports capability floor report` PASS).
- 집계 로직 정독: classify_failure의 6개 유형 매핑이 task 정의와 일치, 리포트 골격
  (성공률 라인 + 5열 표 + 실패 요약)이 지정 형식 그대로.
- 전체 회귀: 19개 테스트 파일 중 16개 exit 0 (bench 156 = 보고서 158 − openpyxl 조건
  분기 2). Windows 전용 3개(agent_cli/encoding_paths/probes)는 이 diff 무접촉 확인으로 대체.

## STATUS 반영

- P11-01 → APPROVED. **P11-02 → READY** (선행 P11-01 충족, LOCAL-LLM 환경 필요).
