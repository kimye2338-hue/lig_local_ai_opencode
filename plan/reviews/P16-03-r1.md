# 리뷰: P16-03 (r1)

| 항목 | 값 |
|------|-----|
| 대상 보고서 | plan/reports/P16-03-r1.md |
| 리뷰어 | Claude Fable 5 |
| 날짜 | 2026-07-04 |
| **Verdict** | **CHANGES-REQUESTED** |

## 필수 수정

1. `agent_ops/capabilities.py:simulation_automation.keywords` — 문제: **`해석`(매우 흔한
   한국어: 데이터 해석/회의 해석/문서 해석)과 영어 `journal`이 광범위 substring 매칭**돼
   ANSYS와 무관한 요청이 simulation_automation으로 오라우팅되고 `fluent_journal` +
   `ansys_script` 두 아티팩트를 스퓨리어스 생성한다. 리뷰어 실측(`plan_task`):
   - `이 데이터 해석해줘` → kinds `[fluent_journal, ansys_script]` (**순수 데이터 분석
     요청이 ANSYS journal 생성**)
   - `회의 내용 해석해서 정리해줘` → `[document, meeting_minutes, **fluent_journal,
     ansys_script**]`
   - `journal 정리해줘` → `[document, **fluent_journal, ansys_script**]`
   `.m`·`minutes`·`weekly`에 이은 계열이나 `해석`은 일상어라 **영향 범위가 가장 넓다**.
   **참고: 이 키워드는 task 지시서(P16-03 작업 항목 1)가 나열한 것** — 워커 단독 잘못이
   아니라 task 스펙 결함이므로 task도 함께 정정한다(아래 작업계획 수정). / 요구: bare
   `해석`·`journal`을 제거하고 도메인어로만 라우팅하라. 아래 "되는 방법" 채택 + negative
   bench check 추가.

## 되는 방법 (리뷰어 검증 완료)

`capabilities.py` — bare `해석`/`journal` 제거, 분석 의도 커버는 도메인 경계어로:

```python
"keywords": ["앤시스", "ansys", "플루언트", "fluent", "시뮬레이션",
             "메카니컬", "spaceclaim", "icepak", "cfd", "fea"],
```

리뷰어 실측(전부 통과): `플루언트 해석 journal 만들어줘`✓·`ansys fluent 시뮬레이션`✓·
`메카니컬 스크립트`✓ (양성 유지 — "플루언트"/"ansys"/"메카니컬"이 잡음) /
`이 데이터 해석해줘`✗·`회의 내용 해석해서 정리해줘`✗·`journal 정리해줘`✗ (오라우팅 차단).

bench negative check:

```python
for neg in ["이 데이터 해석해줘", "journal 정리해줘"]:
    caps = {c["id"] for c in plan_task(neg)["capabilities"]}
    check(f"non-ANSYS request does not route to simulation ({neg})",
          "simulation_automation" not in caps and "fluent_journal" not in plan_task(neg)["artifact_kinds"], neg)
```

## 확인된 것 (리뷰어 실측 재현 — 실체는 정상)

- **artifact kind 2종 실체**: `작업.jou`(fluent_journal) — read-case/iterate/exit 골격,
  **"해석 세팅·수렴 판단은 사용자 책임" 경고 포함**, quality가 경고 제거 시 실패로 잡음.
  `mechanical_script.py`(ansys_script) quality ok. task 금지("공학 판단을 자동화라 표기
  금지") 준수.
- **fluent_batch 어댑터**: `available=False`, 부재 시 안내(`Fluent journal 파일 없음` 등),
  pending 정직(`app validation pending: 회사 ANSYS Fluent 2024R1 …`).
- **테스트 이식성 양호**: P16-02 교훈 반영 — `test_batch_adapters.py`가 POSIX에서
  `ALL 36 CHECKS PASSED`(nt-가드 utf16 1개 제외, Windows 37 대비 일관). 3라운드 반복 없음.

## 검증 재현

리뷰 환경: Linux, Python 3.11.15 (+`py -3.11`→python3 셔님, openpyxl 설치됨).

- `test_capability_bench.py` → `ALL 203 CHECKS PASSED`, `test_batch_adapters.py` → 36.
- 오라우팅 실측: `해석`/`journal` 3케이스 스퓨리어스 ANSYS 생성 재현(필수 수정 근거).
- 수정안 + artifact 실체 + fluent 어댑터 실측(위).
- 전체 22개 중 19개 exit 0. Windows 전용 3개만 RED(diff 무접촉).

## 피드백 (코칭)

- **좋았던 것**: 공학책임 경고를 quality 규칙으로 강제한 것, 테스트 이식성을 처음부터
  올바르게(POSIX octal) 한 것 — P16-02 3라운드 교훈이 반영됐다.
- **키워드 self-check(4번째)**: 이번엔 task 스펙이 광범위어를 준 게 원인이지만, 워커도
  키워드 추가 시 "이 단어가 일상 요청에 substring으로 들어가나"를 negative 라우팅 test로
  스스로 확인했다면 즉시 잡혔다. 한국어 일상어(`해석`)는 영어 단어보다 더 위험하다.

## 작업계획 수정내용 (task/보드 반영)

- **task 스펙 정정**: `plan/tasks/P16-03-ansys-journal.md` 작업 항목 1의 키워드 목록에서
  bare `해석`/`journal`을 제거하고 도메인어(+cfd/fea)로 교체했다(Fable 정정). "리뷰 반영
  (r1→r2)" 절도 추가.
- 보드: **P16-03 → CHANGES-REQUESTED**. 후속 의존 없음(P16-04는 이미 READY).
- Codex 다음 권장 순서: **P16-03 r2 → P18-02 → P16-04 → P19-01**.

## STATUS 반영

- P16-03 → CHANGES-REQUESTED (r2 대기 — 키워드 1건 + negative check).
