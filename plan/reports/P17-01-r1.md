# 작업 보고서: P17-01 (r1)

| 항목 | 값 |
|------|-----|
| 작업 | P17-01 xlsx 입력 ingest (openpyxl optional) |
| 수행자 | Codex GPT-5 |
| 날짜 | 2026-07-03 |
| 시작 HEAD | 88b87ba |
| 결과 커밋 | 2666047, 6087d71 |
| 라운드 | r1 |

## 1. 결과 요약 (3줄 이내, 상태 어휘 사용)

implemented: `input_ingest.py`에 optional `openpyxl` 기반 `.xlsx` 읽기를 추가하고, 미설치 시 unsupported로 정직하게 강등한다.
implemented: 첫 시트 행/열/헤더, 이상 행, 다중 시트, 대용량 앞부분 분석 fact를 CSV와 같은 방식으로 기록한다.
locally validated: 이 PC에서는 openpyxl available 경로로 실제 xlsx fixture를 읽었고 전체 회귀가 통과했다.

## 1.5 사용자 체감 변화

이제 사용자가 시험 데이터 `.xlsx`를 입력으로 주면, 설치 환경에서는 첫 시트의 헤더/행 수/불합격 같은 이상 행이 보고서 근거로 들어간다. openpyxl이 없는 PC에서도 명확히 "openpyxl 미설치"라고 unsupported로 표시하므로 조용히 실패하거나 엉뚱한 보고서를 만들지 않는다.

## 2. 변경 파일과 이유 (파일별 1~2줄, 신규/수정 구분)

- 수정: `workspace-template/agent_ops/input_ingest.py`
  - `openpyxl` optional import와 `.xlsx` 분기를 추가했다.
  - `read_only=True`, `data_only=True`로 첫 시트를 읽고, max 2000행 초과 시 fact에 명시한다.
- 수정: `workspace-template/tests/test_capability_bench.py`
  - openpyxl available이면 xlsx fixture 생성→facts/notable/input-grounded 문서 반영을 검증한다.
  - unavailable이면 unsupported reason과 문서 limitations 반영을 검증하는 분기를 추가했다.

## 3. DoD 자가 점검 (task의 DoD를 전부 나열, 항목별 ✅ / ❌ / 부분 + 한 줄 근거)

- ✅ openpyxl 유무 양쪽 경로가 각각 정직하게 동작 (테스트 증명)
  - benchmark에 available/unavailable 분기를 모두 두었다. 이번 PC에서는 `INFO  xlsx ingest branch: openpyxl available` 경로가 실행됐다.
- ✅ xlsx facts가 CSV와 동일 규칙 (행·열/이상 행/파일명 반영)
  - `xlsx facts mirror CSV rows columns and sheets`, `xlsx ingest flags abnormal rows`, `xlsx input-grounded document reflects file facts` checks가 검증한다.
- ✅ 코어 stdlib-only 원칙 유지 (optional import — 부재 시 기능 저하 없이 unsupported)
  - `try/except ImportError`로 optional 처리했고, 미설치 reason은 `dependencies.json 'office-doc-wheels'`를 가리킨다.
- ✅ 기존 checks 무손상
  - 전체 15개 테스트 파일, 총 431 checks가 통과했다.

## 4. 테스트 증거 (실행 명령 + 출력 마지막 줄 원문 붙여넣기 — 회귀 9개 + 신규 전부)

```text
py -3.11 tests\test_capability_bench.py
→ ALL 155 CHECKS PASSED (capability benchmark)
```

```text
py -3.11 tests\test_agent_cli.py
→ ALL 15 CHECKS PASSED (agent CLI/BAT smoke)
```

```text
py -3.11 tests\test_agent_e2e.py
→ ALL 17 CHECKS PASSED (agent E2E)
```

```text
py -3.11 tests\test_approval_audit.py
→ ALL 18 CHECKS PASSED (approval + audit)
```

```text
py -3.11 tests\test_browser_adapter.py
→ SKIP Chrome CDP live checks - skipped, not failed; ALL 10 STATIC CHECKS PASSED (browser adapter)
```

```text
py -3.11 tests\test_encoding_paths.py
→ 11 checks passed
```

```text
py -3.11 tests\test_lig_providers.py
→ 24 checks passed
```

```text
py -3.11 tests\test_lig_runtime.py
→ 18 checks passed
```

```text
py -3.11 tests\test_probes.py
→ ALL 9 CHECKS PASSED (probes)
```

```text
py -3.11 tests\test_resume.py
→ 9 checks passed
```

```text
py -3.11 tests\test_schedule_store.py
→ ALL 53 CHECKS PASSED (schedule store)
```

```text
py -3.11 tests\test_tool_dispatch.py
→ ALL 23 CHECKS PASSED (tool dispatch)
```

```text
py -3.11 tests\test_toolcall_parser.py
→ 12 checks passed
```

```text
py -3.11 tests\test_work_command.py
→ ALL 19 CHECKS PASSED (work command)
```

```text
py -3.11 tests\test_ws_min.py
→ ALL 38 CHECKS PASSED (minimal websocket client)
```

## 5. 계획과 다르게 한 것 (deviation — 없으면 "없음". 사소한 것도 전부)

없음

## 6. 하지 않은 것 / pending (상태 어휘로)

- dependency_or_app_pending: openpyxl wheel 설치/번들링은 P17-02 이후 범위다. 이 작업에서는 `dependencies.json`의 `office-doc-wheels` optional 항목 존재만 확인했다.

## 7. 리뷰어에게 질문 / 결정 요청 (없으면 "없음")

없음

## 8. 다음 작업 제안 (STATUS 기준 다음 READY 확인 결과)

P17-02는 아직 BLOCKED다. Codex 기준 다음 READY는 P18-01 `secret 스캔 pre-commit 스크립트`다.
