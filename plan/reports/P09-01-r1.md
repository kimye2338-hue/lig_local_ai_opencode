# 작업 보고서: P09-01 (r1)

| 항목 | 값 |
|------|-----|
| 작업 | P09-01 LLM provider 프로필/env 완전 오버라이드 |
| 수행자 | Codex GPT-5 |
| 날짜 | 2026-07-03 |
| 시작 HEAD | ce412af |
| 결과 커밋 | e94ee72, 99cc776, a4451c9 |
| 라운드 | r1 |

## 1. 결과 요약 (3줄 이내, 상태 어휘 사용)

implemented: `company_gateway`/`local_openai` provider profile selection and route/model env overrides are implemented.
locally validated: `local_openai` is ready without an API key and validation reports remain host/key redacted.
locally validated: protocol regression commands pass on this Windows path with Korean text and spaces.

## 2. 변경 파일과 이유 (파일별 1~2줄, 신규/수정 구분)

- 수정: `workspace-template/agent_ops/lig_providers.py`
  - `_ROUTE_DEFAULTS`를 route/model env override tuple로 확장하고 `get_profile()` 및 `local_openai` profile을 추가했다.
  - `validate_config()`에 `profile`/`profile_warning`을 추가하고 profile별 ready 판정을 분리했다.
- 신규: `workspace-template/config/lig-api.env.example`
  - 신규 profile, route, model, timeout env key를 placeholder-only 템플릿으로 문서화했다.
- 수정: `workspace-template/tests/test_lig_providers.py`
  - route/model override, local profile without secret, invalid profile fallback, host redaction checks를 추가했다.
- 수정: `workspace-template/tests/test_agent_cli.py`
  - Windows 공백/한글 경로에서 BAT smoke가 실제 launcher를 직접 실행하도록 하네스 호출 방식을 수정했다.

## 3. DoD 자가 점검 (task의 DoD를 전부 나열, 항목별 ✅ / ❌ / 부분 + 한 줄 근거)

- ✅ 라우트/모델/타임아웃 변경이 env 편집만으로 반영됨을 테스트가 증명
  - `route and model overrides` check가 `LIG_ROUTE_CODING`, `LIG_MODEL_CODING`, `LIG_API_TIMEOUT_SEC` 반영을 검증한다.
- ✅ local_openai 프로필이 api_key 없이 ready=True
  - `local_openai ready without secret` check가 `api_key_set=False` 및 `ready=True`를 검증한다.
- ✅ validate_config 출력에 host/key 원문 미포함
  - 기존 `no secret in report` 및 신규 `validate report omits host strings` check가 secret/host 미노출을 검증한다.
- ✅ env.example에 신규 키 전부 문서화
  - `LIG_PROVIDER_PROFILE`, `LIG_MODEL_CODING`, `LIG_MODEL_CHAT`, `LIG_MODEL_FALLBACK`, `LIG_LOCAL_BASE_URL`, `LIG_LOCAL_MODEL`을 포함했다.
- ✅ 기존 260 checks 무손상
  - 회귀 10개 명령 합산 273 checks가 모두 통과했다.

## 4. 테스트 증거 (실행 명령 + 출력 마지막 줄 원문 붙여넣기 — 회귀 9개 + 신규 전부)

```text
py -3.11 tests\test_capability_bench.py
→ ALL 145 CHECKS PASSED (capability benchmark)
```

```text
py -3.11 tests\test_toolcall_parser.py
→ 12 checks passed
```

```text
py -3.11 tests\test_lig_providers.py
→ 19 checks passed
```

```text
py -3.11 tests\test_lig_runtime.py
→ 14 checks passed
```

```text
py -3.11 tests\test_resume.py
→ 9 checks passed
```

```text
py -3.11 tests\test_encoding_paths.py
→ 11 checks passed
```

```text
py -3.11 tests\test_tool_dispatch.py
→ ALL 23 CHECKS PASSED (tool dispatch)
```

```text
py -3.11 tests\test_agent_e2e.py
→ ALL 17 CHECKS PASSED (agent E2E)
```

```text
py -3.11 tests\test_agent_cli.py
→ ALL 14 CHECKS PASSED (agent CLI/BAT smoke)
```

```text
py -3.11 tests\test_probes.py
→ ALL 9 CHECKS PASSED (probes)
```

## 5. 계획과 다르게 한 것 (deviation — 없으면 "없음". 사소한 것도 전부)

- task는 `workspace-template/config/lig-api.env.example` 수정을 지시했지만 해당 경로가 없었다. 지시 경로에 신규 템플릿 파일을 만들었다.
- 전체 회귀 중 `test_agent_cli.py`가 `cmd /c <bat path>` 방식으로 공백 포함 경로를 잘못 실행해 실패했다. 제품 BAT가 아니라 테스트 하네스 호출 문제였고, launcher BAT를 직접 실행하는 subprocess 인자 리스트로 수정했다.
- 시작 절차의 IN-PROGRESS 보드 커밋을 먼저 만들기 위해 결과 커밋이 2개로 나뉘었다.

## 6. 하지 않은 것 / pending (상태 어휘로)

- company validation pending: 실제 회사 gateway 및 실제 local Ollama/OpenAI-compatible 서버 호출은 수행하지 않았다.
- app validation pending: `P09-02`의 runtime route-selection/doctor 확장은 선행 작업 범위 밖이라 수행하지 않았다.

## 7. 리뷰어에게 질문 / 결정 요청 (없으면 "없음")

없음

## 8. 다음 작업 제안 (STATUS 기준 다음 READY 확인 결과)

STATUS 기준 다음 Codex READY 후보는 `P12-01 stdlib WebSocket 미니 클라이언트`다. 단, P09-01이 APPROVED되면 Fable이 P09-02를 READY로 전환할 수 있다.
