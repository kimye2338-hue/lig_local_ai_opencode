# 작업 보고서: P18-01 (r1)

| 항목 | 값 |
|------|-----|
| 작업 | P18-01 secret 스캔 pre-commit 스크립트 |
| 수행자 | Codex GPT-5 |
| 날짜 | 2026-07-03 |
| 시작 HEAD | 3e51d5a |
| 결과 커밋 | 559bd63, 666471c |
| 라운드 | r1 |

## 1. 결과 요약 (3줄 이내, 상태 어휘 사용)

implemented: staged 파일 대상 secret/hostname scanner `scripts/precommit_scan.py`를 추가했다.
implemented: git hook 설치용 `scripts/install_hooks.bat`과 tmp git repo 기반 테스트를 추가했다.
locally validated: secret scan 단독, install hook smoke, 전체 회귀가 통과했다.

## 1.5 사용자 체감 변화

이제 훅을 설치하면 커밋 직전에 API 키, Bearer 토큰, generic 내부 도메인 패턴이 staged 파일에 들어갔는지 자동으로 막아준다. 예외가 필요한 줄은 `# secret-scan-ok`로 명시해야 하므로, 실수로 lig-api.env 실값이나 내부 주소를 커밋할 위험이 줄어든다.

## 2. 변경 파일과 이유 (파일별 1~2줄, 신규/수정 구분)

- 신규: `workspace-template/scripts/precommit_scan.py`
  - `git diff --cached --name-only`와 `git show :path`로 staged 텍스트만 검사한다.
  - 기본 4패턴과 `LIG_SECRET_EXTRA_PATTERNS` 파일 기반 추가 패턴을 지원한다.
- 신규: `workspace-template/scripts/install_hooks.bat`
  - `git rev-parse --show-toplevel`로 git root를 찾아 `.git/hooks/pre-commit`을 설치한다.
  - UTF-8/`py -3.11` guard 규약을 따른다.
- 신규: `workspace-template/tests/test_secret_scan.py`
  - tmp git repo에서 staged 상태를 재현해 차단/통과/예외/한글 무오탐/추가 패턴을 검증한다.

## 3. DoD 자가 점검 (task의 DoD를 전부 나열, 항목별 ✅ / ❌ / 부분 + 한 줄 근거)

- ✅ 4패턴 검출/통과 테스트
  - `test_secret_scan.py`가 LIG_API_KEY, Bearer, generic key/token/password, generic internal hostname을 검증한다.
- ✅ 회사 hostname이 스크립트/테스트에 등장하지 않음
  - 실제 회사 hostname은 넣지 않았고 테스트는 generic `gateway.local`만 사용한다.
- ✅ install_hooks.bat manual smoke
  - temp git repo에서 `install_hooks.bat` 실행 후 `.git/hooks/pre-commit` 생성과 scanner 호출 라인을 확인했다.
- ✅ 기존 checks 무손상
  - 신규 `test_secret_scan.py` 포함 전체 16개 테스트 파일, 총 440 checks가 통과했다.

## 4. 테스트 증거 (실행 명령 + 출력 마지막 줄 원문 붙여넣기 — 회귀 9개 + 신규 전부)

```text
py -3.11 tests\test_secret_scan.py
→ ALL 9 CHECKS PASSED (secret scan)
```

```text
cmd /c call <temp>\scripts\install_hooks.bat
→ #!/bin/sh
→ py -3.11 "scripts/precommit_scan.py"
```

```text
py -3.11 tests\test_capability_bench.py
→ ALL 155 CHECKS PASSED (capability benchmark)
```

```text
py -3.11 tests\test_agent_cli.py
→ ALL 15 CHECKS PASSED (agent CLI/BAT smoke)
```

```text
py -3.11 tests\test_agent_e2e.py
→ ALL 17 CHECKS PASSED (agent E2E)
```

```text
py -3.11 tests\test_approval_audit.py
→ ALL 18 CHECKS PASSED (approval + audit)
```

```text
py -3.11 tests\test_browser_adapter.py
→ SKIP Chrome CDP live checks - skipped, not failed; ALL 10 STATIC CHECKS PASSED (browser adapter)
```

```text
py -3.11 tests\test_encoding_paths.py
→ 11 checks passed
```

```text
py -3.11 tests\test_lig_providers.py
→ 24 checks passed
```

```text
py -3.11 tests\test_lig_runtime.py
→ 18 checks passed
```

```text
py -3.11 tests\test_probes.py
→ ALL 9 CHECKS PASSED (probes)
```

```text
py -3.11 tests\test_resume.py
→ 9 checks passed
```

```text
py -3.11 tests\test_schedule_store.py
→ ALL 53 CHECKS PASSED (schedule store)
```

```text
py -3.11 tests\test_tool_dispatch.py
→ ALL 23 CHECKS PASSED (tool dispatch)
```

```text
py -3.11 tests\test_toolcall_parser.py
→ 12 checks passed
```

```text
py -3.11 tests\test_work_command.py
→ ALL 19 CHECKS PASSED (work command)
```

```text
py -3.11 tests\test_ws_min.py
→ ALL 38 CHECKS PASSED (minimal websocket client)
```

## 5. 계획과 다르게 한 것 (deviation — 없으면 "없음". 사소한 것도 전부)

없음

## 6. 하지 않은 것 / pending (상태 어휘로)

없음

## 7. 리뷰어에게 질문 / 결정 요청 (없으면 "없음")

없음

## 8. 다음 작업 제안 (STATUS 기준 다음 READY 확인 결과)

Codex 담당 READY 작업은 더 이상 없다. 남은 READY는 P00-01(HUMAN), P00-02(fable), P10-01(FABLE-ONLY)다.
