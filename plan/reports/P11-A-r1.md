# 작업 보고서: P11-A (r1)

| 항목 | 값 |
|------|-----|
| 작업 | P11-A lig_runtime native function calling(tools) 경로 |
| 수행자 | Codex GPT-5 |
| 날짜 | 2026-07-03 |
| 시작 HEAD | 084366c |
| 결과 커밋 | 4671ff6, 4d58d48 |
| 라운드 | r1 |

## 1. 결과 요약 (3줄 이내, 상태 어휘 사용)

implemented: native tool-call 왕복 메시지에 자체 발급 id와 tool_call_id 대응을 추가했다.
locally validated with mock: mock gateway가 id="N/A"를 반환해도 제품 코드는 call_* id를 자체 발급하고, 최종 tool 메시지와 대응한다.
locally validated: call_llm 결과와 runtime-last.json에 tool_call_mode(native|text_fallback|none)가 기록된다.

## 1.5 사용자 체감 변화 (선택 — 이 작업으로 사용자가 실제로 할 수 있게 된 것 한 문단)

회사 gateway가 실측에서 반환한 id="N/A" 형태를 그대로 받아도, agent loop가 OpenAI-style assistant.tool_calls[].id와 role:"tool".tool_call_id를 안정적으로 맞춰 보낸다. 사용자는 native function calling 경로에서 파일 읽기/쓰기 같은 도구 왕복이 더 실제 회사 gateway 형식에 가까운 방식으로 동작하는 것을 기대할 수 있다.

## 2. 변경 파일과 이유 (파일별 1~2줄, 신규/수정 구분)

- 수정: `workspace-template/agent_ops/toolcall_parser.py` — structured tool_calls의 id를 보존하고, id="N/A"는 빈 문자열로 정규화했다.
- 수정: `workspace-template/agent_ops/tool_dispatch.py` — agent loop가 tool call마다 call_<turn>_<index> id를 자체 발급하고 tool 메시지의 tool_call_id로 되돌려준다.
- 수정: `workspace-template/agent_ops/lig_runtime.py` — parse_status 기반 tool_call_mode를 result와 runtime-last.json에 포함했다.
- 수정: `workspace-template/agent_ops/mock_transport.py` — mock tool 응답을 회사 실측 gateway 형태(id="N/A", finish_reason="tool_calls")로 맞췄다.
- 수정: `workspace-template/tests/test_toolcall_parser.py` — id 보존/N/A 정규화/text repaired id="" checks를 추가했다.
- 수정: `workspace-template/tests/test_tool_dispatch.py` — run_agent_loop payload를 캡처해 assistant id와 tool_call_id 대응을 검증했다.
- 수정: `workspace-template/tests/test_lig_runtime.py` — native/text_fallback/none tool_call_mode와 진단 필드 포함을 검증했다.

## 3. DoD 자가 점검 (task의 DoD를 전부 나열, 항목별 ✅ / ❌ / 부분 + 한 줄 근거)

- ✅ mock 전체 플로우가 실증 형식(id ↔ tool_call_id 대응)으로 돈다 — `test_tool_dispatch.py`의 `assistant tool_calls carry self-issued id`, `tool msgs tool_call_id matches assistant ids`.
- ✅ id는 항상 자체 발급, 메시지 어디에도 "N/A"가 등장하지 않음 — mock_transport는 id="N/A"를 반환하지만 dispatch payload capture가 assistant id != "N/A"를 검증한다.
- ✅ call_llm 결과·runtime-last.json에 tool_call_mode(native|text_fallback|none) 기록 — `test_lig_runtime.py`의 native/text_fallback/none checks와 `diag exists w/ fields`.
- ✅ 텍스트 복구(fallback) 경로 무손상 — `test_toolcall_parser.py` repaired checks 및 `test_lig_runtime.py`의 fenced repaired/text_fallback checks 통과.
- ✅ 기존 checks 무손상 — PROTOCOL 전체 회귀 10개 파일 모두 exit 0.

## 4. 테스트 증거 (실행 명령 + 출력 마지막 줄 원문 붙여넣기 — 회귀 9개 + 신규 전부)

```text
cd workspace-template

py -3.11 tests\test_capability_bench.py
→ ALL 156 CHECKS PASSED (capability benchmark)

py -3.11 tests\test_toolcall_parser.py
→ 15 checks passed

py -3.11 tests\test_tool_dispatch.py
→ ALL 26 CHECKS PASSED (tool dispatch)

py -3.11 tests\test_lig_runtime.py
→ 23 checks passed

py -3.11 tests\test_agent_e2e.py
→ ALL 17 CHECKS PASSED (agent E2E)

py -3.11 tests\test_agent_cli.py
→ ALL 15 CHECKS PASSED (agent CLI/BAT smoke)

py -3.11 tests\test_lig_providers.py
→ 24 checks passed

py -3.11 tests\test_resume.py
→ 9 checks passed

py -3.11 tests\test_encoding_paths.py
→ 11 checks passed

py -3.11 tests\test_probes.py
→ ALL 13 CHECKS PASSED (probes)
```

## 5. 계획과 다르게 한 것 (deviation — 없으면 "없음". 사소한 것도 전부)

- `tests/test_toolcall_parser.py`의 기존 structured exact expected 값에 `"id": "1"`을 추가했다. task가 허용한 "id 키 추가 기대값 갱신" 범위다.
- `test_tool_dispatch.py`의 캡처 테스트는 task 예시의 `tmp_root/tmp_diag` 대신 파일 내 기존 격리 변수 `ws/diag`를 재사용했다.

## 6. 하지 않은 것 / pending (상태 어휘로)

- company validation pending: 실제 회사 gateway 반복 검증과 성공률 수치화는 P11-01/P00-03 범위다.
- app validation pending 없음 — 이 작업은 외부 앱 어댑터 available 전환을 포함하지 않는다.

## 7. 리뷰어에게 질문 / 결정 요청 (없으면 "없음")

없음

## 8. 다음 작업 제안 (STATUS 기준 다음 READY 확인 결과)

현재 STATUS 기준 다음 Codex READY는 P14-02(schedule CLI + capability 등록)다. 다만 P11-A는 P11-01의 선행이므로 리뷰 승인 후 P11-01 BLOCKED 해제 판단이 필요하다.
