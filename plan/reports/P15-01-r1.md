# 작업 보고서: P15-01 (r1)

| 항목 | 값 |
|------|-----|
| 작업 | P15-01 Office 2016 호환 quality 규칙 |
| 수행자 | Codex GPT-5 |
| 날짜 | 2026-07-03 |
| 시작 HEAD | 8baf6ca |
| 결과 커밋 | 8c35283, 73e59b3 |
| 라운드 | r1 |

## 1. 결과 요약 (3줄 이내, 상태 어휘 사용)

implemented: Office 2016 부재 함수 21종을 `_OFFICE2016_BANNED`로 상수화하고 vba_macro quality 규칙에 추가했다.
implemented: VBA scaffold 헤더/주의 문구에 `대상: Office 2016 호환`과 대체 가이드를 넣었다.
locally validated: XLOOKUP/TEXTJOIN/LET negative 테스트와 전체 회귀가 통과했다.

## 1.5 사용자 체감 변화

이제 생성된 Excel/Office 매크로가 최신 Office 함수에 기대면 품질 검사에서 바로 걸린다. 회사 Office 2016에서 열었을 때 XLOOKUP 같은 함수 때문에 깨지는 일을 미리 막고, 생성물에도 2016 기준으로 작성해야 한다는 안내가 보인다.

## 2. 변경 파일과 이유 (파일별 1~2줄, 신규/수정 구분)

- 수정: `workspace-template/agent_ops/artifact_quality.py`
  - Office 2016 금지 함수 21종과 `office2016_compat` 규칙을 추가했다.
  - 검출은 `NAME(` 또는 `WorksheetFunction.NAME(` 형태로 제한해 단순 언급 오탐을 줄였다.
- 수정: `workspace-template/agent_ops/artifact_generators.py`
  - VBA host note에 Office 2016 대상 문구와 VLOOKUP/INDEX+MATCH/중첩 IF 대체 가이드를 추가했다.
- 수정: `workspace-template/tests/test_capability_bench.py`
  - 금지 목록 개수, XLOOKUP/TEXTJOIN/LET 검출, 단순 언급 비검출, 헤더 문구를 검증했다.

## 3. DoD 자가 점검 (task의 DoD를 전부 나열, 항목별 ✅ / ❌ / 부분 + 한 줄 근거)

- ✅ 금지 목록 21종 상수화 + 검출 테스트 (최소 XLOOKUP/TEXTJOIN/LET 3종 negative)
  - `_OFFICE2016_BANNED` 21종과 benchmark의 3종 negative checks가 검증한다.
- ✅ 기존 scaffold 오탐 0 (전체 bench 통과)
  - `test_capability_bench.py` 전체 151 checks가 통과했다.
- ✅ 매크로 헤더에 2016 대상 명시
  - `macro header states Office 2016 target` check가 검증한다.
- ✅ 기존 checks 무손상
  - 전체 15개 테스트 파일, 총 427 checks가 통과했다.

## 4. 테스트 증거 (실행 명령 + 출력 마지막 줄 원문 붙여넣기 — 회귀 9개 + 신규 전부)

```text
py -3.11 tests\test_capability_bench.py
→ ALL 151 CHECKS PASSED (capability benchmark)
```

```text
py -3.11 tests\test_agent_cli.py
→ ALL 15 CHECKS PASSED (agent CLI/BAT smoke)
```

```text
py -3.11 tests\test_agent_e2e.py
→ ALL 17 CHECKS PASSED (agent E2E)
```

```text
py -3.11 tests\test_approval_audit.py
→ ALL 18 CHECKS PASSED (approval + audit)
```

```text
py -3.11 tests\test_browser_adapter.py
→ SKIP Chrome CDP live checks - skipped, not failed; ALL 10 STATIC CHECKS PASSED (browser adapter)
```

```text
py -3.11 tests\test_encoding_paths.py
→ 11 checks passed
```

```text
py -3.11 tests\test_lig_providers.py
→ 24 checks passed
```

```text
py -3.11 tests\test_lig_runtime.py
→ 18 checks passed
```

```text
py -3.11 tests\test_probes.py
→ ALL 9 CHECKS PASSED (probes)
```

```text
py -3.11 tests\test_resume.py
→ 9 checks passed
```

```text
py -3.11 tests\test_schedule_store.py
→ ALL 53 CHECKS PASSED (schedule store)
```

```text
py -3.11 tests\test_tool_dispatch.py
→ ALL 23 CHECKS PASSED (tool dispatch)
```

```text
py -3.11 tests\test_toolcall_parser.py
→ 12 checks passed
```

```text
py -3.11 tests\test_work_command.py
→ ALL 19 CHECKS PASSED (work command)
```

```text
py -3.11 tests\test_ws_min.py
→ ALL 38 CHECKS PASSED (minimal websocket client)
```

## 5. 계획과 다르게 한 것 (deviation — 없으면 "없음". 사소한 것도 전부)

없음

## 6. 하지 않은 것 / pending (상태 어휘로)

- app validation pending: 실제 Office 2016 실행 검증과 adapter available 전환은 P15-02 이후 범위다.

## 7. 리뷰어에게 질문 / 결정 요청 (없으면 "없음")

없음

## 8. 다음 작업 제안 (STATUS 기준 다음 READY 확인 결과)

P15-02는 아직 BLOCKED다. Codex 기준 다음 READY는 P17-01 `xlsx 입력 ingest (openpyxl optional)`이다.
