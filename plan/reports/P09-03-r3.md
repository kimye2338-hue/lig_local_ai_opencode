# 작업 보고서: P09-03 (r3)

| 항목 | 값 |
|------|-----|
| 작업 | P09-03 real-LLM 스모크 테스트 + doctor + 실측 |
| 수행자 | Codex GPT-5 |
| 날짜 | 2026-07-03 |
| 시작 HEAD | 61b724f |
| 결과 커밋 | 142db2e, cc98977, 63e9f58, acb5709, 이 보고서/STATUS 커밋 |
| 라운드 | r3 (사용자 요청: 로컬 LLM 설치 후 실측) |

## 1. 결과 요약 (3줄 이내, 상태 어휘 사용)

locally validated: Ollama 0.31.1과 `qwen2.5:7b-instruct`를 설치해 local OpenAI-compatible real smoke 3개 시나리오를 실제 통과시켰다.
implemented: `lig_runtime.default_transport()`가 `/v1` base URL을 `/v1/chat/completions`로 정규화하도록 고쳐 Ollama 404를 해소했다.
locally validated: 전체 `tests/test_*.py` 17개 파일이 exit 0이며 real smoke 포함 461 checks가 통과했다.

## 1.5 사용자 체감 변화 (선택 — 이 작업으로 사용자가 실제로 할 수 있게 된 것 한 문단)

이제 이 PC에서 `LIG_PROVIDER_PROFILE=local_openai`, `LIG_LOCAL_BASE_URL=http://127.0.0.1:11434/v1`, `LIG_LOCAL_MODEL=qwen2.5:7b-instruct` 조합으로 agent_ops real mode를 실제 로컬 LLM에 붙여 돌릴 수 있다. smoke는 파일 읽기/요약 저장, 새 파일 생성, unknown-tool 유도 후 `done.md` 생성 복구까지 확인한다.

## 2. 변경 파일과 이유 (파일별 1~2줄, 신규/수정 구분)

- 수정: `workspace-template/agent_ops/lig_runtime.py`
  - OpenAI-compatible base URL을 chat completions endpoint로 정규화하는 `_chat_completions_url()`을 추가했다.
  - `/v1` base URL을 그대로 POST해 Ollama가 404를 내던 문제를 해결했다.
- 수정: `workspace-template/tests/test_lig_runtime.py`
  - `/v1` 및 이미 완성된 `/v1/chat/completions` URL 정규화 check 2개를 추가했다.
- 수정: `workspace-template/tests/test_real_llm_smoke.py`
  - 7B 모델에서 안정적으로 검증되도록 scenario 1의 필수 단어를 명시하고, 결과 파일이 원본 핵심 단어를 반영하는지 확인한다.
  - r2에서 복원한 unknown-tool scenario와 Windows UTF-8 출력 보강은 유지했다.

## 3. DoD 자가 점검 (task의 DoD를 전부 나열, 항목별 ✅ / ❌ / 부분 + 한 줄 근거)

- ✅ 서버 부재 시 SKIP+exit 0 (테스트 출력 원문 증빙)
  - r2에서 서버 off 상태 `SKIP  local llm not running — skipped, not failed`를 확인했고, 현재는 서버가 켜져 real smoke를 수행했다.
- ✅ doctor llm_endpoints 동작 (gateway host 문자열 미출력)
  - local profile one-liner가 `local_base: "127.0.0.1"` 및 `local_reachable.ok: true`만 출력했다.
- ✅ launch/README §5 추가
  - r1에서 추가한 로컬 LLM real mode 검증 절을 유지했다.
- ✅ 실측 수행 여부와 결과를 보고서에 정직 기록 (수행 시: E2E 성공 증빙 / 미수행 시: 사유)
  - `qwen2.5:7b-instruct`로 3개 real E2E 시나리오가 모두 통과했다.
- ✅ 기존 checks 무손상
  - 전체 `tests/test_*.py`가 exit 0이며 461 checks 통과했다.

## 4. 테스트 증거 (실행 명령 + 출력 마지막 줄 원문 붙여넣기 — 회귀 9개 + 신규 전부)

```text
winget install --id Ollama.Ollama --exact --accept-package-agreements --accept-source-agreements --silent
→ 설치 성공
```

```text
ollama list
→ qwen2.5:7b-instruct    845dbda0ea48    4.7 GB
```

```text
py -3.11 -c "import json,sys; sys.path.insert(0,'.'); from agent_ops.doctor import run_doctor; print(json.dumps(run_doctor()['llm_endpoints'], ensure_ascii=False))"
→ {"profile": "local_openai", "ready": true, "secret_file_found": false, "gateway_url_set": false, "api_key_set": false, "routes": {"lig-coding": {"configured": true, "model_set": true, "timeout_set": true}, "lig-chat": {"configured": true, "model_set": true, "timeout_set": true}, "lig-fallback": {"configured": true, "model_set": true, "timeout_set": true}}, "local_base": "127.0.0.1", "local_reachable": {"ok": true, "status": 200}}
```

```text
py -3.11 tests\test_real_llm_smoke.py
→ ALL 12 CHECKS PASSED (real local LLM smoke)
```

```text
py -3.11 tests\test_agent_cli.py
→ ALL 15 CHECKS PASSED (agent CLI/BAT smoke)
```

```text
py -3.11 tests\test_agent_e2e.py
→ ALL 17 CHECKS PASSED (agent E2E)
```

```text
py -3.11 tests\test_approval_audit.py
→ ALL 18 CHECKS PASSED (approval + audit)
```

```text
py -3.11 tests\test_browser_adapter.py
→ ALL 12 CHECKS PASSED (browser adapter)
```

```text
py -3.11 tests\test_capability_bench.py
→ ALL 156 CHECKS PASSED (capability benchmark)
```

```text
py -3.11 tests\test_encoding_paths.py
→ 11 checks passed
```

```text
py -3.11 tests\test_lig_providers.py
→ 24 checks passed
```

```text
py -3.11 tests\test_lig_runtime.py
→ 20 checks passed
```

```text
py -3.11 tests\test_probes.py
→ ALL 13 CHECKS PASSED (probes)
```

```text
py -3.11 tests\test_resume.py
→ 9 checks passed
```

```text
py -3.11 tests\test_schedule_store.py
→ ALL 53 CHECKS PASSED (schedule store)
```

```text
py -3.11 tests\test_secret_scan.py
→ ALL 9 CHECKS PASSED (secret scan)
```

```text
py -3.11 tests\test_tool_dispatch.py
→ ALL 23 CHECKS PASSED (tool dispatch)
```

```text
py -3.11 tests\test_toolcall_parser.py
→ 12 checks passed
```

```text
py -3.11 tests\test_work_command.py
→ ALL 19 CHECKS PASSED (work command)
```

```text
py -3.11 tests\test_ws_min.py
→ ALL 38 CHECKS PASSED (minimal websocket client)
```

## 5. 계획과 다르게 한 것 (deviation — 없으면 "없음". 사소한 것도 전부)

- 사용자 요청에 따라 Ollama를 이 PC에 설치하고 3B/7B 모델을 모두 내려받았다. 최종 실측은 `qwen2.5:7b-instruct`로 통과했다.
- 실측 중 `/v1` base URL을 그대로 POST하는 런타임 버그를 발견해 `_chat_completions_url()`로 수정했다.
- `qwen2.5:3b-instruct`는 파일 생성은 했지만 빈/불충분한 내용을 쓰는 경우가 있어 최종 smoke 모델로 쓰지 않았다.

## 6. 하지 않은 것 / pending (상태 어휘로)

- company validation pending: 회사 gateway real-mode 장기 실측은 회사 환경의 `lig-api.env` 설정 후 별도 검증 범위다.
- local validation pending: `qwen2.5:3b-instruct`는 설치되어 있으나 이 smoke의 통과 모델로 채택하지 않았다.

## 7. 리뷰어에게 질문 / 결정 요청 (없으면 "없음")

없음

## 8. 다음 작업 제안 (STATUS 기준 다음 READY 확인 결과)

P09-03은 r3 AWAITING-REVIEW로 제출한다. 리뷰 승인 후 `P11-A`가 READY 상태이며, 이후 `P11-01` 선행 중 P09-03이 해소된다.
