# 작업 보고서: P19-01 (r1)

| 항목 | 값 |
|------|-----|
| 작업 | P19-01 회사 파일럿 체크리스트/기록 양식 준비 |
| 수행자 | Codex |
| 날짜 | 2026-07-04 |
| 시작 HEAD | 72137a4 |
| 결과 커밋 | 9643f88, 259befd |
| 라운드 | r1 |

## 1. 결과 요약 (3줄 이내, 상태 어휘 사용)

implemented: 회사 파일럿 Day 1 순서 문서와 12종 업무 기록 양식을 추가했다.
implemented: `launch\gateway-smoke.bat`로 `lig-api.env` 준비 확인 후 기존 gateway probe를 실행하게 했다.
locally validated with mock: `lig-api.env` 미설정 상태에서 BAT가 누락 항목을 출력하고 exit 2로 종료됨을 확인했다.

## 1.5 사용자 체감 변화 (선택 — 이 작업으로 사용자가 실제로 할 수 있게 된 것 한 문단)

회사 PC에서 파일럿을 시작할 때 이제 순서와 기록 양식이 갈라지지 않는다. Day 1 문서는 반입부터 gateway smoke, tool-call 실측까지 바로 따라갈 수 있고, 기록 양식은 12종 업무의 명령과 성공 기준을 미리 채워 둬서 현장에서는 성공/실패와 소요 시간만 적으면 된다.

## 2. 변경 파일과 이유 (파일별 1~2줄, 신규/수정 구분)

- 신규: `workspace-template/docs/PILOT_DAY1.md`
  - 반입, setup, doctor, `lig-api.env`, gateway smoke, tool-call 실측, mock 파서 확인 순서를 명령/기대 출력/RUNBOOK 링크와 함께 정리했다.
- 신규: `workspace-template/docs/PILOT_RECORD.md`
  - MASTER_PLAN P19의 12종 업무를 기록 표로 옮기고 각 업무의 복붙 명령과 성공 기준을 미리 기입했다.
- 신규: `workspace-template/launch/gateway-smoke.bat`
  - `lig-api.env` 준비 상태를 기존 `validate_config`로 확인한 뒤 기존 `probe_gateway.py`를 실행해 3 route smoke 결과를 남긴다.

## 3. DoD 자가 점검 (task의 DoD를 전부 나열, 항목별 ✅ / ❌ / 부분 + 한 줄 근거)

- ✅ 두 문서 + 스모크 BAT: `PILOT_DAY1.md`, `PILOT_RECORD.md`, `gateway-smoke.bat` 추가.
- ✅ mock 환경에서 BAT 경로/인코딩 smoke: `lig-api.env` 미설정 상태에서 BAT가 한글 경로 작업공간에서 exit 2와 누락 항목을 출력했다.
- ✅ 12종 전부 실행 명령 사전 기입: `PILOT_RECORD.md` #1~#12 전부 명령과 성공 기준을 포함한다.
- ✅ 기존 checks 무손상: 전체 22개 `test_*.py` 파일이 통과.

## 4. 테스트 증거 (실행 명령 + 출력 마지막 줄 원문 붙여넣기 — 회귀 9개 + 신규 전부)

```text
$env:LIG_API_ENV_FILE = Join-Path $env:TEMP 'opencodelig-missing-lig-api.env'; Remove-Item -LiteralPath $env:LIG_API_ENV_FILE -ErrorAction SilentlyContinue; & workspace-template\launch\gateway-smoke.bat; exit $LASTEXITCODE
→ missing: LIG_API_KEY missing or placeholder
→ [STOP] Fill %USERPROFILE%\OpenCodeLIG_USERDATA\secrets\lig-api.env.
→ exit 2

rg -n "\| [0-9]+ \|" workspace-template/docs/PILOT_RECORD.md
→ 12개 업무 행 확인 (#1~#12)

전체 회귀: Get-ChildItem workspace-template\tests\test_*.py | Sort-Object Name 후 각 파일 py -3.11 실행
→ ALL 22 TEST FILES PASSED

py -3.11 workspace-template\scripts\precommit_scan.py --staged
→ Secret scan passed.
```

## 5. 계획과 다르게 한 것 (deviation — 없으면 "없음". 사소한 것도 전부)

없음.

## 6. 하지 않은 것 / pending (상태 어휘로)

- company validation pending: 실제 회사 gateway 3 route smoke.
- company validation pending: 12종 파일럿 실측과 성공률 기록.

## 7. 리뷰어에게 질문 / 결정 요청 (없으면 "없음")

없음.

## 8. 다음 작업 제안 (STATUS 기준 다음 READY 확인 결과)

P19-01 이후 Codex가 현재 환경(ANY)에서 바로 착수 가능한 READY는 없다. P17-02는 P16-04 리뷰 승인 후 INTERNET 환경에서 열리고, P19-02는 P19-01과 P17-04 이후 COMPANY+HUMAN 실측이다.
