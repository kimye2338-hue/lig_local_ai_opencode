# 작업 보고서: P16-01 (r2)

| 항목 | 값 |
|------|-----|
| 작업 | P16-01 matlab_automation capability + .m 생성기 |
| 수행자 | Codex GPT-5 |
| 날짜 | 2026-07-04 |
| 시작 HEAD | 9a7fee3 |
| 결과 커밋 | bbdccc6, 3ce018e |
| 라운드 | r2 |

## 1. 결과 요약 (3줄 이내, 상태 어휘 사용)

fixed: `.m` 단독 substring 키워드를 제거하고 `.m 스크립트`/`.m 파일`처럼 경계 있는 구절로 좁혔다.
tested: `요약.md로 저장해줘` 문서 요청이 `matlab_automation`/`matlab_script`로 라우팅되지 않는 negative check를 추가했다.
locally validated: capability benchmark 173 checks 및 전체 19개 테스트 파일이 모두 통과했다.

## 1.5 사용자 체감 변화 (선택 — 이 작업으로 사용자가 실제로 할 수 있게 된 것 한 문단)

이제 `.md` 파일명으로 문서를 저장해 달라는 평범한 요청이 MATLAB 작업으로 섞이지 않는다. MATLAB 스크립트 생성은 여전히 “매트랩”, “matlab”, “.m 스크립트”처럼 명확한 신호가 있을 때만 계획된다.

## 2. 변경 파일과 이유 (파일별 1~2줄, 신규/수정 구분)

- 수정: `workspace-template/agent_ops/capabilities.py` — `matlab_automation.keywords`에서 과광범위한 `.m` 단독 키워드를 제거하고 경계 있는 `.m 스크립트`/`.m 파일` 구절로 대체.
- 수정: `workspace-template/tests/test_capability_bench.py` — `요약.md로 저장해줘` 요청이 MATLAB capability/artifact를 만들지 않는 negative routing check 2개 추가.

## 3. DoD 자가 점검 (task의 DoD를 전부 나열, 항목별 ✅ / ❌ / 부분 + 한 줄 근거)

- ✅ 리뷰 필수 수정 반영 — `.m` 단독 키워드 제거, negative routing check 추가.
- ✅ 기존 MATLAB 양성 라우팅 유지 — `시험 데이터 후처리 매트랩 스크립트 만들어줘`가 여전히 `matlab_automation`/`matlab_script`로 라우팅됨.
- ✅ 문서 요청 오라우팅 방지 — `요약.md로 저장해줘`가 `matlab_automation`/`matlab_script`를 포함하지 않음.
- ✅ 기존 checks 무손상 — 전체 19개 테스트 파일 exit 0.

## 4. 테스트 증거 (실행 명령 + 출력 마지막 줄 원문 붙여넣기 — 회귀 9개 + 신규 전부)

```text
py -3.11 workspace-template\tests\test_capability_bench.py
→ ALL 173 CHECKS PASSED (capability benchmark)

workspace-template/tests/test_*.py 전체 19개 순차 실행
→ ALL 19 TEST FILES PASSED
```

개별 전체 회귀 마지막 줄:

```text
test_agent_cli.py → ALL 15 CHECKS PASSED (agent CLI/BAT smoke)
test_agent_e2e.py → ALL 17 CHECKS PASSED (agent E2E)
test_approval_audit.py → ALL 18 CHECKS PASSED (approval + audit)
test_browser_adapter.py → SKIP Chrome CDP live checks - skipped, not failed; ALL 11 STATIC CHECKS PASSED (browser adapter)
test_capability_bench.py → ALL 173 CHECKS PASSED (capability benchmark)
test_capability_floor.py → ALL 11 CHECKS PASSED (capability floor)
test_encoding_paths.py → 11 checks passed
test_lig_providers.py → 24 checks passed
test_lig_runtime.py → 23 checks passed
test_office_adapters.py → ALL 12 CHECKS PASSED (office adapters)
test_probes.py → ALL 13 CHECKS PASSED (probes)
test_real_llm_smoke.py → ALL 12 CHECKS PASSED (real local LLM smoke)
test_resume.py → 9 checks passed
test_schedule_store.py → ALL 68 CHECKS PASSED (schedule store)
test_secret_scan.py → ALL 9 CHECKS PASSED (secret scan)
test_tool_dispatch.py → ALL 27 CHECKS PASSED (tool dispatch)
test_toolcall_parser.py → 15 checks passed
test_work_command.py → ALL 19 CHECKS PASSED (work command)
test_ws_min.py → ALL 38 CHECKS PASSED (minimal websocket client)
```

## 5. 계획과 다르게 한 것 (deviation — 없으면 "없음". 사소한 것도 전부)

없음

## 6. 하지 않은 것 / pending (상태 어휘로)

- app validation pending: MATLAB 2024a에서 `matlab -batch "run('작업.m')"` 실제 실행 검증은 P16-02 이후 범위로 남김.
- optional recommendation not adopted: `그래프 그려`/`플롯`/`후처리`는 이번 재현 결함이 아니므로 유지. 향후 비-MATLAB 오라우팅 신고가 있으면 별도 축소.

## 7. 리뷰어에게 질문 / 결정 요청 (없으면 "없음")

없음

## 8. 다음 작업 제안 (STATUS 기준 다음 READY 확인 결과)

P16-01은 capability/artifact kind hard gate이므로 r2 리뷰를 기다린다. 승인 후 worker-loop는 STATUS의 다음 Codex READY 항목으로 진행 가능하다.
