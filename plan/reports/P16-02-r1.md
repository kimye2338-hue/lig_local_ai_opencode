# 작업 보고서: P16-02 (r1)

| 항목 | 값 |
|------|-----|
| 작업 | P16-02 matlab -batch / AutoCAD accoreconsole 어댑터 |
| 수행자 | Codex |
| 날짜 | 2026-07-04 |
| 시작 HEAD | 8cc7d91 |
| 결과 커밋 | 53f1b91 |
| 라운드 | r1 |

## 1. 결과 요약
- implemented: `matlab_batch` 어댑터가 `MATLAB_EXE`/PATH/표준 경로를 찾아 `matlab -batch "run('<script>')"`를 subprocess로 실행합니다.
- implemented: `autocad_batch` 어댑터가 원본 DWG를 `사본_*.dwg`로 복사한 뒤 `accoreconsole.exe /i <사본.dwg> /s <script.scr>`를 실행하고 UTF-16LE 로그/exit 53을 분류합니다.
- implemented: `autocad_script` artifact kind, `.scr` 생성기, quality rule, registry 등록, fake executable 테스트를 추가했습니다.

## 1.5 사용자 체감 변화
MATLAB/AutoCAD가 없는 PC에서도 작업 계획과 산출물 생성은 깨지지 않고, 앱이 있는 회사 PC에서는 어떤 실행 파일/env를 지정해야 하는지 명확한 안내를 받습니다. AutoCAD는 원본 DWG 대신 사본으로 실행되는 경로가 코드와 테스트로 고정됐습니다.

## 2. 변경 파일과 이유
- 신규 `workspace-template/agent_ops/adapters/matlab_batch.py`
  - MATLAB `-batch` 실행 파일 탐색, timeout, stdout/stderr 수집, audit 기록.
- 신규 `workspace-template/agent_ops/adapters/autocad_batch.py`
  - accoreconsole 탐색, DWG 사본 복사, `/i`/`/s` 실행, UTF-16LE decoding, exit 53 안내.
- 수정 `workspace-template/agent_ops/adapters/__init__.py`
  - `matlab`, `autocad` adapter를 `available=False`와 app validation pending으로 등록.
- 수정 `workspace-template/agent_ops/capabilities.py`
  - `office_cad_automation`에 `autocad_script` artifact kind와 AutoCAD pending 추가.
- 수정 `workspace-template/agent_ops/artifact_generators.py`
  - `gen_autocad_script()`와 `작업.scr` 생성 연결.
- 수정 `workspace-template/agent_ops/artifact_quality.py`
  - `autocad_script` 품질 규칙과 `.scr` suffix 추가.
- 신규 `workspace-template/tests/test_batch_adapters.py`
  - 앱 부재 안내, fake exe 실행, 원본 DWG 불변, UTF-16LE/exit 53, registry/bench 연동 검증.

## 3. DoD 자가 점검
- ✅ 두 어댑터 모두 앱 부재에서 crash 없이 안내 반환: `test_batch_adapters.py`.
- ✅ 원본 DWG 불가침: fake accoreconsole 실행 후 원본 bytes unchanged, `/i`는 `사본_*.dwg`.
- ✅ `.scr` 생성기 + 품질 규칙 bench 통과: `test_batch_adapters.py`, `test_capability_bench.py`.
- ✅ available=False + app validation pending 표기: adapter registry checks.
- ✅ MATLAB Engine API 미도입: stdlib subprocess만 사용.
- ✅ AutoCAD 로그 UTF-16LE decoding + exit 53 분류: fake exe 테스트.

## 4. 테스트 증거

```text
py -3.11 workspace-template\tests\test_batch_adapters.py
→ ALL 24 CHECKS PASSED (batch adapters)

py -3.11 workspace-template\tests\test_capability_bench.py
→ ALL 193 CHECKS PASSED (capability benchmark)

py -3.11 workspace-template\scripts\precommit_scan.py --staged
→ Secret scan passed.

py -3.11 <full test runner over workspace-template\tests\test_*.py>
→ ALL 22 TEST FILES PASSED
```

## 5. 계획과 다르게 한 것
없음.

## 6. 하지 않은 것 / pending
- app validation pending: 회사 MATLAB 2024a에서 실제 `-batch` 실행 검증.
- app validation pending: 회사 AutoCAD 2019 `accoreconsole.exe`에서 실제 DWG + SCR 실행 검증.
- adapter `available=True` 전환은 하지 않았습니다.

## 7. 리뷰어에게 질문 / 결정 요청
`autocad_script` artifact kind 추가는 worker-loop hard gate에 해당하므로, APPROVED 전 다음 READY로 진행하지 않습니다.

## 8. 다음 작업 제안
리뷰 승인 후 STATUS 기준 다음 Codex READY는 `P16-03` 또는 Fable 권장 순서의 `P18-02`입니다.
