# 작업 보고서: P16-03 (r2)

| 항목 | 값 |
|------|-----|
| 작업 | P16-03 simulation_automation (Fluent journal) + fluent_batch |
| 수행자 | Codex |
| 날짜 | 2026-07-04 |
| 시작 HEAD | f406c1b |
| 결과 커밋 | 3d9d471, a5e84e1 |
| 라운드 | r2 (plan/reviews/P16-03-r1.md 필수 수정 반영) |

## 1. 결과 요약 (3줄 이내, 상태 어휘 사용)

implemented: `simulation_automation` 키워드에서 bare `해석`/`journal`을 제거하고 도메인어(`cfd`, `fea` 포함)만 남겼다.
locally validated: 일상어 negative corpus 10문장 메타 체크를 추가해 specialized capability 누출을 차단했고 capability bench 213 checks가 통과했다.
locally validated: batch adapters 37 checks, 전체 22개 테스트 파일, staged secret scan이 통과했다.

## 1.5 사용자 체감 변화 (선택 — 이 작업으로 사용자가 실제로 할 수 있게 된 것 한 문단)

이제 "이 데이터 해석해줘", "journal 정리해줘" 같은 평범한 요청이 갑자기 Fluent journal/ANSYS script를 만드는 쪽으로 새지 않는다. ANSYS/Fluent 요청은 `플루언트`, `ansys`, `메카니컬`, `cfd`, `fea` 같은 도메인 신호가 있을 때만 잡히고, 향후 비슷한 키워드 실수는 negative corpus가 먼저 막아준다.

## 2. 변경 파일과 이유 (파일별 1~2줄, 신규/수정 구분)

- 수정: `workspace-template/agent_ops/capabilities.py`
  - `simulation_automation.keywords`에서 bare `해석`/`journal`을 제거하고 검증된 도메인 키워드 목록으로 교체했다.
- 수정: `workspace-template/tests/test_capability_bench.py`
  - `NEGATIVE_CORPUS` 10문장을 추가해 일상 요청이 `file_ops`/`document_generation` 밖의 specialized capability로 새지 않는지 검증한다.

## 3. DoD 자가 점검 (task의 DoD를 전부 나열, 항목별 ✅ / ❌ / 부분 + 한 줄 근거)

- ✅ journal/script 생성+품질 bench 통과 (입력 반영 포함): r1 구현은 유지했고 `test_capability_bench.py` 213 checks 통과.
- ✅ "공학 판단은 사용자 책임" 경고가 산출물에 포함 (quality 규칙으로 강제): r1 quality 규칙 유지, capability bench의 Fluent/ANSYS checks 통과.
- ✅ fluent_batch는 부재 시 안내 반환, available=False: `test_batch_adapters.py` 37 checks 통과.
- ✅ 기존 checks 무손상: 전체 22개 `test_*.py` 파일이 통과.
- ✅ 리뷰 필수 수정: bare `해석`/`journal` 제거 및 everyday negative corpus 추가, targeted bench에서 10문장 모두 통과.

## 4. 테스트 증거 (실행 명령 + 출력 마지막 줄 원문 붙여넣기 — 회귀 9개 + 신규 전부)

```text
py -3.11 workspace-template\tests\test_capability_bench.py
→ ALL 213 CHECKS PASSED (capability benchmark)

py -3.11 workspace-template\tests\test_batch_adapters.py
→ ALL 37 CHECKS PASSED (batch adapters)

py -3.11 workspace-template\scripts\precommit_scan.py --staged
→ Secret scan passed.

전체 회귀: Get-ChildItem workspace-template\tests\test_*.py | Sort-Object Name 후 각 파일 py -3.11 실행
→ ALL 22 TEST FILES PASSED
```

## 5. 계획과 다르게 한 것 (deviation — 없으면 "없음". 사소한 것도 전부)

없음.

## 6. 하지 않은 것 / pending (상태 어휘로)

- app validation pending: ANSYS Fluent 2024R1 실제 `fluent 3ddp -g -i 작업.jou -t<코어수>` 실행 검증.
- app validation pending: Mechanical/SpaceClaim GUI 스크립팅 콘솔에서 `mechanical_script.py` 실행 검증.
- company validation pending: 실제 회사 case/project 파일 기준 해석 절차 검증과 결과 확인.

## 7. 리뷰어에게 질문 / 결정 요청 (없으면 "없음")

없음.

## 8. 다음 작업 제안 (STATUS 기준 다음 READY 확인 결과)

P16-03은 artifact kind 추가 hard gate였고 r2는 리뷰 필수 수정 반영이므로 Fable APPROVED 전 auto-advance 중단. 승인 후 STATUS 기준 다음 READY는 P16-04, P18-02, P19-01이다.
