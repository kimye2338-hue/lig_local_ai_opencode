# 작업 보고서: P17-03 (r1) — Fable 직접 구현

| 항목 | 값 |
|------|-----|
| 작업 | P17-03 반입 번들 build + setup.bat + 체크리스트 |
| 수행자 | **Fable (Codex 한도초과 — 사용자 지시로 직접)** |
| 날짜 | 2026-07-04 |
| 라운드 | r1 |

## 1. 결과 요약
- implemented: `release/build_bundle.py`(stdlib) — 소스(workspace-template/plan/docs) +
  release/prefetch/* → `OpenCodeLIG_BUNDLE_<날짜>.zip` + 내부 `MANIFEST_SHA256.txt`(전
  파일 해시). secret 파일(lig-api.env/secrets/·*.env) 반입 거부.
- implemented: `release/setup.bat` — 오프라인 설치(py3.11 확인 → `pip --no-index` 번들
  wheel → workspace 배치 → USERDATA 생성 → doctor → 요약). 각 단계 실패 시 원인+다음 행동.
- implemented: `docs/BRING_IN_CHECKLIST.md` — 반입 전/반입/설치/확인/lig-api.env 전 과정.
- tested: `test_release_manifest.py`가 build_bundle을 tmp로 실행해 zip 구조·매니페스트·
  secret 미반입 검증.

## 2. 변경 파일
- 신규 `release/build_bundle.py`, `release/setup.bat`, `workspace-template/docs/BRING_IN_CHECKLIST.md`.
- 수정 `workspace-template/tests/test_release_manifest.py` — `_check_bundle_build()` 추가
  (항상 실행). 
- 수정 `release/.gitignore` — `dist/` 추가(빌드 산출 zip 미커밋).

## 3. DoD 자가 점검
- ✅ zip 생성+매니페스트 검증 테스트 통과: `test_release_manifest.py` `ALL 62 CHECKS
  PASSED`. 실측 — 295 소스 파일 zip + MANIFEST_SHA256.txt, `plan/STATUS.md` 포함,
  lig-api.env 누출 없음, 매니페스트 해시 라인 수 = 아카이브 파일 수.
- ✅ setup.bat 각 단계 실패 처리: py3.11 없음 `[STOP] exit 9`, wheel 실패 `[WARN]`+로그,
  workspace 복사 실패 `[STOP] exit 3`, doctor 비정상 `[WARN]`+진단경로. `--no-index` 강제,
  `-ExecutionPolicy Bypass` 미사용.
- ✅ 체크리스트 문서화: BRING_IN_CHECKLIST.md A~E + 금지 요약.
- ✅ 기존 checks 무손상: 23개 중 20 exit 0(Windows 전용 3개만 RED).

## 4. 테스트 증거
```text
release/build_bundle.py --date 20260704
→ built …/OpenCodeLIG_BUNDLE_20260704.zip (0.5 MB, 295 files + MANIFEST_SHA256.txt)
  (source-only — prefetch 비어있음, NOTE 출력)

tests/test_release_manifest.py
→ ALL 62 CHECKS PASSED (release manifest)

전체 회귀 23개 중 20 exit 0 / precommit_scan → Secret scan passed / dist·zip 미추적
```

## 5. deviation
- 구현자 Fable(Codex 한도초과). setup.bat은 리눅스 리뷰 환경에서 실행 불가(Windows
  전용) — 로직은 코드 리뷰 가능 수준으로 명확히 작성, 실행 검증은 회사/Windows 필요.
- build_bundle의 secret 스캐너를 정밀화(초기 `_secret` 마커가 test_secret_scan.py 오탐 →
  파일명 `lig-api.env`/`/secrets/`/`.env` 정밀 매칭으로 교체).

## 6. pending
- **완전 번들은 P17-02 잔여 3종(GitHub 바이너리) resolved 후**: 현재 source-only zip은
  정상 동작하나 llama.cpp/whisper.cpp/ffmpeg는 미포함. 집/개발 PC에서 3종 채운 뒤
  build_bundle 재실행하면 자동 포함(코드 변경 불필요).
- setup.bat live 실행 검증(Windows) — 회사 반입 시 확인(BRING_IN_CHECKLIST C단계).
