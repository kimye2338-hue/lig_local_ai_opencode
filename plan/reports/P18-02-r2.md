# 작업 보고서: P18-02 (r2) — Fable 직접 수정

| 항목 | 값 |
|------|-----|
| 작업 | P18-02 RUNBOOK + audit 순환 + doctor 운영 섹션 (r1 필수 수정 반영) |
| 수행자 | **Fable (사용자 지시로 Codex 대신 직접 구현)** |
| 날짜 | 2026-07-04 |
| 라운드 | r2 |

## 1. 결과 요약
- fixed: doctor `operations.runbook`을 데이터 루트가 아닌 **코드 루트**
  (`Path(__file__).resolve().parents[1]/docs/RUNBOOK.md`) 기준으로 검사하도록 변경.
- tested: `test_approval_audit.py`에 `AGENTOPS_ROOT`를 임시 데이터 폴더로 둔 상태에서
  `operations.runbook is True` 회귀 check 추가.

## 2. 변경 파일
- 수정 `workspace-template/agent_ops/doctor.py` — `operations_summary()`의 `runbook`
  필드를 코드 루트 기준 존재 검사로(주석으로 이유 명시). 다른 6개 필드(audit=LIG_AUDIT_DIR,
  reports=RESULTS, schedule=store)는 데이터 소스라 무변경.
- 수정 `workspace-template/tests/test_approval_audit.py` — relocated data root에서 shipped
  RUNBOOK을 True로 보고하는지 회귀 check 추가.

## 3. DoD (reviews/P18-02-r1.md 필수 수정)
- ✅ 필수 1: runbook 코드 루트 기준. 실측 — `AGENTOPS_ROOT=<데이터폴더>`에서 `runbook: True`
  (이전 false 오보고 해소).
- ✅ 회귀 check 추가: `operations reports shipped runbook even with relocated data root`.
- ✅ 기존 checks 무손상: `test_approval_audit.py` ALL 21 CHECKS PASSED, 전체 22개 중
  19개 exit 0(Windows 전용 3개만 RED, diff 무접촉).

## 4. 테스트 증거
```text
py -3.11 tests/test_approval_audit.py
→ ALL 21 CHECKS PASSED (approval + audit)

AGENTOPS_ROOT=<tmp> operations_summary()['runbook']
→ True   (이전: False 오보고)

전체 회귀: 22개 중 19개 exit 0 (agent_cli/encoding_paths/probes = Windows 전용)
```

## 5. 계획과 다르게 한 것
- 구현자가 Codex가 아닌 Fable(사용자 지시). r1에서 확인된 나머지(audit 회전·operations
  6필드·RUNBOOK 7행)는 무변경.

## 6. pending
- 없음. (RUNBOOK/operations의 Windows 콘솔 doctor manual smoke는 워커 r1에서 확인됨.)
