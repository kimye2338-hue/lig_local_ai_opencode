# 작업 보고서: P15-02 (r1)

| 항목 | 값 |
|------|-----|
| 작업 | P15-02 excel_com 어댑터 (사본 정책) |
| 수행자 | Codex GPT-5 |
| 날짜 | 2026-07-03 |
| 시작 HEAD | 7e4e4fd |
| 결과 커밋 | fb65a9c, 0efc386 |
| 라운드 | r1 |

## 1. 결과 요약 (3줄 이내, 상태 어휘 사용)

implemented: `agent_ops.adapters.excel_com.execute(action, options)`를 추가하고 Excel 원본 대신 `사본_<파일명>`만 여는 정책을 구현했다.
implemented: office adapter에 `execute`를 연결하되 `available=False`를 유지해 앱 실측 전 자동 사용을 막았다.
locally validated: pywin32 미설치 환경에서 known API가 안내 오류로 실패하고, VBProject 차단은 `manual_import` fallback을 반환함을 테스트했다.

## 1.5 사용자 체감 변화 (선택 — 이 작업으로 사용자가 실제로 할 수 있게 된 것 한 문단)

Excel 자동화 경로가 이제 원본 파일을 직접 건드리지 않는 사본 기반으로 고정된다. 이 PC처럼 pywin32/Excel COM이 없는 환경에서도 크래시 대신 설치 안내를 내며, VBProject 접근이 막힌 회사 PC에서는 Alt+F11 수동 가져오기 절차로 이어질 수 있다.

## 2. 변경 파일과 이유 (파일별 1~2줄, 신규/수정 구분)

- 신규: `workspace-template/agent_ops/adapters/excel_com.py` — copy-only Excel COM adapter와 `open_copy/read_range/write_range/run_macro_file/save/close` action을 추가.
- 수정: `workspace-template/agent_ops/adapters/__init__.py` — office adapter에 `excel_com.execute`를 연결하되 `available=False` 유지.
- 신규: `workspace-template/tests/test_office_adapters.py` — optional import, unknown action, pywin32 부재, original-open API 부재, VBProject fallback, audit 기록을 검증.

## 3. DoD 자가 점검 (task의 DoD를 전부 나열, 항목별 ✅ / ❌ / 부분 + 한 줄 근거)

- ✅ pywin32 absence: 모든 known API가 설치 안내로 실패하고 crash 0 — `test_office_adapters.py` 5개 action absence checks.
- ✅ copy enforced: 공개 action은 `open_copy`뿐이고 `open`/`open_original` 없음 — action set check.
- ✅ VBProject fallback: 접근 실패 시 `fallback=manual_import`와 Alt+F11 guide 반환 — fake COM session으로 검증.
- ✅ available false: office adapter는 execute만 연결하고 `available=False` 유지 — adapter registry check.
- ✅ 기존 checks intact — 전체 19개 테스트 파일 exit 0.

## 4. 테스트 증거 (실행 명령 + 출력 마지막 줄 원문 붙여넣기 — 회귀 9개 + 신규 전부)

```text
cd workspace-template

py -3.11 tests\test_office_adapters.py
→ SKIP Excel COM live checks - skipped, not failed; ALL 14 STATIC CHECKS PASSED (office adapters)

py -3.11 tests\test_schedule_store.py
→ ALL 60 CHECKS PASSED (schedule store)

py -3.11 tests\test_capability_bench.py
→ ALL 158 CHECKS PASSED (capability benchmark)

py -3.11 tests\test_capability_floor.py
→ ALL 10 CHECKS PASSED (capability floor)

py -3.11 tests\test_real_llm_smoke.py
→ ALL 12 CHECKS PASSED (real local LLM smoke)

py -3.11 tests\test_toolcall_parser.py
→ 15 checks passed

py -3.11 tests\test_lig_providers.py
→ 24 checks passed

py -3.11 tests\test_lig_runtime.py
→ 23 checks passed

py -3.11 tests\test_resume.py
→ 9 checks passed

py -3.11 tests\test_encoding_paths.py
→ 11 checks passed

py -3.11 tests\test_tool_dispatch.py
→ ALL 26 CHECKS PASSED (tool dispatch)

py -3.11 tests\test_agent_e2e.py
→ ALL 17 CHECKS PASSED (agent E2E)

py -3.11 tests\test_agent_cli.py
→ ALL 15 CHECKS PASSED (agent CLI/BAT smoke)

py -3.11 tests\test_probes.py
→ ALL 13 CHECKS PASSED (probes)

py -3.11 tests\test_approval_audit.py
→ ALL 18 CHECKS PASSED (approval + audit)

py -3.11 tests\test_browser_adapter.py
→ SKIP Chrome CDP live checks - skipped, not failed; ALL 11 STATIC CHECKS PASSED (browser adapter)

py -3.11 tests\test_secret_scan.py
→ ALL 9 CHECKS PASSED (secret scan)

py -3.11 tests\test_work_command.py
→ ALL 19 CHECKS PASSED (work command)

py -3.11 tests\test_ws_min.py
→ ALL 38 CHECKS PASSED (minimal websocket client)
```

## 5. 계획과 다르게 한 것 (deviation — 없으면 "없음". 사소한 것도 전부)

없음

## 6. 하지 않은 것 / pending (상태 어휘로)

- pending: 실제 Excel/pywin32 live 실행은 이 PC에 pywin32가 없어 SKIP. task 계약상 `available=False` 유지.

## 7. 리뷰어에게 질문 / 결정 요청 (없으면 "없음")

없음

## 8. 다음 작업 제안 (STATUS 기준 다음 READY 확인 결과)

STATUS 기준 다음 Codex READY는 P16-01(matlab_automation capability + .m 생성기)와 P18-02(RUNBOOK + audit 순환 + doctor 운영 섹션)다. 다만 worker-loop auto-advance 연속 3개 한도에 도달하므로 P15-02 제출 후 중단한다.
