# 작업 보고서: P16-03 (r1)

| 항목 | 값 |
|------|-----|
| 작업 | P16-03 simulation_automation (Fluent journal) + fluent_batch |
| 수행자 | Codex |
| 날짜 | 2026-07-04 |
| 시작 HEAD | 351706a |
| 결과 커밋 | 02e4dd6, 689b25e |
| 라운드 | r1 |

## 1. 결과 요약 (3줄 이내, 상태 어휘 사용)

implemented: `simulation_automation` capability, `fluent_journal`/`ansys_script` artifact kind, Fluent journal/Mechanical script scaffold, `fluent_batch` adapter를 추가했다.
locally validated: Fluent 부재 안내, fake executable 배치 실행, artifact quality, capability bench, 전체 22개 테스트 파일이 통과했다.
app validation pending: ANSYS Fluent 2024R1/Mechanical/SpaceClaim 실제 실행 검증은 회사 P19 실측으로 남겼고 adapter `available=False`를 유지했다.

## 1.5 사용자 체감 변화 (선택 — 이 작업으로 사용자가 실제로 할 수 있게 된 것 한 문단)

이제 "플루언트 해석 journal 만들어줘" 같은 요청이 일반 문서가 아니라 ANSYS/Fluent용 실행 절차 산출물로 라우팅된다. 사용자는 `작업.jou`에서 case 경로, 반복 수, export 항목을 확인한 뒤 `fluent 3ddp -g -i 작업.jou -t<코어수>` 형태로 회사 Fluent에서 검증할 수 있고, Mechanical/SpaceClaim 쪽은 GUI 콘솔용 scaffold를 별도 파일로 받는다. 단 해석 세팅과 수렴 판단은 자동화 성공으로 포장하지 않고 사용자 책임으로 명시했다.

## 2. 변경 파일과 이유 (파일별 1~2줄, 신규/수정 구분)

- 신규: `workspace-template/agent_ops/adapters/fluent_batch.py`
  - `FLUENT_EXE`, PATH, ANSYS Inc 표준 경로 탐색과 `fluent 3ddp -g -i <journal> -t<cores>` 배치 실행을 추가했다.
- 수정: `workspace-template/agent_ops/adapters/__init__.py`
  - `fluent` adapter를 registry에 `available=False`로 등록하고 `fluent_journal` 소비 관계를 노출했다.
- 수정: `workspace-template/agent_ops/capabilities.py`
  - `simulation_automation` capability와 `fluent_journal`/`ansys_script` planning metadata를 추가했다.
- 수정: `workspace-template/agent_ops/artifact_generators.py`
  - `gen_fluent_journal`, `gen_ansys_script`를 추가하고 `GENERATORS`에 연결했다.
- 수정: `workspace-template/agent_ops/artifact_quality.py`
  - Fluent journal의 read-case/iterate/export/exit/경고/pending 규칙과 ANSYS GUI script 규칙, 확장자 검사를 추가했다.
- 수정: `workspace-template/tests/test_batch_adapters.py`
  - Fluent 부재 안내, env override, fake executable 실행, command shape, 생성물 품질, registry unavailable 검증을 추가했다.
- 수정: `workspace-template/tests/test_capability_bench.py`
  - simulation 라우팅/생성/품질 bench와 validator negative check를 추가했다.

## 3. DoD 자가 점검 (task의 DoD를 전부 나열, 항목별 ✅ / ❌ / 부분 + 한 줄 근거)

- ✅ journal/script 생성+품질 bench 통과 (입력 반영 포함): `test_batch_adapters.py`에서 `fluent_journal`/`ansys_script` quality 통과, `test_capability_bench.py`에서 simulation benchmark 통과.
- ✅ "공학 판단은 사용자 책임" 경고가 산출물에 포함 (quality 규칙으로 강제): `artifact_quality.py`의 `fluent_engineering_warning`, `ansys_engineering_warning` 규칙과 테스트가 확인.
- ✅ fluent_batch는 부재 시 안내 반환, available=False: `test_batch_adapters.py`의 missing executable check와 adapter registry unavailable check 통과.
- ✅ 기존 checks 무손상: 전체 22개 `test_*.py` 파일이 통과.

## 4. 테스트 증거 (실행 명령 + 출력 마지막 줄 원문 붙여넣기 — 회귀 9개 + 신규 전부)

```text
py -3.11 workspace-template\tests\test_batch_adapters.py
→ ALL 37 CHECKS PASSED (batch adapters)

py -3.11 workspace-template\tests\test_capability_bench.py
→ ALL 203 CHECKS PASSED (capability benchmark)

py -3.11 workspace-template\scripts\precommit_scan.py --staged
→ Secret scan passed.

전체 회귀: Get-ChildItem workspace-template\tests\test_*.py | Sort-Object Name 후 각 파일 py -3.11 실행
→ ALL 22 TEST FILES PASSED
```

## 5. 계획과 다르게 한 것 (deviation — 없으면 "없음". 사소한 것도 전부)

없음.

## 6. 하지 않은 것 / pending (상태 어휘로)

- app validation pending: ANSYS Fluent 2024R1 실제 `fluent 3ddp -g -i 작업.jou -t<코어수>` 실행 검증.
- app validation pending: Mechanical/SpaceClaim GUI 스크립팅 콘솔에서 `mechanical_script.py` 실행 검증.
- company validation pending: 실제 회사 case/project 파일 기준 해석 절차 검증과 결과 확인.

## 7. 리뷰어에게 질문 / 결정 요청 (없으면 "없음")

없음.

## 8. 다음 작업 제안 (STATUS 기준 다음 READY 확인 결과)

P16-03은 capability/artifact kind 추가 hard gate이므로 Fable APPROVED 전 auto-advance 중단. 승인 후 STATUS 기준 다음 READY는 P16-04, P18-02, P19-01 순으로 확인된다.
