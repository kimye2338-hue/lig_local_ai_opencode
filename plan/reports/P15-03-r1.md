# 작업 보고서: P15-03 (r1)

| 항목 | 값 |
|------|-----|
| 작업 | P15-03 outlook_com 어댑터 (일정/메일 read) |
| 수행자 | Codex |
| 날짜 | 2026-07-04 |
| 시작 HEAD | 0b0c7b6 |
| 결과 커밋 | 9d6d2b4, f57d85a |
| 라운드 | r1 |

## 1. 결과 요약
- Outlook COM read adapter를 `static reviewed; company validation pending` 상태로 추가했습니다.
- 새 Outlook 인스턴스 기동 없이 `GetActiveObject("Outlook.Application")`만 사용하고, read_calendar/read_inbox는 자식 프로세스 격리로 실행합니다.
- schedule `sync-outlook` CLI와 mock 기반 sync 중복 방지, inbox 분류, dangerous send fail-closed 검증을 추가했습니다.

## 1.5 사용자 체감 변화
Outlook을 이미 켜 둔 회사 PC에서는 향후 일정 읽기와 받은편지함 분류 입력을 같은 agent_ops 흐름에 붙일 수 있습니다. 집/테스트 PC처럼 Outlook 또는 pywin32가 없거나 Outlook이 꺼져 있으면 멈추지 않고 안내 메시지로 빠집니다.

## 2. 변경 파일과 이유
- 신규 `workspace-template/agent_ops/adapters/outlook_com.py`
  - active Outlook 세션 전용 COM read adapter, subprocess timeout, schedule sync, inbox read, dangerous send fail-closed 구현.
- 수정 `workspace-template/agent_ops/adapters/__init__.py`
  - `outlook` adapter를 `available: False`, `company validation pending` 상태로 registry에 등록.
- 수정 `workspace-template/agent_ops/agentops.py`
  - `schedule sync-outlook --days N` CLI 추가. Outlook/pywin32 부재 시 stderr 안내와 exit 2.
- 수정 `workspace-template/agent_ops/schedule_store.py`
  - Outlook sync 결과 보존을 위해 `source="outlook"` 허용.
- 수정 `workspace-template/tests/test_office_adapters.py`
  - Outlook registry, unknown action, absence path, mock sync duplicate, inbox classifier, send dangerous checks 추가.
- 수정 `workspace-template/tests/test_schedule_store.py`
  - `schedule sync-outlook` absence CLI check 추가.

## 3. DoD 자가 점검
- ✅ read 계열 + sync 중복 방지: `test_office_adapters.py`에서 read action registry, mock `sync_to_schedule` unique add/duplicate skip/source outlook 검증.
- ✅ send_mail은 dangerous + 기본 비노출: `send_mail()`이 `risk="dangerous"`로 fail closed, public `ACTIONS`에는 미포함.
- ✅ Outlook 부재 SKIP/안내, 기존 checks 무손상: fake closed Outlook 안내와 `LIG_OUTLOOK_COM_DISABLE=1` CLI exit 2 검증, 전체 20개 테스트 파일 통과.
- ✅ 상태 표기: adapter registry는 `available: False`, pending은 `company validation pending: Outlook 2016 실행 세션에서 일정/메일 read 검증`.

## 4. 테스트 증거

```text
py -3.11 workspace-template\tests\test_office_adapters.py
→ ALL 22 CHECKS PASSED (office adapters)

py -3.11 workspace-template\tests\test_schedule_store.py
→ ALL 69 CHECKS PASSED (schedule store)

py -3.11 workspace-template\tests\test_capability_bench.py
→ ALL 193 CHECKS PASSED (capability benchmark)

py -3.11 -m py_compile workspace-template\agent_ops\adapters\outlook_com.py
→ exit 0

py -3.11 <full test runner over workspace-template\tests\test_*.py>
→ ALL 20 TEST FILES PASSED
```

## 5. 계획과 다르게 한 것
- `schedule_store.SOURCES`에 `outlook`을 추가했습니다. task의 `sync_to_schedule(..., source="outlook")` 요구를 보존하기 위한 필요 변경입니다.
- 실제 Outlook 2016 실측은 하지 않았습니다. task 환경이 ANY이고 회사 실측은 P19로 명시되어 있어 mock/static 검증까지만 수행했습니다.

## 6. 하지 않은 것 / pending
- `company validation pending`: Outlook 2016 실행 세션에서 read_calendar/read_inbox live 검증.
- `company validation pending`: send_mail live execution. 현재 구현은 dangerous 분류와 fail-closed만 제공합니다.
- 메일 body 전문은 audit/diagnostics에 기록하지 않았습니다.

## 7. 리뷰어에게 질문 / 결정 요청
없음.

## 8. 다음 작업 제안
STATUS 기준 다음 Codex READY는 `P15-04`(EXCEL)이며, 현재 환경에서 Excel live 실측이 적합하지 않으면 `P16-02` 또는 `P18-02`로 넘어갈 수 있습니다.
