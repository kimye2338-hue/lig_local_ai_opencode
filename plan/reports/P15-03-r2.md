# 작업 보고서: P15-03 (r2)

| 항목 | 값 |
|------|-----|
| 작업 | P15-03 outlook_com 어댑터 (일정/메일 read) |
| 수행자 | Codex |
| 날짜 | 2026-07-04 |
| 시작 HEAD | b0de754 |
| 결과 커밋 | 6caf553 |
| 라운드 | r2 (plan/reviews/P15-03-r1.md 반영) |

## 1. 결과 요약
- `plan/reviews/P15-03-r1.md` 필수 수정 1건을 반영했습니다.
- Outlook 자식 subprocess가 `AGENTOPS_ROOT` 데이터 루트가 아니라 코드 루트에서 `agent_ops`를 import하도록 `cwd`와 `PYTHONPATH`를 고정했습니다.
- 기존 GetActiveObject 전용, subprocess timeout, sync 중복 방지, send dangerous fail-closed 동작은 유지했습니다.

## 1.5 사용자 체감 변화
설치/테스트처럼 코드 위치와 사용자 데이터 위치가 분리된 환경에서도 `schedule sync-outlook`과 Outlook read adapter가 `"no JSON"`으로 실패하지 않고, Outlook/pywin32 부재 안내를 정상적으로 돌려줍니다.

## 2. 변경 파일과 이유
- 수정 `workspace-template/agent_ops/adapters/outlook_com.py`
  - `CODE_ROOT = Path(__file__).resolve().parents[2]` 추가.
  - `_run_child()`에서 `cwd=str(CODE_ROOT)`와 `PYTHONPATH=CODE_ROOT+...`를 설정.
- 수정 `workspace-template/tests/test_office_adapters.py`
  - `AGENTOPS_ROOT`가 데이터 루트인 테스트 조건에서 자식 subprocess가 코드 루트로 import해야 한다는 회귀 의도 주석 추가.

## 3. DoD 자가 점검
- ✅ r1 필수 수정: 자식 subprocess cwd/PYTHONPATH를 코드 루트로 고정.
- ✅ read 계열 + sync 중복 방지 유지: `test_office_adapters.py` 통과.
- ✅ send_mail dangerous + 기본 비노출 유지: `test_office_adapters.py` 통과.
- ✅ Outlook 부재 안내 + 기존 checks 무손상: `test_office_adapters.py`, `test_schedule_store.py`, full regression 통과.
- ✅ 상태 표기 유지: adapter registry는 r1 그대로 `available: False`, `company validation pending`.

## 4. 테스트 증거

```text
py -3.11 workspace-template\tests\test_office_adapters.py
→ ALL 22 CHECKS PASSED (office adapters)

py -3.11 workspace-template\tests\test_schedule_store.py
→ ALL 69 CHECKS PASSED (schedule store)

py -3.11 workspace-template\tests\test_capability_bench.py
→ ALL 193 CHECKS PASSED (capability benchmark)

py -3.11 <full test runner over workspace-template\tests\test_*.py>
→ ALL 20 TEST FILES PASSED
```

## 5. 계획과 다르게 한 것
없음. 리뷰어가 제시한 `CODE_ROOT` + `PYTHONPATH` 수정을 그대로 적용했습니다.

## 6. 하지 않은 것 / pending
- `company validation pending`: Outlook 2016 실행 세션에서 read_calendar/read_inbox live 검증.
- `company validation pending`: send_mail live execution. 현재 구현은 dangerous 분류와 fail-closed만 제공합니다.

## 7. 리뷰어에게 질문 / 결정 요청
없음.

## 8. 다음 작업 제안
STATUS 기준 다음 Codex READY는 `P15-04`(EXCEL)입니다. 현재 환경에서 Excel live 실측이 적합하지 않으면 `P18-02` 또는 `P16-02`로 진행하는 편이 안전합니다.
