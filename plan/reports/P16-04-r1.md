# 작업 보고서: P16-04 (r1)

| 항목 | 값 |
|------|-----|
| 작업 | P16-04 hwp_com + solidworks_com 어댑터 |
| 수행자 | Codex |
| 날짜 | 2026-07-04 |
| 시작 HEAD | 513641b |
| 결과 커밋 | 80f3a4e, 9bfa76d |
| 라운드 | r1 |

## 1. 결과 요약 (3줄 이내, 상태 어휘 사용)

implemented: HWP Markdown→신규 HWP 변환 어댑터와 SolidWorks 사본 문서 매크로 실행 어댑터를 추가하고 registry execute를 연결했다.
locally validated: 앱/pywin32 부재 안내, HWP 신규 파일 API 표면, SolidWorks 사본 정책과 .bas manual_import 강등을 테스트했다.
app validation pending: HWP 2019, SolidWorks 2022 실제 실행 검증 전이므로 두 adapter 모두 `available=False`를 유지했다.

## 1.5 사용자 체감 변화 (선택 — 이 작업으로 사용자가 실제로 할 수 있게 된 것 한 문단)

이제 agent_ops가 HWP와 SolidWorks 실행 단계도 정직하게 설명할 수 있다. 한글 문서는 원본을 여는 대신 Markdown에서 새 `.hwp` 파일로 저장하는 경로만 열려 있고, SolidWorks 문서는 항상 `사본_...` 파일로 복사한 뒤 다룬다. `.bas` 매크로를 무리하게 성공 처리하지 않고 매크로 편집기 import 안내로 강등하므로, 회사 PC에서 실제 앱 검증 전에도 위험한 자동 저장이나 가짜 성공을 피한다.

## 2. 변경 파일과 이유 (파일별 1~2줄, 신규/수정 구분)

- 신규: `workspace-template/agent_ops/adapters/hwp_com.py`
  - `md_to_hwp` action을 추가했다. pywin32 optional import, HWPFrame COM, RegisterModule 시도, 신규 문서 저장, 정리/감사 기록을 포함한다.
- 신규: `workspace-template/agent_ops/adapters/solidworks_com.py`
  - `run_macro` action을 추가했다. 원본 문서 사본 생성, SolidWorks active/dispatch 연결, `.bas` manual_import 강등, 자동 저장 금지를 구현했다.
- 수정: `workspace-template/agent_ops/adapters/__init__.py`
  - `hwp`/`solidworks` adapter에 execute를 연결하고 `available=False`를 유지했다.
- 수정: `workspace-template/tests/test_office_adapters.py`
  - HWP/SolidWorks 부재 안내, unknown action, action surface, 사본/신규 파일 정책, registry unavailable 검증을 추가했다.

## 3. DoD 자가 점검 (task의 DoD를 전부 나열, 항목별 ✅ / ❌ / 부분 + 한 줄 근거)

- ✅ 앱 부재 crash 0 + 안내 반환: `test_office_adapters.py`에서 HWP pywin32 부재/소스 없음, SolidWorks pywin32 또는 app 접근 실패 안내를 검증했다.
- ✅ SolidWorks 자동 저장 없음 / HWP는 신규 파일만 생성: SolidWorks 결과는 `saved=False` 경로이고 `.bas`는 manual_import로 강등, HWP action surface는 `md_to_hwp` 신규 파일 변환만 노출한다.
- ✅ available=False + app validation pending 유지: `ADAPTERS["hwp"]`, `ADAPTERS["solidworks"]` 모두 unavailable이며 pending 문구 유지.
- ✅ 기존 checks 무손상: 전체 22개 `test_*.py` 파일이 통과.

## 4. 테스트 증거 (실행 명령 + 출력 마지막 줄 원문 붙여넣기 — 회귀 9개 + 신규 전부)

```text
py -3.11 workspace-template\tests\test_office_adapters.py
→ ALL 42 CHECKS PASSED (office adapters)

py -3.11 workspace-template\tests\test_capability_bench.py
→ ALL 213 CHECKS PASSED (capability benchmark)

py -3.11 workspace-template\scripts\precommit_scan.py --staged
→ Secret scan passed.

전체 회귀: Get-ChildItem workspace-template\tests\test_*.py | Sort-Object Name 후 각 파일 py -3.11 실행
→ ALL 22 TEST FILES PASSED
```

## 5. 계획과 다르게 한 것 (deviation — 없으면 "없음". 사소한 것도 전부)

없음.

## 6. 하지 않은 것 / pending (상태 어휘로)

- app validation pending: HWP 2019에서 실제 `md_to_hwp` 저장 검증.
- app validation pending: SolidWorks 2022에서 `.swp`/매크로 실행 검증.
- company validation pending: 회사 표준 HWP/SolidWorks 파일과 보안 설정 기준 검증.

## 7. 리뷰어에게 질문 / 결정 요청 (없으면 "없음")

없음.

## 8. 다음 작업 제안 (STATUS 기준 다음 READY 확인 결과)

P16-04는 `available=True` 전환이나 capability/artifact kind 추가가 없어 hard gate는 아니다. auto-advance 조건 충족 시 STATUS 기준 다음 READY는 P18-02다.
