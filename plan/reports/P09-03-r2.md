# 작업 보고서: P09-03 (r2)

| 항목 | 값 |
|------|-----|
| 작업 | P09-03 real-LLM 스모크 테스트 + doctor + 실측 |
| 수행자 | Codex GPT-5 |
| 날짜 | 2026-07-03 |
| 시작 HEAD | 61b724f |
| 결과 커밋 | 142db2e, cc98977, 이 보고서/STATUS 커밋 |
| 라운드 | r2 (plan/reviews/P09-03-r1.md 필수 수정 반영) |

## 1. 결과 요약 (3줄 이내, 상태 어휘 사용)

implemented: real smoke 시나리오 ③을 `cad_export` unknown-tool 유도 후 `done.md` 생성으로 정상 복구하는 원 요구에 맞게 복원했다.
locally validated: 서버 off 상태 `test_real_llm_smoke.py`가 규약 문구로 SKIP+exit 0을 반환하고, doctor `llm_endpoints` one-liner가 secret-free로 출력됨을 확인했다.
locally validated: 전체 `tests/test_*.py` 17개 파일이 exit 0이며 447 checks 통과, local LLM smoke는 서버 부재로 정상 SKIP이다.

## 1.5 사용자 체감 변화 (선택 — 이 작업으로 사용자가 실제로 할 수 있게 된 것 한 문단)

로컬 LLM이 없는 PC에서도 real smoke 테스트가 Windows 콘솔 인코딩 문제 없이 정상 SKIP으로 끝난다. 로컬 LLM이 켜진 PC에서는 세 번째 시나리오가 실제로 없는 `cad_export` 도구를 유도한 뒤, 모델이 가능한 도구로 복구해 `done.md`를 만드는지 확인하게 되어 task의 복구 경로 의도가 살아났다.

## 2. 변경 파일과 이유 (파일별 1~2줄, 신규/수정 구분)

- 수정: `workspace-template/tests/test_real_llm_smoke.py`
  - 시나리오 ③ task 문구를 `cad_export` unknown-tool 유도 문구로 복원하고 `done.md` 존재 검증을 추가했다.
  - Windows cp949 콘솔에서 SKIP 문구의 em dash 출력이 실패하지 않도록 stdout/stderr UTF-8 reconfigure를 추가했다.
- 수정: `plan/STATUS.md`
  - P09-03 r2 착수 및 제출 이력을 추가하고 상태를 `AWAITING-REVIEW`로 갱신한다.
- 신규: `plan/reports/P09-03-r2.md`
  - r1 필수 수정 반영 내역과 실제 실행 증거를 기록한다.

## 3. DoD 자가 점검 (task의 DoD를 전부 나열, 항목별 ✅ / ❌ / 부분 + 한 줄 근거)

- ✅ 서버 부재 시 SKIP+exit 0 (테스트 출력 원문 증빙)
  - `py -3.11 tests\test_real_llm_smoke.py`가 `SKIP  local llm not running — skipped, not failed`를 출력하고 exit 0으로 종료했다.
- ✅ doctor llm_endpoints 동작 (gateway host 문자열 미출력)
  - doctor one-liner 출력은 profile, presence flag, route configured/model/timeout flag, local reachability status만 포함한다.
- ✅ launch/README §5 추가
  - r1에서 추가된 `workspace-template/launch/README.md` §5를 유지했다.
- ✅ 실측 수행 여부와 결과를 보고서에 정직 기록 (수행 시: E2E 성공 증빙 / 미수행 시: 사유)
  - 현재 PC에서 local LLM endpoint가 응답하지 않아 E2E 실측은 미수행이며 SKIP 경로만 locally validated로 기록했다.
- ✅ 기존 checks 무손상
  - 전체 `tests/test_*.py`가 exit 0이며 447 checks 통과, local LLM smoke는 정상 SKIP이다.

## 4. 테스트 증거 (실행 명령 + 출력 마지막 줄 원문 붙여넣기 — 회귀 9개 + 신규 전부)

```text
py -3.11 tests\test_real_llm_smoke.py
→ SKIP  local llm not running — skipped, not failed
```

```text
py -3.11 -c "import json,sys; sys.path.insert(0,'.'); from agent_ops.doctor import run_doctor; print(json.dumps(run_doctor()['llm_endpoints'], ensure_ascii=False))"
→ {"profile": "company_gateway", "ready": false, "secret_file_found": false, "gateway_url_set": false, "api_key_set": false, "routes": {"lig-coding": {"configured": false, "model_set": true, "timeout_set": true}, "lig-chat": {"configured": false, "model_set": true, "timeout_set": true}, "lig-fallback": {"configured": false, "model_set": true, "timeout_set": true}}, "local_base": "not configured", "local_reachable": {"ok": false, "skipped": "not local_openai profile"}}
```

```text
py -3.11 tests\test_agent_cli.py
→ ALL 15 CHECKS PASSED (agent CLI/BAT smoke)
```

```text
py -3.11 tests\test_agent_e2e.py
→ ALL 17 CHECKS PASSED (agent E2E)
```

```text
py -3.11 tests\test_approval_audit.py
→ ALL 18 CHECKS PASSED (approval + audit)
```

```text
py -3.11 tests\test_browser_adapter.py
→ ALL 12 CHECKS PASSED (browser adapter)
```

```text
py -3.11 tests\test_capability_bench.py
→ ALL 156 CHECKS PASSED (capability benchmark)
```

```text
py -3.11 tests\test_encoding_paths.py
→ 11 checks passed
```

```text
py -3.11 tests\test_lig_providers.py
→ 24 checks passed
```

```text
py -3.11 tests\test_lig_runtime.py
→ 18 checks passed
```

```text
py -3.11 tests\test_probes.py
→ ALL 13 CHECKS PASSED (probes)
```

```text
py -3.11 tests\test_resume.py
→ 9 checks passed
```

```text
py -3.11 tests\test_schedule_store.py
→ ALL 53 CHECKS PASSED (schedule store)
```

```text
py -3.11 tests\test_secret_scan.py
→ ALL 9 CHECKS PASSED (secret scan)
```

```text
py -3.11 tests\test_tool_dispatch.py
→ ALL 23 CHECKS PASSED (tool dispatch)
```

```text
py -3.11 tests\test_toolcall_parser.py
→ 12 checks passed
```

```text
py -3.11 tests\test_work_command.py
→ ALL 19 CHECKS PASSED (work command)
```

```text
py -3.11 tests\test_ws_min.py
→ ALL 38 CHECKS PASSED (minimal websocket client)
```

## 5. 계획과 다르게 한 것 (deviation — 없으면 "없음". 사소한 것도 전부)

- r1 권고대로 `agentops.py agent` 직접 subprocess 호출은 유지했다. BAT 자체는 `test_agent_cli.py`가 검증하고, real smoke는 CLI 엔트리의 real-mode 루프를 직접 검증한다.
- Windows cp949 콘솔에서 SKIP 문구의 em dash가 `UnicodeEncodeError`를 일으켜, test 파일에서 stdout/stderr를 UTF-8로 reconfigure했다. 이는 r1 리뷰 필수 수정 외 추가 보강이다.
- local LLM 서버가 현재 환경에서 실행 중이 아니어서 3개 real E2E 시나리오는 수행하지 않았다. task 환경 태그가 옵션으로 허용한 서버 off SKIP 경로만 검증했다.

## 6. 하지 않은 것 / pending (상태 어휘로)

- locally validated with local LLM pending: Ollama 또는 OpenAI 호환 로컬 서버가 켜진 상태의 3개 real E2E 시나리오 실측은 수행하지 않았다.
- company validation pending: 회사 gateway real-mode 장기 실측은 회사 환경의 `lig-api.env` 설정 후 별도 검증 범위다.

## 7. 리뷰어에게 질문 / 결정 요청 (없으면 "없음")

없음

## 8. 다음 작업 제안 (STATUS 기준 다음 READY 확인 결과)

P09-03은 r2 AWAITING-REVIEW로 제출한다. 리뷰 승인 후 `P11-A`가 READY 상태이며, 이후 `P11-01` 선행 중 하나가 해소된다.
