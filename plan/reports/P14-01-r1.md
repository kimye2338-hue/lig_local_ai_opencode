# 작업 보고서: P14-01 (r1)

| 항목 | 값 |
|------|-----|
| 작업 | P14-01 schedule store + 결정적 날짜 파서 |
| 수행자 | Codex GPT-5 |
| 날짜 | 2026-07-03 |
| 시작 HEAD | e3021a0 |
| 결과 커밋 | 1b334ec, 28a4693 |
| 라운드 | r1 |

## 1. 결과 요약 (3줄 이내, 상태 어휘 사용)

implemented: stdlib-only `schedule_store.py`를 추가해 일정 add/list/mark_done/remove와 `LIG_SCHEDULE_DIR` 테스트 격리를 지원한다.
implemented: "오늘/내일/요일/N일 후/7월 15일/오후 3시" 계열을 LLM 없이 결정적으로 파싱하고, 모호한 입력은 되묻는다.
locally validated: 신규 schedule 테스트와 전체 회귀가 통과했다.

## 1.5 사용자 체감 변화

이제 일정 기능의 바닥이 생겼다. 다음 CLI 작업(P14-02)이 붙으면 사용자는 "금요일 14시 진동시험 보고서 마감" 같은 말을 안전하게 일정으로 저장할 수 있고, 애매한 "언젠가" 같은 입력은 시스템이 임의로 찍지 않고 다시 물어보게 된다.

## 2. 변경 파일과 이유 (파일별 1~2줄, 신규/수정 구분)

- 신규: `workspace-template/agent_ops/schedule_store.py`
  - 일정 JSON 저장소, 1세대 `.bak` 백업, CRUD API, 기간 조회, category 자동 추정을 구현했다.
  - 날짜 파서는 외부 패키지/LLM 없이 `datetime`, 정규식, `weekday()` 정수 계산만 사용한다.
- 신규: `workspace-template/tests/test_schedule_store.py`
  - task가 요구한 파서 표, 모호 입력 되묻기, CRUD, today/week/overdue, `.bak`, 저장 스키마를 고정했다.

## 3. DoD 자가 점검 (task의 DoD를 전부 나열, 항목별 ✅ / ❌ / 부분 + 한 줄 근거)

- ✅ 파서 케이스 표 전부 테스트로 고정 (now 주입, 결정적)
  - `test_schedule_store.py`가 고정 `NOW = 2026-07-03 10:00`으로 모든 요구 케이스를 검증한다.
- ✅ 모호 입력 → 되묻기 반환 (추측 금지 증명 테스트)
  - `ambiguous asks question`, `empty asks question`, `invalid due add asks question` checks가 검증한다.
- ✅ .bak 백업 + 원자적 쓰기
  - `_save()`가 기존 파일을 `.bak`으로 복사한 뒤 `atomic_write_json()`을 사용하고, `backup file exists after updates`가 검증한다.
- ✅ LLM/외부 패키지 사용 없음
  - 모듈은 stdlib + local `core.atomic_write_json`만 사용하고, 테스트가 `dateutil` 미사용을 확인한다.
- ✅ 기존 checks 무손상
  - 신규 `test_schedule_store.py` 포함 전체 15개 테스트 파일, 총 421 checks가 통과했다.

## 4. 테스트 증거 (실행 명령 + 출력 마지막 줄 원문 붙여넣기 — 회귀 9개 + 신규 전부)

```text
py -3.11 tests\test_schedule_store.py
→ ALL 53 CHECKS PASSED (schedule store)
```

```text
py -3.11 tests\test_agent_cli.py
→ ALL 15 CHECKS PASSED (agent CLI/BAT smoke)
```

```text
py -3.11 tests\test_agent_e2e.py
→ ALL 17 CHECKS PASSED (agent E2E)
```

```text
py -3.11 tests\test_approval_audit.py
→ ALL 18 CHECKS PASSED (approval + audit)
```

```text
py -3.11 tests\test_browser_adapter.py
→ SKIP Chrome CDP live checks - skipped, not failed; ALL 10 STATIC CHECKS PASSED (browser adapter)
```

```text
py -3.11 tests\test_capability_bench.py
→ ALL 145 CHECKS PASSED (capability benchmark)
```

```text
py -3.11 tests\test_encoding_paths.py
→ 11 checks passed
```

```text
py -3.11 tests\test_lig_providers.py
→ 24 checks passed
```

```text
py -3.11 tests\test_lig_runtime.py
→ 18 checks passed
```

```text
py -3.11 tests\test_probes.py
→ ALL 9 CHECKS PASSED (probes)
```

```text
py -3.11 tests\test_resume.py
→ 9 checks passed
```

```text
py -3.11 tests\test_tool_dispatch.py
→ ALL 23 CHECKS PASSED (tool dispatch)
```

```text
py -3.11 tests\test_toolcall_parser.py
→ 12 checks passed
```

```text
py -3.11 tests\test_work_command.py
→ ALL 19 CHECKS PASSED (work command)
```

```text
py -3.11 tests\test_ws_min.py
→ ALL 38 CHECKS PASSED (minimal websocket client)
```

## 5. 계획과 다르게 한 것 (deviation — 없으면 "없음". 사소한 것도 전부)

없음

## 6. 하지 않은 것 / pending (상태 어휘로)

- implemented pending CLI wiring: `agentops.py schedule ...`와 capability 등록은 P14-02 범위다.

## 7. 리뷰어에게 질문 / 결정 요청 (없으면 "없음")

없음

## 8. 다음 작업 제안 (STATUS 기준 다음 READY 확인 결과)

P14-02는 아직 BLOCKED다. Codex 기준 다음 READY는 P15-01 `Office 2016 호환 quality 규칙`이다.
