# 작업 보고서: P09-02 (r1)

| 항목 | 값 |
|------|-----|
| 작업 | P09-02 작업 유형→라우트 자동 선택 + 진단 |
| 수행자 | Codex GPT-5 |
| 날짜 | 2026-07-03 |
| 시작 HEAD | e8ea04e |
| 결과 커밋 | eaea54d, 491f830 |
| 라운드 | r1 |

## 1. 결과 요약 (3줄 이내, 상태 어휘 사용)

implemented: capability id 기반 `select_route()`를 추가해 매크로/파일/브라우저/스프레드시트 계열은 `lig-coding`, 문서/발표/메일 계열은 `lig-chat`으로 자동 선택한다.
implemented: `call_llm()` 진단에 `route_selected`, `route_reason`, `profile`을 기록하고, `agentops.py agent` 경로에서 `plan_task()` 분류 결과를 전달한다.
locally validated with mock: mock transport 기반 runtime/CLI 테스트와 전체 회귀가 모두 통과했다.

## 1.5 사용자 체감 변화

사용자가 “매크로 만들어줘” 같은 코드/도구형 작업을 시키면 코딩용 라우트로, “문서 작성해줘” 같은 문서형 작업은 채팅/문서용 라우트로 자동 배정된다. 앞으로 real LLM 연결 시 작업마다 모델 경로를 손으로 고르지 않아도 되고, 진단 파일에서 왜 그 라우트가 선택됐는지도 확인할 수 있다.

## 2. 변경 파일과 이유 (파일별 1~2줄, 신규/수정 구분)

- 수정: `workspace-template/agent_ops/lig_providers.py`
  - capability→route 상수 테이블, `select_route()`, `route_reason()`을 추가했다.
  - P09-01 r2 권고에 따라 알 수 없는 profile warning을 한국어로 바꿨다.
- 수정: `workspace-template/agent_ops/lig_runtime.py`
  - `call_llm(..., capability_ids=...)`를 추가하고, 기존 provider/env default 동작은 보존했다.
  - runtime 진단에 route/profile 필드를 추가했다.
- 수정: `workspace-template/agent_ops/tool_dispatch.py`
  - agent loop가 capability ids를 `call_llm()`으로 전달하게 했다.
- 수정: `workspace-template/agent_ops/agentops.py`
  - `agent` subcommand에서 `plan_task()`로 task를 분류해 route 선택에 사용한다.
- 수정: `workspace-template/tests/test_lig_providers.py`, `workspace-template/tests/test_lig_runtime.py`, `workspace-template/tests/test_agent_cli.py`
  - route mapping, runtime 진단, CLI mock agent route 적용을 검증했다.

## 3. DoD 자가 점검 (task의 DoD를 전부 나열, 항목별 ✅ / ❌ / 부분 + 한 줄 근거)

- ✅ capability→라우트 매핑이 상수 테이블 기반 (신규 capability 추가 시 한 줄)
  - `_ROUTE_CAPABILITY_MAP`과 `select_route()`로 단일 위치에서 결정한다.
- ✅ 진단에 선택 근거 기록, host/key 미포함
  - `test_lig_runtime.py`의 `diag exists w/ fields`, `diag records route`, `no secret in diag` checks가 검증한다.
- ✅ mock 모드 기존 동작/테스트 완전 보존
  - `test_agent_cli.py`와 `test_agent_e2e.py`가 통과했고, CLI mock에서 문서 task→chat route도 검증했다.
- ✅ 기존 checks 무손상
  - 전체 12개 테스트 파일, 총 339 checks가 통과했다.

## 4. 테스트 증거 (실행 명령 + 출력 마지막 줄 원문 붙여넣기 — 회귀 9개 + 신규 전부)

```text
py -3.11 tests\test_agent_cli.py
→ ALL 15 CHECKS PASSED (agent CLI/BAT smoke)
```

```text
py -3.11 tests\test_agent_e2e.py
→ ALL 17 CHECKS PASSED (agent E2E)
```

```text
py -3.11 tests\test_approval_audit.py
→ ALL 18 CHECKS PASSED (approval + audit)
```

```text
py -3.11 tests\test_capability_bench.py
→ ALL 145 CHECKS PASSED (capability benchmark)
```

```text
py -3.11 tests\test_encoding_paths.py
→ 11 checks passed
```

```text
py -3.11 tests\test_lig_providers.py
→ 24 checks passed
```

```text
py -3.11 tests\test_lig_runtime.py
→ 18 checks passed
```

```text
py -3.11 tests\test_probes.py
→ ALL 9 CHECKS PASSED (probes)
```

```text
py -3.11 tests\test_resume.py
→ 9 checks passed
```

```text
py -3.11 tests\test_tool_dispatch.py
→ ALL 23 CHECKS PASSED (tool dispatch)
```

```text
py -3.11 tests\test_toolcall_parser.py
→ 12 checks passed
```

```text
py -3.11 tests\test_ws_min.py
→ ALL 38 CHECKS PASSED (minimal websocket client)
```

## 5. 계획과 다르게 한 것 (deviation — 없으면 "없음". 사소한 것도 전부)

없음

## 6. 하지 않은 것 / pending (상태 어휘로)

- company validation pending: 실제 gateway 호출과 real LLM route 실측은 P09-03 범위다.

## 7. 리뷰어에게 질문 / 결정 요청 (없으면 "없음")

없음

## 8. 다음 작업 제안 (STATUS 기준 다음 READY 확인 결과)

P09-02가 APPROVED되면 Fable이 P09-03을 READY로 전환할 수 있다. 현재 STATUS 기준 다음 Codex READY는 `P12-02 브라우저 CDP 어댑터 + chrome-debug.bat`다.
