# 작업 보고서: P09-01 (r2)

| 항목 | 값 |
|------|-----|
| 작업 | P09-01 LLM provider 프로필/env 완전 오버라이드 |
| 수행자 | Codex GPT-5 |
| 날짜 | 2026-07-03 |
| 시작 HEAD | 965e9fb |
| 결과 커밋 | e27c4ab, 9daf29b |
| 라운드 | r2 (reviews/P09-01-r1.md 필수 수정 반영) |

## 1. 결과 요약 (3줄 이내, 상태 어휘 사용)

implemented: `LIG_PROVIDER_PROFILE`, `LIG_LOCAL_BASE_URL`, `LIG_LOCAL_MODEL`은 이제 `os.environ` 값이 env 파일보다 우선한다.
locally validated: env 파일이 `company_gateway`여도 셸 환경변수로 `local_openai` 전환이 되는 테스트를 추가했다.
locally validated: 전체 `tests/test_*.py` 12개 파일이 모두 exit 0으로 통과했다.

## 1.5 사용자 체감 변화

이제 `set LIG_PROVIDER_PROFILE=local_openai`처럼 CMD/배치에서 환경변수만 설정해도 로컬 LLM 프로필로 바로 전환된다. 즉 `lig-api.env` 파일을 매번 수정하지 않고도 회사 gateway 모드와 집/로컬 OpenAI 호환 서버 모드를 빠르게 바꿔 테스트할 수 있다.

## 2. 변경 파일과 이유 (파일별 1~2줄, 신규/수정 구분)

- 수정: `workspace-template/agent_ops/lig_providers.py`
  - `_with_shell_overrides()`를 추가해 `LIG_PROVIDER_PROFILE`, `LIG_LOCAL_BASE_URL`, `LIG_LOCAL_MODEL`에 한해 `os.environ`이 env 파일 값을 덮어쓰게 했다.
- 수정: `workspace-template/tests/test_lig_providers.py`
  - 테스트 시작 시 관련 env key를 pop해 결정성을 확보했다.
  - env 파일 값보다 셸 환경변수의 local profile 값이 우선되는 check를 추가했다.
  - self-review grep에 걸리던 `sk-` 형태 가짜 문자열을 중립 테스트 문자열로 교체했다.

## 3. DoD 자가 점검 (task의 DoD를 전부 나열, 항목별 ✅ / ❌ / 부분 + 한 줄 근거)

- ✅ 라우트/모델/타임아웃 변경이 env 편집만으로 반영됨을 테스트가 증명
  - `route and model overrides` 및 신규 `shell env overrides file profile` check가 env 기반 반영을 검증한다.
- ✅ local_openai 프로필이 api_key 없이 ready=True
  - `local_openai ready without secret`와 `shell env overrides file profile`이 `local_openai` ready 경로를 검증한다.
- ✅ validate_config 출력에 host/key 원문 미포함
  - `no secret in report`, `validate report omits host strings`, 전체 secret-like grep self-review로 확인했다.
- ✅ env.example에 신규 키 전부 문서화
  - r1에서 추가된 `workspace-template/config/lig-api.env.example` 유지. r2에서 변경 필요 없음.
- ✅ 기존 checks 무손상
  - 신규 check 포함 전체 330 checks가 모두 통과했다.

## 4. 테스트 증거 (실행 명령 + 출력 마지막 줄 원문 붙여넣기 — 회귀 9개 + 신규 전부)

```text
py -3.11 tests\test_agent_cli.py
→ ALL 14 CHECKS PASSED (agent CLI/BAT smoke)
```

```text
py -3.11 tests\test_agent_e2e.py
→ ALL 17 CHECKS PASSED (agent E2E)
```

```text
py -3.11 tests\test_approval_audit.py
→ ALL 18 CHECKS PASSED (approval + audit)
```

```text
py -3.11 tests\test_capability_bench.py
→ ALL 145 CHECKS PASSED (capability benchmark)
```

```text
py -3.11 tests\test_encoding_paths.py
→ 11 checks passed
```

```text
py -3.11 tests\test_lig_providers.py
→ 20 checks passed
```

```text
py -3.11 tests\test_lig_runtime.py
→ 14 checks passed
```

```text
py -3.11 tests\test_probes.py
→ ALL 9 CHECKS PASSED (probes)
```

```text
py -3.11 tests\test_resume.py
→ 9 checks passed
```

```text
py -3.11 tests\test_tool_dispatch.py
→ ALL 23 CHECKS PASSED (tool dispatch)
```

```text
py -3.11 tests\test_toolcall_parser.py
→ 12 checks passed
```

```text
py -3.11 tests\test_ws_min.py
→ ALL 38 CHECKS PASSED (minimal websocket client)
```

## 5. 계획과 다르게 한 것 (deviation — 없으면 "없음". 사소한 것도 전부)

- 권고 항목인 `profile_warning` 한국어화는 이번 필수 수정 범위에서 채택하지 않았다. 필수 수정은 env override였고, 기존 경고 문구 변경은 사용자 출력 스냅샷 영향 가능성이 있어 별도 리뷰 지시가 있을 때 반영하는 편이 안전하다고 판단했다.
- self-review 과정에서 기존 테스트의 `sk-` 형태 fake 문자열을 `FAKE_DO_NOT_LEAK_12345`로 교체했다. 동작 변경은 없고 secret-like grep 노이즈 제거 목적이다.

## 6. 하지 않은 것 / pending (상태 어휘로)

- company validation pending: 실제 회사 gateway 및 실제 local OpenAI-compatible 서버 호출은 수행하지 않았다.
- app validation pending: P09-02의 route-selection/doctor 확장은 선행 해제 후 별도 작업이다.

## 7. 리뷰어에게 질문 / 결정 요청 (없으면 "없음")

없음

## 8. 다음 작업 제안 (STATUS 기준 다음 READY 확인 결과)

P09-01이 APPROVED되면 Fable이 P09-02를 READY로 전환할 수 있다. 현재 STATUS 기준 다음 Codex READY는 `P12-02 브라우저 CDP 어댑터 + chrome-debug.bat`다.
