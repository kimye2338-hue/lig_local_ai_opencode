# 작업 보고서: P18-02 (r1)

| 항목 | 값 |
|------|-----|
| 작업 | P18-02 RUNBOOK + audit 순환 + doctor 운영 섹션 |
| 수행자 | Codex |
| 날짜 | 2026-07-04 |
| 시작 HEAD | 65a801d |
| 결과 커밋 | 77418fa, 33f9d31 |
| 라운드 | r1 |

## 1. 결과 요약 (3줄 이내, 상태 어휘 사용)

implemented: 운영 RUNBOOK을 추가하고 audit append 전 회전, doctor `operations` 섹션을 구현했다.
locally validated: 작은 `LIG_AUDIT_MAX_BYTES`로 `.bak` 회전과 신규 audit 이어쓰기를 검증했다.
locally validated: doctor manual smoke에서 operations 필드와 RUNBOOK 존재 여부가 출력되는 것을 확인했다.

## 1.5 사용자 체감 변화 (선택 — 이 작업으로 사용자가 실제로 할 수 있게 된 것 한 문단)

장애가 났을 때 이제 "어느 파일을 먼저 보고 무엇을 실행할지"가 한 문서와 doctor 출력에 모인다. audit가 커져도 자동으로 `.bak` 회전되므로 장기 사용 중 로그 파일이 계속 비대해지는 위험이 줄고, doctor만 봐도 audit 크기, 마지막 기록, schedule 개수, RUNBOOK 존재 여부를 바로 확인할 수 있다.

## 2. 변경 파일과 이유 (파일별 1~2줄, 신규/수정 구분)

- 신규: `workspace-template/docs/RUNBOOK.md`
  - 7개 운영 증상을 기존 diagnostics/audit/schedule/results 경로와 실제 확인 명령에 연결했다.
- 수정: `workspace-template/agent_ops/audit.py`
  - `record()` append 직전 `LIG_AUDIT_MAX_BYTES` 기준으로 `audit_<YYYYMMDD_HHMMSS>.jsonl.bak` 회전을 수행한다.
- 수정: `workspace-template/agent_ops/doctor.py`
  - secret-free `operations` 섹션을 추가해 audit, schedule, RUNBOOK, 최신 work report 상태를 출력한다.
- 수정: `workspace-template/agent_ops/agentops.py`
  - Windows 콘솔에서 doctor JSON 출력이 한글/특수문자 때문에 실패하지 않도록 stdout UTF-8 reconfigure를 적용했다.
- 수정: `workspace-template/tests/test_approval_audit.py`
  - audit rotation 강제 테스트를 추가했다.

## 3. DoD 자가 점검 (task의 DoD를 전부 나열, 항목별 ✅ / ❌ / 부분 + 한 줄 근거)

- ✅ RUNBOOK 7개 증상, 전부 실제 진단 파일 경로·실제 명령과 연결: `workspace-template/docs/RUNBOOK.md`에 diagnostics/audit/schedule/results/doctor 기준으로 작성했다.
- ✅ audit 회전 테스트 통과: `LIG_AUDIT_MAX_BYTES=200`으로 `.bak` 생성과 신규 `audit.jsonl` 이어쓰기를 검증했다.
- ✅ doctor operations manual smoke 출력 첨부: 아래 테스트 증거에 operations 출력 핵심 필드를 첨부했다.
- ✅ 기존 checks 무손상: 전체 22개 `test_*.py` 파일이 통과.

## 4. 테스트 증거 (실행 명령 + 출력 마지막 줄 원문 붙여넣기 — 회귀 9개 + 신규 전부)

```text
py -3.11 workspace-template\tests\test_approval_audit.py
→ ALL 20 CHECKS PASSED (approval + audit)

py -3.11 workspace-template\agent_ops\agentops.py doctor
→ "operations": {
    "audit_file": "C:\\Users\\김예찬\\OpenCodeLIG_USERDATA\\audit\\audit.jsonl",
    "audit_size_bytes": 546875,
    "audit_last_ts": "2026-07-04T21:33:57",
    "audit_rotated": 0,
    "schedule_items": 0,
    "runbook": true,
    "last_work_report": ""
  }

전체 회귀: Get-ChildItem workspace-template\tests\test_*.py | Sort-Object Name 후 각 파일 py -3.11 실행
→ ALL 22 TEST FILES PASSED

py -3.11 workspace-template\scripts\precommit_scan.py --staged
→ Secret scan passed.
```

## 5. 계획과 다르게 한 것 (deviation — 없으면 "없음". 사소한 것도 전부)

doctor manual smoke 중 Windows cp949 콘솔에서 JSON 출력이 `UnicodeEncodeError`로 실패했다. task의 doctor smoke를 그대로 통과시키기 위해 `agentops.py doctor` 출력 직전에 stdout을 UTF-8로 재설정했다.

## 6. 하지 않은 것 / pending (상태 어휘로)

- reviewer validation pending: RUNBOOK 표의 운영 문구와 대응 순서 검토.
- reviewer validation pending: doctor `operations` 필드명이 스펙과 일치하는지 독립 확인.

## 7. 리뷰어에게 질문 / 결정 요청 (없으면 "없음")

없음.

## 8. 다음 작업 제안 (STATUS 기준 다음 READY 확인 결과)

P18-02는 capability/artifact kind/dependencies 변경이나 `available=True` 전환이 없어 hard gate가 아니다. auto-advance 조건 충족 시 STATUS 기준 다음 READY는 P19-01이다.
