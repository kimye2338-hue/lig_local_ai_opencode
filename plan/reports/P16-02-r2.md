# 작업 보고서: P16-02 (r2)

| 항목 | 값 |
|------|-----|
| 작업 | P16-02 matlab -batch / AutoCAD accoreconsole 어댑터 리뷰 수정 |
| 수행자 | Codex |
| 날짜 | 2026-07-04 |
| 시작 HEAD | 83bbbe0 |
| 결과 커밋 | efbb0ad |
| 라운드 | r2 (plan/reviews/P16-02-r1.md 반영) |

## 1. 결과 요약
- implemented: `tests/test_batch_adapters.py`의 fake 실행파일을 Windows `.cmd` / POSIX `.sh`+chmod 분기로 바꿨습니다.
- locally validated: AutoCAD UTF-16LE stdout 내용 assertion은 Windows에서만 실행되도록 `os.name == "nt"` 가드를 추가했습니다.
- locally validated: 어댑터/artifact 코드는 변경하지 않고, 리뷰 필수 수정인 테스트 이식성만 반영했습니다.

## 1.5 사용자 체감 변화
사용자가 직접 보는 기능 변화는 없습니다. 대신 리뷰/리눅스 환경에서도 P16-02의 hard-gate 검증(autocad_script artifact kind, registry, 사본 정책)이 끝까지 실행되어 승인 판단이 가능해집니다.

## 2. 변경 파일과 이유
- 수정 `workspace-template/tests/test_batch_adapters.py`
  - `_write_fake_exe()` 추가: Windows는 `.cmd`, POSIX는 실행권한이 있는 `.sh` fake executable 생성.
  - UTF-16LE stdout의 `"유니코드 로그"` 내용 검사는 Windows 전용으로 가드.
  - artifact-kind/registry/사본 정책 checks는 플랫폼 전용 블록 밖에서 계속 실행.

## 3. DoD 자가 점검
- ✅ r1 필수 수정: fake executable을 POSIX에서도 실행 가능하게 변경.
- ✅ UTF-16LE 내용 assertion Windows 가드: POSIX 리뷰 환경에서 hard fail 방지.
- ✅ hard-gate checks 보존: artifact kind metadata, capability, registry, quality checks 유지.
- ✅ 어댑터/artifact 코드 무변경: `matlab_batch.py`, `autocad_batch.py`, generator/quality/capability는 r1 그대로.
- ✅ 기존 P16-02 DoD 유지: `test_batch_adapters.py`, `test_capability_bench.py`, 전체 회귀 통과.

## 4. 테스트 증거

```text
py -3.11 workspace-template\tests\test_batch_adapters.py
→ ALL 24 CHECKS PASSED (batch adapters)

py -3.11 workspace-template\tests\test_capability_bench.py
→ ALL 193 CHECKS PASSED (capability benchmark)

py -3.11 workspace-template\tests\test_capability_floor.py
→ ALL 11 CHECKS PASSED (capability floor)

py -3.11 workspace-template\scripts\precommit_scan.py --staged
→ Secret scan passed.

py -3.11 <full test runner over workspace-template\tests\test_*.py>
→ ALL 22 TEST FILES PASSED
```

## 5. 계획과 다르게 한 것
없음. `plan/reviews/P16-02-r1.md`의 되는 방법을 그대로 적용했습니다.

## 6. 하지 않은 것 / pending
- app validation pending: 회사 MATLAB 2024a에서 실제 `-batch` 실행 검증.
- app validation pending: 회사 AutoCAD 2019 `accoreconsole.exe`에서 실제 DWG + SCR 실행 검증.
- adapter `available=True` 전환은 하지 않았습니다.

## 7. 리뷰어에게 질문 / 결정 요청
없음.

## 8. 다음 작업 제안
P16-02는 artifact kind 추가 hard gate 작업이므로 r2도 APPROVED 전 auto-advance하지 않습니다. 승인 후 다음 권장 순서는 `P18-02 → P16-03 → P16-04`입니다.
