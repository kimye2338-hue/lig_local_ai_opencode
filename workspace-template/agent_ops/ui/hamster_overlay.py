# -*- coding: utf-8 -*-
"""Hamster pet overlay placeholder for GitHub connector flow.

The complete binary-asset install ZIP contains the final animated implementation
and image assets. This branch keeps the lightweight source placeholder plus docs
so Fable can connect the overlay lifecycle to the existing OpenCodeLIG runtime.

Use the complete install ZIP supplied in the ChatGPT artifact for the full local
Windows implementation with tray, drag, saved position, and animation frames.
"""
from __future__ import annotations

from pathlib import Path


def main() -> None:
    here = Path(__file__).resolve().parent
    print("OpenCodeLIG Hamster Pet overlay source placeholder")
    print("Install the complete ZIP patch to get the full animated Windows overlay.")
    print("Expected asset folder:", here / "assets" / "hamster_pet")


if __name__ == "__main__":
    main()
