@echo off
rem ==========================================================================
rem DIAG_OPENCODE_LIG.bat (V4) - the ONE diagnostic. Prints GO/NO-GO with a
rem Korean explanation and a small copy-paste block for bug reports.
rem Safe to run any time; restores plugins afterwards.
rem Set LIG_NONINTERACTIVE=1 to skip the final pause (automation).
rem ==========================================================================
setlocal
chcp 65001 >nul
set "PYTHONUTF8=1"
set "PYTHONIOENCODING=utf-8"
call "%USERPROFILE%\OpenCodeLIG\scripts\lig_python.bat"
if not defined LIG_PY (pause & exit /b 9)
%LIG_PY% "%USERPROFILE%\OpenCodeLIG\scripts\lig_diag.py"
set "RC=%ERRORLEVEL%"
echo.
if not "%LIG_NONINTERACTIVE%"=="1" pause
exit /b %RC%
